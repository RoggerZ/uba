# Kafka-First EventBus 后续待办事项

> 最近更新：2026-05-17
> 当前基线：父仓 commit `7297118`；`analytics-core` commit `2d3063a`；`analytics-service` commit `171aca1`。
> 记录范围：只记录 Kafka-first EventBus 完成后拆出的生产硬化、真实集成验证和性能基线后续项，不重复记录已完成的 v1 契约、Redis/direct 适配、Sarama provider 基础实现和文档同步。

## 状态约定

- `待完成`：尚未进入实现。
- `进行中`：已有 active goal 或本地改动正在推进。
- `已完成`：代码、验证、文档和提交记录均已闭环。
- `暂缓`：方向成立，但暂不作为近期工作。

## 总览

| ID | 任务 | 状态 | 优先级 | 主要仓库 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| KAFKA-HARDEN-001 | 真实 Kafka broker 集成验证 | 已完成 | P0 | `src/analytics-core` | `analytics-core` commit `2d3063a` 已补真实 broker 集成测试，并在本机 `confluentinc/cp-kafka:7.5.0` / `127.0.0.1:9092` 通过 publish -> consume、乱序完成不提前提交、取消后同组 consumer replay |
| KAFKA-HARDEN-002 | Producer 可靠性默认值评审与落地 | 待完成 | P0 | `src/analytics-core` | 明确并测试 `acks=all` / Sarama `WaitForAll`、retry、required acks、flush 行为；若启用 idempotent producer，补齐 config 约束和失败用例 |
| KAFKA-HARDEN-003 | Consumer rebalance 与 ordered commit 集成压测 | 待完成 | P0 | `src/analytics-core` | 覆盖 rebalance、claim 关闭、in-flight message、partition committer flush/stop 语义；证明不会提前提交未完成 offset |
| KAFKA-HARDEN-004 | DLQ / retry / consumer lag / pause-resume 可观测性 | 待完成 | P1 | `src/analytics-core`、`src/analytics-service` | provider 暴露最小 metrics 或 stats；service 能输出 DLQ、retry、lag、pause/resume 相关日志或指标；文档写明排障入口 |
| KAFKA-HARDEN-005 | SASL/TLS 与 broker ACL 配置矩阵 | 待完成 | P1 | `src/analytics-core`、`src/analytics-service` | 配置结构支持 SASL/TLS/ACL 所需字段；本地测试可跳过真实认证 broker，但必须有配置解析、校验和错误信息测试 |
| KAFKA-HARDEN-006 | KafkaBus 性能基线 | 待完成 | P1 | `src/analytics-core` | 新增 Kafka publish 与 consume/commit benchmark；与 Redis Stream 现有基线同口径记录；明确数据量、broker、本机环境和结论边界 |
| KAFKA-HARDEN-007 | 生产 runbook 与故障演练文档 | 待完成 | P1 | 父仓文档、`src/analytics-service` | 记录生产推荐 env、topic 创建、DLQ replay/补偿、consumer group reset、常见告警和回滚路径 |

## 任务细化

### KAFKA-HARDEN-001：真实 Kafka broker 集成验证（已完成）

- 完成版本：`analytics-core` commit `2d3063a`。
- 实际 broker：本机已有 `confluentinc/cp-kafka:7.5.0` 容器，对外地址 `127.0.0.1:9092`，`kafka-topics --version` 输出 `7.5.0-ccs`。
- 通过命令：`ANALYTICS_CORE_KAFKA_INTEGRATION=1 ANALYTICS_CORE_KAFKA_BROKERS=127.0.0.1:9092 go test ./eventbus/kafka -run 'TestKafkaIntegration' -count=1 -v`。
- 覆盖测试：`TestKafkaIntegrationPublishConsume` 验证 publish -> consume；`TestKafkaIntegrationOrderedCommitWaitsForEarlierOffset` 验证 offset 1 先完成时不会在 offset 0 前提交，offset 0 释放后提交到 2；`TestKafkaIntegrationUncommittedMessageReplaysAfterRestart` 验证 handler 取消后 offset 不提交，新的同组 consumer 能 replay 原 offset 0。
- 额外验证：`go test ./eventbus/kafka`、`go test -run Example ./eventbus/kafka`、`go vet ./eventbus/kafka`、`golint ./eventbus/kafka`、`git diff --check -- eventbus/kafka/integration_test.go` 均通过。
- 踩坑记录：`docker compose up -d kafka` 拉取 `bitnami/kafka:3.9` 时被当前 Docker 镜像源返回 `403 Forbidden`，详见 `踩坑记录.md` 的 `KAFKA-EVENTBUS-009`。

### KAFKA-HARDEN-002：Producer 可靠性默认值评审与落地

- 背景：当前 Kafka provider 已能发布 JSON `EventEnvelope`，但生产默认可靠性策略还未拍板。
- 决策点：是否把 Sarama `Producer.RequiredAcks = WaitForAll` 作为生产默认；是否启用 idempotent producer；retry 和 flush 默认值是否按生产路径收紧。
- 验收重点：配置缺失时的错误信息明确；可靠性默认值有单测保护；如启用 idempotence，必须处理 Sarama 对 `Net.MaxOpenRequests` 等相关约束。

### KAFKA-HARDEN-003：Consumer rebalance 与 ordered commit 集成压测

- 背景：ordered committer 已覆盖乱序完成，但 rebalance / claim 关闭场景仍是生产风险点。
- 必须覆盖：`ConsumeClaim` 结束时 in-flight message 的处理策略；partition committer stop 后是否还允许提交；session 生命周期结束前的 flush/abort 边界。
- 验收重点：不能因为后面的 offset 完成较早而跳过前面的失败消息；不能在 DLQ 写失败时提交原 offset。

### KAFKA-HARDEN-004：DLQ / retry / consumer lag / pause-resume 可观测性

- 背景：v1 已有 retry、DLQ 和 protector 基础逻辑，但生产排障需要可观察信号。
- 候选指标：consume count、handler success/failure、retry count、DLQ write success/failure、partition lag、in-flight count、pause/resume state、ordered commit next offset。
- 验收重点：指标或日志不泄漏事件敏感 payload；service 侧能定位 topic、partition、offset、consumer group 和 DLQ topic。

### KAFKA-HARDEN-005：SASL/TLS 与 broker ACL 配置矩阵

- 背景：第一版明确不做 SASL/TLS 完整配置矩阵，但生产接入前需要补齐。
- 配置范围：brokers、client id、consumer group、topic、DLQ topic、SASL mechanism、username/password、TLS enable、CA/cert/key 路径或内容、ACL 需求说明。
- 验收重点：不把密钥写入文档样例；配置解析和缺失项报错可测试；本地无认证 broker 时允许只做配置层测试。

### KAFKA-HARDEN-006：KafkaBus 性能基线

- 背景：已有 Redis Stream 基线，KafkaBus 还缺同口径 benchmark。
- 建议口径：publish 单事件、publish batch 候选、consume + handler no-op + ordered commit、失败 retry、DLQ 写入。
- 验收重点：明确 benchmark 不等同 SLA；记录本机 CPU、Kafka 镜像、partition 数、message size、parallelism 和 commit batch 策略。

### KAFKA-HARDEN-007：生产 runbook 与故障演练文档

- 背景：代码能力落地后，需要 operator-facing 文档让生产接入不靠口头记忆。
- 必须包含：topic 创建建议、partition key 选择、consumer group 命名、env 样例、DLQ replay/补偿、consumer group reset、常见错误、回滚到 Redis Stream 的限制说明。
- 验收重点：文档只写可执行步骤和真实配置名，不写空泛架构宣传。

## 不重复纳入本待办的事项

- EventBus 公共契约重定义：已完成。
- Redis Stream 适配新契约：已完成。
- direct provider delivery id 语义收口：已完成。
- Sarama 基础 producer / consumer group / ordered commit / retry / DLQ：已完成。
- `analytics-service` Kafka env 和 runtime 装配：已完成。
