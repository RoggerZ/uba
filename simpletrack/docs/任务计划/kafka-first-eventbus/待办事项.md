# Kafka-First EventBus 后续待办事项

> 最近更新：2026-05-17
> 当前基线：父仓本轮同步 commit `81dd496`；KAFKA-PROD-006 文档同步为 `82da0d8`；KAFKA-PROD-007 文档同步为 `848494a`；`analytics-core` commit `d835341`；`analytics-service` commit `c6d1139`。
> 记录范围：只记录 Kafka-first EventBus 完成后拆出的生产硬化、真实集成验证和性能基线后续项，不重复记录已完成的 v1 契约、Redis/direct 适配、Sarama provider 基础实现和文档同步。

## 状态约定

- `待完成`：尚未进入实现。
- `进行中`：已有 active goal 或本地改动正在推进。
- `已完成`：代码、验证、文档和提交记录均已闭环。
- `暂缓`：方向成立，但暂不作为近期工作。

## 总览

| ID | 任务 | 状态 | 优先级 | 主要仓库 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| KAFKA-HARDEN-001 | 真实 Kafka broker 集成验证 | 已完成 | P0 | `src/analytics-core` | `analytics-core` commit `2d3063a` 已补真实 broker 集成测试，并在本机 `confluentinc/cp-kafka:7.5.0` / `127.0.0.1:9092` 通过 publish -> consume、乱序完成不提前提交、取消后同组 consumer replay |
| KAFKA-HARDEN-002 | Producer 可靠性默认值评审与落地 | 已完成 | P0 | `src/analytics-core` | `analytics-core` commit `cc8797c` 已把生产默认改为 `acks=all` / Sarama `WaitForAll`，补齐 producer retry、flush、`acks=none/leader/all` 映射、idempotent producer opt-in 约束和失败用例 |
| KAFKA-HARDEN-003 | Consumer rebalance 与 ordered commit 集成压测 | 已完成 | P0 | `src/analytics-core` | `analytics-core` commit `2e52263` 已覆盖 claim 关闭、generation abort、late completion 不提交 stale session、pause 状态 rebalance reconcile |
| KAFKA-HARDEN-004 | DLQ / retry / consumer lag / pause-resume 可观测性 | 已完成 | P1 | `src/analytics-core`、`src/analytics-service` | `analytics-core` commit `2e52263` 暴露 `Stats()`；`analytics-service` commit `9acadab` 暴露 `KafkaEventBusStats()`；覆盖 retry、DLQ、lag estimate、pause/resume counters |
| KAFKA-HARDEN-005 | SASL/TLS 与 broker ACL 配置矩阵 | 已完成 | P1 | `src/analytics-core`、`src/analytics-service` | `analytics-core` commit `2e52263` 支持 TLS、mTLS、SASL/PLAIN provider options；`analytics-service` commit `9acadab` 支持 env/runtime TLS/SASL 校验；ACL 矩阵写入 runbook |
| KAFKA-HARDEN-006 | KafkaBus 性能基线 | 已完成 | P1 | `src/analytics-core` | `analytics-core` commit `2e52263` 已新增 publish、process no-op、ordered committer benchmark，并记录本机 200ms 基线 |
| KAFKA-HARDEN-007 | 生产 runbook 与故障演练文档 | 已完成 | P1 | 父仓文档、`src/analytics-service` | 父仓 commit `279e001` 已新增生产运行手册与故障演练，覆盖 env、topic、ACL、DLQ、reset、replay、回滚限制 |
| KAFKA-PROD-001 | Kafka diagnostics 服务级观测出口 | 已完成 | P0 | `src/analytics-service` | `analytics-service` commit `0f695db` 已实现默认关闭的 `/v1/kafka/diagnostics` 内部 route，覆盖 process-scoped query token、worker queue、completion gate、ordered commit `next_offset` / `lag_estimate`、DLQ/retry/pause counters，并通过全量 Go 验证与 code-review 复审；父仓同步 commit `32b4dbf` |
| KAFKA-PROD-002 | Kafka metrics / exporter 友好出口 | 已完成 | P0 | `src/analytics-service` | `analytics-service` commit `c6d1139` 已新增默认关闭的 `/v1/kafka/metrics` Prometheus text route、`kafka_metrics` scope、config/runtime/OpenAPI/README/tests；已通过全量 Go 验证与提交前 code-review，父仓同步 commit `869553c` |
| KAFKA-PROD-003 | 真实认证 Kafka broker drill 入口 | 已完成 | P0 | `src/analytics-core`、父仓文档 | `analytics-core` commit `d835341` 已补 gated integration TLS / mTLS / SASL/PLAIN env 与认证 env 映射单测；本机 disposable `SASL_SSL` broker `127.0.0.1:39093` 已跑通 live publish / consume / ordered commit / replay 证据 |
| KAFKA-PROD-004 | 多 broker / 真实 broker benchmark 验证入口 | 已完成 | P0 | `src/analytics-core`、父仓文档 | `analytics-core` commit `d835341` 已新增 replicated-topic integration、broker outage gate 和真实 broker publish benchmark；本机 3 broker 临时集群 replicated/outage 测试通过，单 broker 真实 publish benchmark 已记录 |
| KAFKA-PROD-005 | 生产级多 broker `SASL_SSL` drill | 已完成 | P0 | 父仓文档、`src/analytics-core` 验证入口 | 父仓 `d444717` 新增 `local-auth-kafka-prod` 3 broker KRaft `SASL_SSL` drill；`127.0.0.1:39193,39194,39195` 下通过 publish/consume、ordered commit、restart replay、replicated publish/consume 和 outage gate；演练后 disposable 容器/network 已清理 |
| KAFKA-PROD-006 | 认证 broker benchmark | 已完成 | P1 | `src/analytics-core`、父仓文档 | 父仓 `82da0d8` 记录 `local-auth-kafka-prod` 三 broker `SASL_SSL` 环境下 `BenchmarkKafkaIntegrationPublish -count=3` 与固定 `10x` 复测；记录 broker 数、partition、RF、minISR、message size、acks/retry/flush、Go/Kafka/Docker/机器版本，并明确本机结果不是生产 SLA |
| KAFKA-PROD-007 | Kafka broker/exporter SLO 指标源 | 已完成 | P1 | 父仓文档 | 父仓 `848494a` 已明确 broker/exporter 指标与 service 进程内 metrics 的事实源分层；consumer lag 使用 Burrow / kafka-exporter / 托管 Kafka lag 指标，broker replication / offline / minISR / request latency 使用 broker JMX / exporter / 托管 Kafka metrics，DLQ / retry / pause / worker backpressure 使用 `analytics-service` 进程内 metrics |
| KAFKA-PROD-008 | SCRAM / OAuth / Kerberos 认证路线评审 | 待评审 | P2 | 父仓文档、后续可能涉及 `src/analytics-core` / `src/analytics-service` | 输出机制矩阵：先做/暂缓/否决、触发条件、配置范围、测试环境和回滚策略；未评审前 runbook 只声明 `SASL/PLAIN + TLS` 已支持 |

## 任务细化

### KAFKA-HARDEN-001：真实 Kafka broker 集成验证（已完成）

- 完成版本：`analytics-core` commit `2d3063a`。
- 实际 broker：本机已有 `confluentinc/cp-kafka:7.5.0` 容器，对外地址 `127.0.0.1:9092`，`kafka-topics --version` 输出 `7.5.0-ccs`。
- 通过命令：`ANALYTICS_CORE_KAFKA_INTEGRATION=1 ANALYTICS_CORE_KAFKA_BROKERS=127.0.0.1:9092 go test ./eventbus/kafka -run 'TestKafkaIntegration' -count=1 -v`。
- 覆盖测试：`TestKafkaIntegrationPublishConsume` 验证 publish -> consume；`TestKafkaIntegrationOrderedCommitWaitsForEarlierOffset` 验证 offset 1 先完成时不会在 offset 0 前提交，offset 0 释放后提交到 2；`TestKafkaIntegrationUncommittedMessageReplaysAfterRestart` 验证 handler 取消后 offset 不提交，新的同组 consumer 能 replay 原 offset 0。
- 额外验证：`go test ./eventbus/kafka`、`go test -run Example ./eventbus/kafka`、`go vet ./eventbus/kafka`、`golint ./eventbus/kafka`、`git diff --check -- eventbus/kafka/integration_test.go` 均通过。
- 踩坑记录：`docker compose up -d kafka` 拉取 `bitnami/kafka:3.9` 时被当前 Docker 镜像源返回 `403 Forbidden`，详见 `踩坑记录.md` 的 `KAFKA-EVENTBUS-009`。

### KAFKA-HARDEN-002：Producer 可靠性默认值评审与落地（已完成）

- 完成版本：`analytics-core` commit `cc8797c`；父仓同步 commit `3f97360`。
- 生产默认：`ProducerAcks` 默认为 `all`，内部映射到 Sarama `WaitForAll`；`ProducerRetryMax` 默认 `5`，`ProducerRetryBackoff` 默认 `100ms`。
- API 语义：`ProducerRetryMax` 与 `ProducerRetryBackoff` 改为指针；`nil` 表示使用可靠默认值，显式 `0` 保留为调用方选择，不再静默覆盖。
- idempotent producer：仍为显式 opt-in；启用后内部设置 `Producer.Idempotent=true`、`Net.MaxOpenRequests=1`，并拒绝 `acks != all` 或 `retry max == 0`。
- 覆盖测试：默认 durable config、`leader` / `none` acks 映射、retry / flush 映射、显式 0 retry/backoff、非幂等默认 in-flight、不合法配置和 idempotent Sarama 约束。
- 验证：`go test ./eventbus/kafka`、`go test -run Example ./eventbus/kafka`、`go vet ./eventbus/kafka`、`golint ./eventbus/kafka`、`go test ./...`、`go test -run Example ./...`、`go vet ./...`、`go doc -all ./eventbus/kafka`、目标文件 `git diff --check` 均通过。
- 阶段评审：code-review 初审指出 producer retry 零值语义缺口，修复后复审 `APPROVE`；code-simplifier / ai-slop-cleaner 只读检查 `APPROVE`。

### KAFKA-HARDEN-003：Consumer rebalance 与 ordered commit 集成压测（已完成）

- 背景：ordered committer 已覆盖乱序完成，但 rebalance / claim 关闭场景仍是生产风险点。
- 完成内容：`ConsumeClaim` 使用 generation id 绑定当前 claim 生命周期；claim 退出时 abort 当前 generation，late completion 不能再通过 stale Sarama session 标记提交。
- 覆盖测试：ordered commit 乱序完成、claim 关闭后 late completion、DLQ 写失败不提交原 offset、pause 状态在 rebalance 后按当前压力重新 reconcile。
- 完成版本：`analytics-core` commit `2e52263`。
- 验证：`go test ./eventbus/kafka -count=1`、`go test ./...`、`go test -run Example ./...`、`go vet ./...`、`go doc -all ./eventbus/kafka`、`golint ./eventbus/kafka`、`git diff --check` 均已通过。

### KAFKA-HARDEN-004：DLQ / retry / consumer lag / pause-resume 可观测性（已完成）

- 背景：v1 已有 retry、DLQ 和 protector 基础逻辑，但生产排障需要可观察信号。
- 完成内容：`analytics-core/eventbus/kafka.Bus.Stats()` 暴露诊断快照；`analytics-service/internal/runtime.Runtime.KafkaEventBusStats()` 提供窄出口，后续可接入 exporter 或结构化日志。
- 指标范围：consume count、handler success/failure/retry、malformed、DLQ success/failure、paused partitions、pause/resume transition、worker queue、completion gate、ordered commit next offset 与 lag estimate。
- 边界说明：`Stats()` 是诊断快照，不作为 billing、SLO 或精确 broker lag 的唯一事实源；不包含原始事件 payload。
- 完成版本：`analytics-core` commit `2e52263`；`analytics-service` commit `9acadab`。

### KAFKA-HARDEN-005：SASL/TLS 与 broker ACL 配置矩阵（已完成）

- 背景：第一版明确不做 SASL/TLS 完整配置矩阵，但生产接入前需要补齐。
- 完成内容：service env 增加 TLS enable、server name、CA、client cert/key、insecure skip verify、SASL enable、mechanism、username/password、handshake；runtime 组装 `*tls.Config` 并映射到 Kafka provider options。
- 校验范围：TLS cert/key 必须成对；TLS/SASL material 不能在未启用对应开关时静默忽略；当前只支持 SASL/PLAIN，SCRAM/OAuth/Kerberos fail fast。
- 文档范围：runbook 已补 ACL 矩阵、SASL/TLS 支持矩阵，并明确生产模板必须启用 TLS，明文 SASL/PLAIN 只能用于临时测试 broker。
- 完成版本：`analytics-core` commit `2e52263`；`analytics-service` commit `9acadab`。

### KAFKA-HARDEN-006：KafkaBus 性能基线（已完成）

- 背景：已有 Redis Stream 基线，KafkaBus 还缺同口径 benchmark。
- 完成内容：新增 `BenchmarkKafkaBusPublish`、`BenchmarkKafkaProcessNoopHandler`、`BenchmarkKafkaOrderedCommitterRegisterComplete`。
- 本机 200ms 基线：publish 约 `902.9 ns/op`、`825 B/op`、`10 allocs/op`；process no-op 约 `2119 ns/op`、`929 B/op`、`17 allocs/op`；ordered committer register/complete 约 `128.1 ns/op`、`224 B/op`、`4 allocs/op`。
- 边界说明：该 benchmark 是 provider 内部 CPU / allocation 基线，不等同真实 broker SLA；真实吞吐仍需要独立 broker、partition、message size、batch 和网络压测。
- 完成版本：`analytics-core` commit `2e52263`。

### KAFKA-HARDEN-007：生产 runbook 与故障演练文档（已完成）

- 背景：代码能力落地后，需要 operator-facing 文档让生产接入不靠口头记忆。
- 完成内容：新增 `生产运行手册与故障演练.md`，覆盖生产 env、topic 创建、partition key、ACL、SASL/TLS、诊断入口、DLQ 排查/补偿、consumer group reset、broker outage、DLQ write failure、rebalance、restart replay、回滚限制。
- 验收重点：文档使用真实 `ANALYTICS_SERVICE_*` 配置名；密钥以部署密钥系统注入，不写入仓库；Redis Stream 被明确为本地/小流量/测试实现，不作为 Kafka 无损生产回滚方案。
- 完成版本：父仓同步 commit `279e001`；关联代码版本为 `analytics-core` commit `2e52263` 与 `analytics-service` commit `9acadab`。

### KAFKA-PROD-001：Kafka diagnostics 服务级观测出口（已完成）

- 完成版本：`analytics-service` commit `0f695db`；父仓同步 commit `32b4dbf`；`analytics-core` 复用 commit `2e52263` 的 `eventbus/kafka.Bus.Stats()`。
- 完成内容：新增默认关闭的内部 `GET /v1/kafka/diagnostics`，由 `ANALYTICS_SERVICE_KAFKA_DIAGNOSTICS_ENABLED=true` 开启，并要求 `ANALYTICS_SERVICE_EVENTBUS=kafka` 与 query token。
- 鉴权边界：复用 query token 生命周期和 scope 机制，新增 `kafka_diagnostics` scope；该 route 是进程级诊断，不需要 `write_key`，也不会走 source readback policy。
- 响应范围：返回 topic、DLQ topic、worker pool、completion gate、ordered commit、paused partitions、retry / DLQ / pause-resume counters；`lag_estimate` 明确是本进程诊断估算，不是 broker 权威 lag。
- 已通过验证：`go test ./internal/config ./internal/collectapi ./internal/runtime -count=1`、`go test ./...`、`go test -run Example ./...`、`go vet ./...`、`golint ./internal/collectapi ./internal/config ./internal/runtime ./internal/controlplane`、目标包 `go doc -all`、`git diff --check`。
- 审查修复：code-review 初审指出 diagnostics 会被误带 `write_key` / `AllowedWriteKeys` 误伤，以及 `paused` 可能输出 `null`；已改为 process-scoped token 校验并固定空 `paused` 为 `{}`，补回归测试。
- 复审结论：code-review follow-up 返回 `APPROVE`，确认 `write_key` 误伤和空 `paused` 漂移均已收口。
- 收口说明：该接口只暴露进程内诊断快照，不回显原始事件 payload、broker secret、TLS/SASL material，也不作为精确 broker lag / SLO / billing 事实源。

### KAFKA-PROD-002：Kafka metrics / exporter 友好出口（已完成）

- 完成版本：`analytics-service` commit `c6d1139`；父仓同步 commit `869553c`。
- 完成内容：新增默认关闭的内部 `GET /v1/kafka/metrics`，由 `ANALYTICS_SERVICE_KAFKA_METRICS_ENABLED=true` 开启，并通过独立 `kafka_metrics` query-token scope 访问。
- 输出格式：Prometheus text exposition，包含 retry / DLQ / pause-resume counters、worker queue gauges、completion gate gauges、per topic-partition ordered commit gauges。
- 安全边界：该 route 是 process-scoped，不解析 `write_key`，不受 `AllowedWriteKeys` 误伤，不回显 raw payload、broker secret、TLS/SASL material；`simpletrack_kafka_ordered_commit_lag_estimate` 仍只是进程内估算。
- 已通过验证：`go test ./internal/config ./internal/collectapi ./internal/runtime -count=1`、`go test ./...`、`go test -run Example ./...`、`go vet ./...`、`golint ./internal/collectapi ./internal/config ./internal/runtime ./internal/controlplane`、目标包 `go doc -all`、`git diff --check`。
- 阶段审查：code-review 子代理 `Nash` 返回 `APPROVE`；code-simplifier / ai-slop-cleaner 子代理 `Galileo` 的 metrics 多 partition 渲染测试与 numeric helper drift 建议已落地。

### KAFKA-PROD-003：真实认证 Kafka broker drill 入口（已完成）

- 完成版本：`analytics-core` commit `d835341`；父仓同步 commit `869553c`。
- 子代理结论：TLS + SASL/PLAIN provider/runtime 配置链路已有单测和 runbook，但现有 live integration 仍是 plaintext broker；本项应补“认证 broker 真实跑过”的入口和证据。
- 完成内容：`eventbus/kafka` integration harness 读取 `ANALYTICS_CORE_KAFKA_TLS_*`、`ANALYTICS_CORE_KAFKA_SASL_*` env；补 `TestKafkaIntegrationOptionsReadAuthenticatedBrokerEnv` 锁定 TLS server name、insecure test knob、SASL/PLAIN username/password/handshake 映射。
- 运行入口：真实认证 broker 准备好后，使用同一组 env 运行既有 `TestKafkaIntegrationPublishConsume`、`TestKafkaIntegrationOrderedCommitWaitsForEarlierOffset`、`TestKafkaIntegrationUncommittedMessageReplaysAfterRestart`。
- 已通过验证：`go test ./eventbus/kafka -count=1`、`go test ./...`、`go test -run Example ./...`、`go vet ./...`、`golint ./eventbus/kafka`、`go doc -all ./eventbus/kafka`、`git diff --check`。
- 本机认证证据：2026-05-17 在 `simpletrack-kafka-auth` disposable 单 broker KRaft `SASL_SSL` 环境下，`127.0.0.1:39093` 通过 `TestKafkaIntegrationPublishConsume`、`TestKafkaIntegrationOrderedCommitWaitsForEarlierOffset`、`TestKafkaIntegrationUncommittedMessageReplaysAfterRestart`。
- 边界说明：本轮证据覆盖 TLS CA + SASL/PLAIN 的本地认证链路；生产级多 broker 认证集群、SCRAM/OAuth/Kerberos 和认证 broker benchmark 仍独立评审。

### KAFKA-PROD-004：多 broker / 真实 broker benchmark 验证入口（已完成）

- 完成版本：`analytics-core` commit `d835341`；父仓同步 commit `869553c`。
- 子代理结论：现有 integration 是单 broker / replication factor 1，现有 benchmark 是 fake producer 内部 CPU/allocation 基线；不能宣称已经覆盖 cross-broker failure 和真实 broker throughput。
- 完成内容：新增 `ANALYTICS_CORE_KAFKA_REPLICATED_INTEGRATION=1` gated 的 `TestKafkaReplicatedIntegrationPublishConsume`，默认创建至少 3 partitions、replication factor 3、`min.insync.replicas=2` 的生产形态 topic；新增 `ANALYTICS_CORE_KAFKA_OUTAGE_INTEGRATION=1` gated 的 `TestKafkaReplicatedIntegrationSurvivesBrokerOutage`，只允许停止显式指定的一次性演练 broker 容器。
- Benchmark 入口：新增 `ANALYTICS_CORE_KAFKA_BENCHMARK=1 go test ./eventbus/kafka -bench BenchmarkKafkaIntegrationPublish -benchmem`，用于真实 broker SyncProducer publish 成本记录。
- 本机多 broker 证据：临时 Confluent 3 broker 集群 `codex-kafka-prod-1/2/3` + `codex-kafka-prod-zk` 下，`TestKafkaReplicatedIntegrationPublishConsume` 通过；`TestKafkaReplicatedIntegrationSurvivesBrokerOutage` 在停止 `codex-kafka-prod-3` 后通过，随后临时集群已清理。
- 本机真实 broker benchmark：已有单 broker `127.0.0.1:9092` 下 `BenchmarkKafkaIntegrationPublish` 三次结果约 `1332729 ns/op`、`1028574 ns/op`、`1085085 ns/op`，内存约 `7011-7214 B/op`、`78 allocs/op`。
- 边界说明：还没有写入生产 SLA 阈值；更大规模 broker、partition、message size、跨机网络和认证 broker benchmark 仍应在独立压测环境归档。

### KAFKA-PROD-005：生产级多 broker `SASL_SSL` drill（已完成）

- 目标：在 3 broker `SASL_SSL` 环境中复跑认证链路和 replicated/outage gate，补齐比本机单 broker 更接近生产的拓扑证据。
- 入口文档：`生产级认证与压测计划.md` 阶段 10。
- 完成内容：新增 `local-auth-kafka-prod`，使用 3 broker KRaft controller quorum，internal/external listener 都为 `SASL_SSL`，每个 broker 使用独立 server certificate，共用 CA/truststore/JAAS。
- 本机认证多 broker 证据：2026-05-17 在 `confluentinc/cp-kafka:7.5.0` 三 broker 环境下，`127.0.0.1:39193,127.0.0.1:39194,127.0.0.1:39195` 通过 `TestKafkaIntegrationPublishConsume`、`TestKafkaIntegrationOrderedCommitWaitsForEarlierOffset`、`TestKafkaIntegrationUncommittedMessageReplaysAfterRestart`、`TestKafkaReplicatedIntegrationPublishConsume`、`TestKafkaReplicatedIntegrationSurvivesBrokerOutage`。
- Topic detail：`publish-consume`、`ordered-commit`、`restart-replay` 三类 topic 为 1 partition、RF=3、`min.insync.replicas=2`；`replicated-publish-consume`、`replicated-outage` 两类 topic 为 3 partitions、RF=3、`min.insync.replicas=2`。
- 运行命令：`. 'C:\Users\admin\Documents\src\uba\.tmp\kafka-auth-prod\certs\analytics-core-auth-prod-env.ps1'; go test ./eventbus/kafka -run 'TestKafkaIntegration(PublishConsume|OrderedCommitWaitsForEarlierOffset|UncommittedMessageReplaysAfterRestart)$|TestKafkaReplicatedIntegration(PublishConsume|SurvivesBrokerOutage)$' -count=1 -v`。
- 主题形态：普通认证测试沿用单 partition，以保护 ordered commit 语义；replicated tests 内部创建至少 3 partitions、replication factor 3、`min.insync.replicas=2`。
- 清理证据：`Stop-KafkaSaslSslProdDrill.ps1 -RemoveVolumes` 已清理 disposable 容器和 `simpletrack-kafka-auth-prod_default` network；`.tmp/kafka-auth-prod/` 只保留本机证书材料且不提交。
- 边界：不把该环境当生产部署模板；不提交证书、私钥、JAAS 或测试密码；认证 broker benchmark 仍由 KAFKA-PROD-006 单独推进。

### KAFKA-PROD-006：认证 broker benchmark（已完成）

- 目标：在 `SASL_SSL` broker 下跑真实 broker publish benchmark，形成可复测的认证开销观察基线。
- 入口文档：`生产级认证与压测计划.md` 阶段 11。
- 完成版本：`analytics-core` commit `d835341`；父仓文档同步为 `82da0d8`。
- 运行环境：`local-auth-kafka-prod` 3 broker KRaft `SASL_SSL`，`127.0.0.1:39193,39194,39195`，`confluentinc/cp-kafka:7.5.0` / `7.5.0-ccs`，Docker Server `28.5.2`，Go `go1.25.4 windows/amd64`，CPU `Intel(R) Core(TM) i5-14500`。
- Topic 与 producer 参数：benchmark topic 形如 `analytics.events.integration.benchmark-publish.<runID>`，1 partition、RF=3、`min.insync.replicas=2`；payload 约 256 bytes；producer 默认 `acks=all`、retry `5`、retry backoff `100ms`，flush 相关阈值未设置，未启用 idempotent producer。
- 验收命令：`. 'C:\Users\admin\Documents\src\uba\.tmp\kafka-auth-prod\certs\analytics-core-auth-prod-env.ps1'; $env:ANALYTICS_CORE_KAFKA_BENCHMARK='1'; go test ./eventbus/kafka -bench BenchmarkKafkaIntegrationPublish -benchmem -run '^$' -count=3 -v`。
- 默认结果：约 `275.2ms/op`、`293.7ms/op`、`1005.9ms/op`；自动校准只跑 1 到 4 次，波动较大。
- 固定样本复测：`-benchtime=10x -count=3` 约 `108.9-117.3ms/op`、`82.3-111.5KB/op`、`342-442 allocs/op`，作为当前本机复测基线。
- 边界：本机 Docker benchmark 只作为认证链路相对基线，不写成生产 SLA；后续更大规模 partition、message size、batch/async producer 和跨机网络压测另立任务。

### KAFKA-PROD-007：Kafka broker/exporter SLO 指标源（已完成）

- 目标：把 broker/exporter 权威指标与 `analytics-service` 进程内 diagnostics / metrics 分层。
- 入口文档：`生产级认证与压测计划.md` 阶段 12。
- 完成版本：父仓文档同步为 `848494a`；`analytics-core` commit `d835341`；`analytics-service` commit `c6d1139`。
- 完成内容：指标事实源表已写入 `生产级认证与压测计划.md` 阶段 12，并同步到 `生产运行手册与故障演练.md` 的观测入口。
- 事实源分层：consumer group lag 以 Burrow、kafka-exporter、托管 Kafka consumer-lag 指标或 broker end offset + committed offset 计算为准；under replicated partitions、offline partitions、minISR violation、broker request latency / error rate 以 broker JMX、JMX exporter、kafka-exporter 或托管 Kafka metrics 为准。
- 进程内指标：`/v1/kafka/metrics` 与 `/v1/kafka/diagnostics` 只作为 `analytics-service` 本地可靠性信号，覆盖 DLQ success/failure、handler retry/failure、pause/resume、worker queue、completion gate 和 ordered commit 本地状态。
- 边界：`simpletrack_kafka_ordered_commit_lag_estimate` 仍是进程内估算，不是 broker 权威 lag；本阶段不新增代码、不写生产阈值、不落 dashboard / alert rules。

### KAFKA-PROD-008：SCRAM / OAuth / Kerberos 认证路线评审（待评审）

- 目标：决定是否以及何时支持 `SCRAM-SHA-256/512`、`OAuthBearer`、`Kerberos/GSSAPI`。
- 入口文档：`生产级认证与压测计划.md` 阶段 13。
- 验收：输出机制矩阵，包含触发条件、实现范围、测试环境和回滚策略。
- 边界：未评审完成前，生产 runbook 继续只声明 `SASL/PLAIN + TLS` 为已支持路径。

## 不重复纳入本待办的事项

- EventBus 公共契约重定义：已完成。
- Redis Stream 适配新契约：已完成。
- direct provider delivery id 语义收口：已完成。
- Sarama 基础 producer / consumer group / ordered commit / retry / DLQ：已完成。
- `analytics-service` Kafka env 和 runtime 装配：已完成。
