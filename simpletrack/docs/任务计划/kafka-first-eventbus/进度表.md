# Kafka-First EventBus 进度表

> 最近更新：2026-05-18

## 总览

| 阶段 | 状态 | 当前证据 | 下一步 |
| --- | --- | --- | --- |
| 阶段 0：流程重整 | 已完成 | 已创建本任务 plan 目录，且 Kafka provider 半成品代码已恢复到暂停状态 | 恢复实现时进入阶段 1 |
| 阶段 1：边界确认与代码映射 | 已完成 | 两个子代理已完成 goal-mode 映射：xwl_bi 可迁移 ordered commit / completion gate / protector 思路；service 影响面集中在 config/runtime/README | 已进入实现收口 |
| 阶段 2：EventBus 契约与现有 provider 适配 | 已完成 | `analytics-core` 已改为 provider-owned ack/retry/DLQ；direct / Redis Stream / ingestion 目标包测试通过 | 等待阶段评审综合结论 |
| 阶段 3：Kafka provider 核心实现 | 已完成 | 新增 Sarama Kafka provider、ordered commit、completion gate、worker pool、protector、DLQ、单测与 gated 集成测试 | 等待阶段评审综合结论 |
| 阶段 4：service 装配与本地依赖 | 已完成 | `analytics-service` 已支持 Kafka env/runtime；`analytics-core/docker-compose.yml` 已增加单节点 Kafka；service 目标包测试通过 | 等待阶段评审综合结论 |
| 阶段 5：验证与正式文档同步 | 已完成 | 两个子仓 `go test ./...`、`go test -run Example ./...`、`go vet ./...` 已通过；architect 评审为 WATCH；code-simplifier / ai-slop-cleaner 的 worker pool must-fix 已处理；code-review 子代理连续被 budget 限制打断，未算完成；`analytics-core` `2b4f38d`、`analytics-service` `171aca1` 与父仓同步 `cb5be3a` 已提交推送 | 后续 Kafka 生产硬化项另起评审 |
| 阶段 6：评审 watch 收口 | 已完成 | 已用 `analytics-core` `4f42947` 统一 `Message.ID` delivery-id 语义，并把 Kafka v1 生产硬化拆成明确后续项；父仓同步 commit `9231441` 已推送 | 后续 Kafka 生产硬化项另起评审 |
| 阶段 7：Kafka 生产硬化 003-007 | 已完成 | `analytics-core` `2e52263` 已补 rebalance-safe ordered commit、诊断 `Stats()`、TLS/SASL options 和 KafkaBus benchmark；`analytics-service` `9acadab` 已补 TLS/SASL env/runtime 校验与 `KafkaEventBusStats()`；runbook 已补生产 TLS、ACL、DLQ、reset、故障演练 | 父仓同步 commit `279e001` 已记录 |
| 阶段 8：Kafka diagnostics 服务级出口 | 已完成 | `analytics-service` commit `0f695db` 已实现默认关闭的内部 `/v1/kafka/diagnostics` route、OpenAPI、README 与测试；`go test ./...`、`go test -run Example ./...`、`go vet ./...`、`golint` 目标包已通过；code-review follow-up `APPROVE`；父仓同步 commit `32b4dbf` | 后续仅评审 exporter / SLO / 真实认证 broker 诊断接入 |
| 阶段 9：Kafka PROD 002-004 | 已完成 | `analytics-service` commit `c6d1139` 已补默认关闭的 Prometheus `/v1/kafka/metrics` route；`analytics-core` commits `06893f9` / `d835341` 已补认证 broker env harness、replicated topic integration、broker outage gate 与真实 broker publish benchmark；本机 disposable `SASL_SSL` broker `127.0.0.1:39093` 下认证 publish/consume、ordered commit、restart replay 三项 integration 已通过；全量 Go 验证、阶段审查、子仓提交推送均完成 | 父仓本轮同步 commit `869553c`；后续只评审生产级认证集群、SCRAM/OAuth/Kerberos、SLO 与更大规模压测 |
| 阶段 10：生产级认证与压测计划 | 已完成 | 父仓 commit `c31750e` 已推送本机单 broker `SASL_SSL` drill 证据；父仓 `d444717` 新增 3 broker KRaft `SASL_SSL` drill；父仓 `82da0d8` 记录认证 broker benchmark，固定 `10x` 复测约 `108.9-117.3ms/op`；父仓 `848494a` 已补 broker/exporter SLO 指标源分层；KAFKA-PROD-008 文档同步为 `8f60208`，结论为 SCRAM 先做候选、OAuth 暂缓、Kerberos 当前否决 | 后续按真实部署需要另开 SCRAM 代码任务，dashboard / alert rules 与生产阈值仍独立评审 |

## 实时记录

| 时间 | 事项 | 状态 | 证据 / 备注 |
| --- | --- | --- | --- |
| 2026-05-17 | 用户调整执行规范：降低文档频率，提高代码推进频率，阶段性交付再评审 | 已接收 | 本目录作为轻量任务计划，不替代长期实施决策库 |
| 2026-05-17 | 创建 active goal，后续按 goal 模式推进 | 已完成 | 目标：重整 Kafka-first EventBus 实施流程 |
| 2026-05-17 | 暂停当前 Kafka provider 实现 | 已完成 | `src/analytics-core` 已恢复到仅剩无关 `.idea/` 未跟踪项；Kafka 代码后续从阶段 1 重新推进 |
| 2026-05-17 | 创建任务 plan 目录 | 已完成 | `simpletrack/docs/任务计划/kafka-first-eventbus/` |
| 2026-05-17 | 用户补充文档归档与提交/push 频率规则 | 已接收 | 过程性文档只写本目录；任务完成后再汇总正式文档；阶段性 commit，低频 push；push 失败走 `http://localhost:64320` 重试 |
| 2026-05-17 | 开始执行 Kafka-first EventBus 改造 | 进行中 | 进入阶段 1/2；先改公共契约与现有 provider，后台 explorer 做 service 和 xwl_bi 映射 |
| 2026-05-17 | 子代理映射完成 | 已完成 | xwl_bi 映射与 analytics-service 影响面映射均完成；父代理已整合结果 |
| 2026-05-17 | analytics-core EventBus 契约与 Redis/direct 适配完成 | 已完成 | `go test ./eventbus/direct ./eventbus/redisstream ./ingestion ./eventbus/kafka` 通过 |
| 2026-05-17 | Kafka provider 核心实现完成 | 已完成 | `eventbus/kafka` 新增 Sarama producer/consumer、ordered commit、completion gate、DLQ、gated integration test；`go test ./eventbus/kafka` 通过 |
| 2026-05-17 | analytics-service Kafka 装配完成 | 已完成 | `go test ./internal/config ./internal/runtime ./internal/collectapi` 通过 |
| 2026-05-17 | 全量验证完成 | 已完成 | `analytics-core` 与 `analytics-service` 均通过 `go test ./...`、`go test -run Example ./...`、`go vet ./...`；`golint` 目标范围通过；`docker compose config --quiet` 通过 |
| 2026-05-17 | 阶段性交付评审启动 | 进行中 | 已按 goal-mode 启动 `code-reviewer`、`architect`、`code-simplifier + ai-slop-cleaner` 只读子代理 |
| 2026-05-17 | 阶段性评审结论收口 | 进行中 | architect：WATCH，无 BLOCK；code-simplifier / ai-slop-cleaner：指出 `worker_pool.go` must-fix，已补 handler panic retry/DLQ、worker pool panic/close/cancel 测试并重跑验证；code-review 子代理三次因 budget 限制未完成，不冒充通过 |
| 2026-05-17 | analytics-core 注释规范补强 | 已完成 | 按根目录 Go 注释规范补齐 EventBus provider 内部核心边界注释；`analytics-core` commit `2b4f38d` 已推送；验证：`go test ./...`、`go test -run Example ./...`、`go vet ./...`、`golint ./...`、相关 `go doc -all` |
| 2026-05-17 | 正式实施决策文档同步 | 已完成 | 已把精简结论写入 `simpletrack/docs/实施决策/README.md`、`修订记录.md`、`分阶段实施计划.md`、`analytics-core实施方案.md`，并回填 `analytics-core` `2b4f38d`、`analytics-service` `171aca1`、父仓同步 `cb5be3a` |
| 2026-05-17 | Message.ID 契约 watch 收口 | 已完成 | `eventbus.Message.ID` 明确为 provider-native delivery id；`Envelope.ID` 才是业务事件 id；direct provider 改为 `direct:N`；`analytics-core` commit `4f42947` 已推送；父仓同步 commit `9231441` 已推送；事件总线与 core 全量验证已通过 |
| 2026-05-17 | KAFKA-HARDEN-001 真实 Kafka broker 集成验证 | 已完成 | `analytics-core` commit `2d3063a` 已推送；本机 `confluentinc/cp-kafka:7.5.0` / `127.0.0.1:9092` 下 `TestKafkaIntegrationPublishConsume`、`TestKafkaIntegrationOrderedCommitWaitsForEarlierOffset`、`TestKafkaIntegrationUncommittedMessageReplaysAfterRestart` 均通过；父仓同步 commit `7297118` |
| 2026-05-17 | KAFKA-HARDEN-002 Producer 可靠性默认值评审与落地 | 已完成 | `analytics-core` commit `cc8797c` 已推送；默认 `acks=all` / `WaitForAll`，retry 默认 `5` / `100ms`，retry 指针字段区分 `nil` 默认与显式 `0`，idempotent producer opt-in 并校验 `acks=all`、`retry max >= 1`；父仓同步 commit `3f97360` |
| 2026-05-17 | KAFKA-HARDEN-003 Consumer rebalance 与 ordered commit 集成压测 | 已完成 | `analytics-core` commit `2e52263` 已实现 generation-bound claim、claim close abort、late completion 不提交 stale session、rebalance 后 pause reconcile；验证已通过 `go test ./eventbus/kafka -count=1`、`go test ./...`、`go test -run Example ./...`、`go vet ./...`、`go doc -all ./eventbus/kafka`、`golint ./eventbus/kafka`、`git diff --check` |
| 2026-05-17 | KAFKA-HARDEN-004 DLQ / retry / consumer lag / pause-resume 可观测性 | 已完成 | `analytics-core` commit `2e52263` 已新增 `Bus.Stats()` 诊断快照，`analytics-service` commit `9acadab` 已新增 `Runtime.KafkaEventBusStats()` 窄出口；覆盖 consumed、handler success/failure/retry、DLQ success/failure、pause/resume、worker queue、completion gate、ordered commit lag estimate |
| 2026-05-17 | KAFKA-HARDEN-005 SASL/TLS 与 broker ACL 配置矩阵 | 已完成 | `analytics-core` commit `2e52263` 已支持 TLS 与 SASL/PLAIN provider options，`analytics-service` commit `9acadab` 已支持 TLS/SASL env、配置校验和 runtime TLS assembly；runbook 已写 ACL 与 SASL/TLS 矩阵，并补生产必须启用 TLS 的提醒 |
| 2026-05-17 | KAFKA-HARDEN-006 KafkaBus 性能基线 | 已完成 | `analytics-core` commit `2e52263` 已新增 Kafka publish、process no-op、ordered committer benchmark；本机 200ms 基线约为 `902.9 ns/op`、`2119 ns/op`、`128.1 ns/op` |
| 2026-05-17 | KAFKA-HARDEN-007 生产 runbook 与故障演练文档 | 已完成 | 父仓 commit `279e001` 已新增 `生产运行手册与故障演练.md`，覆盖 env、topic、ACL、SASL/TLS、DLQ、consumer group reset、broker outage、DLQ write failure、rebalance、restart replay、回滚限制 |
| 2026-05-17 | KAFKA-HARDEN-003 到 007 阶段评审 | 已完成 | code-review 初审 `REQUEST CHANGES`，指出 rebalance 后 stale pause state 与 TLS/SASL material 静默忽略，均已修复；复审 `APPROVE`。code-simplifier / ai-slop-cleaner 返回 `COMMENT`，must-fix doc variable drift 已修复，剩余为非阻塞 cleanup 建议。architect 返回 `WATCH`，提醒 `Stats()` 仅为诊断快照、生产文档需强调 TLS；runbook 已补 TLS 提醒 |
| 2026-05-17 | KAFKA-PROD-001 Kafka diagnostics 服务级观测出口启动 | 已完成 | 已创建 goal；基于 `analytics-service` commit `9acadab` 启动实现；子代理只读锚点查询遇到 `502 Bad Gateway`，不计完成，主代理本地推进 |
| 2026-05-17 | KAFKA-PROD-001 代码实现与首轮验证 | 已完成 | `analytics-service` 已新增 `ANALYTICS_SERVICE_KAFKA_DIAGNOSTICS_ENABLED`、`ANALYTICS_SERVICE_KAFKA_DIAGNOSTICS_PATH`、`kafka_diagnostics` scope、`GET /v1/kafka/diagnostics`、OpenAPI 与 README；已通过 `go test ./internal/config ./internal/collectapi ./internal/runtime -count=1`、`go test -run Example ./internal/collectapi -count=1`、`go test ./...`、`go vet ./...`、`git diff --check` |
| 2026-05-17 | KAFKA-PROD-001 初审阻塞修复 | 已完成 | code-review 初审 `REQUEST CHANGES`：1) diagnostics 仍可能被误带 `write_key` / `AllowedWriteKeys` 误伤；2) 空 `paused` 可能返回 `null`。已修复为 process-scoped token 校验并固定空 paused map 为 `{}`，补 `TestKafkaDiagnosticsIgnoresWriteKeyAllowlist`、`TestKafkaDiagnosticsReturnsEmptyPausedObject`、runtime clone 回归测试；复跑 `go test ./...`、`go test -run Example ./...`、`go vet ./...`、`golint` 目标包、目标包 `go doc -all`、`git diff --check` 均通过 |
| 2026-05-17 | KAFKA-PROD-001 follow-up 复审 | 已完成 | code-review follow-up 返回 `APPROVE`，确认 diagnostics 已走 `requireProcessQueryToken`，不再被 `write_key` / `AllowedWriteKeys` 误伤；空 `paused` 已由测试锁定为 `{}` |
| 2026-05-17 | KAFKA-PROD-001 子仓提交推送 | 已完成 | `analytics-service` commit `0f695db` 已提交并推送到 `main`；父仓 gitlink 已更新到 `0f695db`，本任务目录和正式实施决策文档正在回填父仓 commit |
| 2026-05-17 | KAFKA-PROD-002/003/004 goal 启动 | 已完成 | 已创建 active goal；初始两个子代理 `Bacon` / `Ampere` 均遇到 responses upstream `502 Bad Gateway`，不计完成；随后重发窄范围只读子任务 |
| 2026-05-17 | KAFKA-PROD-003 子代理映射 | 已完成 | `Leibniz` 只读结论：TLS + SASL/PLAIN 代码/单测已有，但 live integration 仍是 plaintext，最小交付是认证 broker env harness + 真实 broker drill 证据入口；未改文件 |
| 2026-05-17 | KAFKA-PROD-004 子代理映射 | 已完成 | `Mendel` 只读结论：现有 integration 是单 broker / replication factor 1，benchmark 是 fake producer；最小交付是 replicated topic integration 入口 + 真实 broker benchmark + outage 证据门槛；未改文件 |
| 2026-05-17 | KAFKA-PROD-002 metrics 出口实现 | 已完成 | `analytics-service` commit `c6d1139` 已新增 `ANALYTICS_SERVICE_KAFKA_METRICS_ENABLED`、`ANALYTICS_SERVICE_KAFKA_METRICS_PATH`、`kafka_metrics` scope、`GET /v1/kafka/metrics`、Prometheus text 渲染、OpenAPI、README 与测试；已通过目标包、全量 `go test ./...`、Example、`go vet`、`golint`、`go doc -all`、`git diff --check` |
| 2026-05-17 | KAFKA-PROD-003/004 验证入口实现 | 已完成 | `analytics-core` commit `06893f9` 已新增 TLS / mTLS / SASL/PLAIN env harness、replicated-topic gated test、真实 broker publish benchmark；commit `d835341` 又补 `TestKafkaReplicatedIntegrationSurvivesBrokerOutage`，只停止显式指定的 disposable broker container |
| 2026-05-17 | KAFKA-PROD-002/003/004 提交前审查 | 已完成 | code-review 子代理 `Nash` 返回 `APPROVE`；code-simplifier / ai-slop-cleaner 子代理 `Galileo` 无 MUST，提出的 multi-partition metrics render test 与 numeric helper drift 建议已修复后复跑验证 |
| 2026-05-17 | KAFKA-PROD-004 真实 broker 验证 | 已完成 | 本机 plaintext broker `127.0.0.1:9092` 下 `TestKafkaIntegrationPublishConsume`、`TestKafkaIntegrationOrderedCommitWaitsForEarlierOffset`、`TestKafkaIntegrationUncommittedMessageReplaysAfterRestart` 通过；真实 broker publish benchmark 三次约 `1332729` / `1028574` / `1085085 ns/op` |
| 2026-05-17 | KAFKA-PROD-004 多 broker / outage 验证 | 已完成 | 临时 3 broker Confluent 集群 `codex-kafka-prod-1/2/3` 下 replicated publish/consume 通过；停止 disposable `codex-kafka-prod-3` 后 `TestKafkaReplicatedIntegrationSurvivesBrokerOutage` 通过；临时集群已清理 |
| 2026-05-17 | KAFKA-PROD-003 认证 broker 证据边界 | 已完成 | `analytics-core` commit `d835341` 已提供认证 env harness 与 runbook 命令；本轮新增本机 disposable `SASL_SSL` broker drill，并在 `127.0.0.1:39093` 下跑通 publish/consume、ordered commit、restart replay 三项认证 integration；该证据覆盖本地认证路径，不替代生产级多 broker 认证集群演练 |
| 2026-05-17 | KAFKA-PROD-003 本机 SASL_SSL drill | 已完成 | `Start-KafkaSaslSslDrill.ps1 -TimeoutSeconds 120` 启动 `simpletrack-kafka-auth`；加载 `C:\Users\admin\Documents\src\uba\.tmp\kafka-auth\certs\analytics-core-auth-env.ps1` 后执行 `go test ./eventbus/kafka -run 'TestKafkaIntegration(PublishConsume|OrderedCommitWaitsForEarlierOffset|UncommittedMessageReplaysAfterRestart)$' -count=1 -v`，三项测试全部 PASS；父仓文档同步 commit `c31750e` 已推送 |
| 2026-05-17 | KAFKA-PROD-002/003/004 子仓提交推送 | 已完成 | `analytics-service` `c6d1139` 已推送；`analytics-core` `06893f9` / `d835341` 已推送；父仓 gitlink 已更新到 `c6d1139` / `d835341`，本轮父仓同步 commit `869553c` |
| 2026-05-17 | KAFKA-PROD-003 drill 文档提交推送 | 已完成 | 父仓 commit `c31750e` 已记录本机 `SASL_SSL` drill 脚本、证据和踩坑；首次直连 `git push origin master` 失败，随后用 `http://localhost:64320` 代理推送成功 |
| 2026-05-17 | KAFKA-PROD-005 到 008 下一阶段计划 | 进行中 | 已新增 `生产级认证与压测计划.md`，把生产级多 broker `SASL_SSL`、认证 broker benchmark、broker/exporter SLO 指标源、SCRAM/OAuth/Kerberos 路线评审拆成阶段 10-13；本轮文档同步由当前提交记录承载 |
| 2026-05-17 | KAFKA-PROD-005 子代理并行摸底 | 已跳过 | 两个只读子代理均遇到 responses upstream `502 Bad Gateway`；不计完成、不等待，父代理本地读取 integration tests 与任务文档后继续推进 |
| 2026-05-17 | KAFKA-PROD-005 生产级 3 broker SASL_SSL drill | 已完成 | 新增 `local-auth-kafka-prod`；第一次 Zookeeper 方案被 broker JAAS 影响 Zookeeper preflight，已改成 3 broker KRaft controller quorum；`Start-KafkaSaslSslProdDrill.ps1 -TimeoutSeconds 300` 启动 `simpletrack-kafka-auth-prod-1/2/3`，对外 `127.0.0.1:39193,39194,39195` |
| 2026-05-17 | KAFKA-PROD-005 目标 integration 验证 | 已完成 | 在 `analytics-core` commit `d835341` 下加载 `.tmp\kafka-auth-prod\certs\analytics-core-auth-prod-env.ps1` 后执行 `go test ./eventbus/kafka -run 'TestKafkaIntegration(PublishConsume|OrderedCommitWaitsForEarlierOffset|UncommittedMessageReplaysAfterRestart)$|TestKafkaReplicatedIntegration(PublishConsume|SurvivesBrokerOutage)$' -count=1 -v`，五项全部 PASS，用时 `19.394s` |
| 2026-05-17 | KAFKA-PROD-005 drill 清理 | 已完成 | `Stop-KafkaSaslSslProdDrill.ps1 -RemoveVolumes` 已清理 `simpletrack-kafka-auth-prod-*` 容器和 `simpletrack-kafka-auth-prod_default` network；`docker ps -a --filter name=simpletrack-kafka-auth-prod`、`docker network ls --filter name=simpletrack-kafka-auth-prod` 均为空 |
| 2026-05-17 | KAFKA-PROD-006 认证 broker benchmark | 已完成 | 复用 `local-auth-kafka-prod` 3 broker KRaft `SASL_SSL` 环境，`analytics-core` commit `d835341` 下完成 `BenchmarkKafkaIntegrationPublish -count=3` 和 `-benchtime=10x -count=3`；默认自动校准结果约 `275.2ms/op`、`293.7ms/op`、`1005.9ms/op`，固定 `10x` 复测约 `108.9-117.3ms/op`；benchmark 后已执行 `Stop-KafkaSaslSslProdDrill.ps1 -RemoveVolumes` 并确认容器/network 为空；文档同步当前为父仓 `82da0d8` |
| 2026-05-17 | KAFKA-PROD-007 Kafka broker/exporter SLO 指标源 | 已完成 | Bohr 子代理完成 goal-mode 外部指标源评审；Sagan / Mill 两个本地映射子代理遇到 responses upstream `502 Bad Gateway`，未计完成，父代理用本地 `rg` / 文档读取补足映射。父仓 `848494a` 已把 consumer lag、under replicated partitions、offline partitions、minISR、request latency、DLQ/retry/pause 等指标事实源写入任务计划和 runbook；结论是 broker/cluster 指标走 Kafka JMX / exporter / 托管 Kafka metrics，`analytics-service` metrics 只表达本进程 DLQ、retry、pause、worker queue、completion gate 与 ordered commit 本地状态 |
| 2026-05-18 | 推送父仓后继续 KAFKA-PROD-008 goal | 已完成 | 父仓 `master` 已推送到 `origin/master`，`64b4a31` 成为远端当前基线；随后继续推进 SCRAM / OAuth / Kerberos 认证路线评审 |
| 2026-05-18 | KAFKA-PROD-008 SCRAM / OAuth / Kerberos 认证路线评审 | 已完成 | Arendt 子代理按 goal-mode 完成依赖评审：SCRAM 生产可用且优先做，OAuth 需要 token/provider 生命周期所以暂缓，Kerberos/GSSAPI 当前否决、仅企业 Kerberos 体系触发；Hilbert / Copernicus 本地映射子代理均遇到 responses upstream `502 Bad Gateway`，未计完成，父代理用本地代码读取补足实现边界。当前 runtime 仍只支持 `SASL/PLAIN + TLS`，非 `plain` 机制继续 fail fast；文档同步为 `8f60208` |

## 阶段完成同步规则

阶段完成时只做两类文档动作：

1. 阶段推进期间只更新本目录的 `进度表.md` 和 `踩坑记录.md`。
2. 任务全部完成后，再把精简结论同步到 AGENTS.md 指定的正式实施决策文档。
3. 阶段完成或改动较多时再 commit；push 保持低频，且网络失败时用 `http://localhost:64320` 重试。
