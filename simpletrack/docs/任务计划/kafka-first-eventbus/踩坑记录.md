# Kafka-First EventBus 踩坑记录

> 最近更新：2026-05-17

## 坑位速查

记录规则：

- 先查根目录已有踩坑 / 卡壳记录，例如 `docs/开发环境卡壳问题记录.md`，能复用就优先复用。
- 本任务的新记录只写在当前文件。
- 同类问题不重复新增编号，只更新既有条目的状态、证据或第一处理动作。

| 编号 | 问题 | 状态 | 第一处理动作 |
| --- | --- | --- | --- |
| KAFKA-EVENTBUS-001 | 实现先于任务 plan 推进，容易产生不可编译半成品 | 已记录 | 先停代码，建立本目录，再按阶段恢复 |
| KAFKA-EVENTBUS-002 | Sarama 版本查询访问 `proxy.golang.org` 超时 | 已记录 | 优先复用本机 module cache 与 `xwl_bi` 已验证版本，必要时再走本机代理 |
| KAFKA-EVENTBUS-003 | 长期实施决策文档过大，不适合小步频繁修改 | 已记录 | 本任务使用轻量 plan 目录，阶段完成后再同步精简结论 |
| KAFKA-EVENTBUS-004 | 高频 commit/push 会打断实现节奏，push 还可能遇到网络问题 | 已记录 | 阶段性 commit、低频 push；push 失败使用 `http://localhost:64320` 重试 |
| KAFKA-EVENTBUS-005 | 子代理通道返回 `502 Bad Gateway` | 已记录 | 参考根目录子代理失败处理规则：不算审查/映射完成，主代理继续本地推进，阶段评审时再重试 |
| KAFKA-EVENTBUS-006 | 子代理启动时未显式要求 goal 模式 | 已修正 | 后续子代理 prompt 第一条必须要求创建子任务 goal，完成时标记 complete |
| KAFKA-EVENTBUS-007 | 子代理被 goal budget 限制打断，未完成实质审查 | 已记录 | 不把 preflight 当审查完成；缩小范围重新派发只读评审 |
| KAFKA-EVENTBUS-008 | Git commit 钩子要求 Lore message 带 OmX co-author trailer | 已记录 | 提交信息补 `Co-authored-by: OmX <omx@oh-my-codex.dev>` 后重试 |
| KAFKA-EVENTBUS-009 | `docker compose up -d kafka` 拉取 `bitnami/kafka:3.9` 被镜像源 403 拦住 | 已记录 | 先复用本机已运行 Kafka broker 跑验证；后续再单独处理 compose 镜像源或镜像替换 |
| KAFKA-EVENTBUS-010 | 诊断型 `Stats()` 容易被误读为精确 broker lag 或 SLO 事实源 | 已记录 | 文档和最终说明都标注为诊断快照；生产 SLO 仍接 broker/exporter 指标 |
| KAFKA-EVENTBUS-011 | 子代理只读锚点查询再次返回 `502 Bad Gateway` | 已记录 | 不等待、不冒充完成；主代理本地推进，提交前另起窄范围评审 |
| KAFKA-EVENTBUS-012 | 子代理在 KAFKA-PROD-002/003/004 首轮并行调查再次 502 | 已记录 | 关闭失败子代理结果，重发窄范围只读任务；只采纳成功返回的 `Leibniz` / `Mendel` 结论 |
| KAFKA-EVENTBUS-013 | broker outage 演练顺序错误会让 RF=3 topic 创建失败 | 已记录 | 先三 broker 在线创建 RF=3 / minISR=2 topic，再停止 disposable broker 跑 outage gate |
| KAFKA-EVENTBUS-014 | `analytics-core` SSH push 失败但 HTTPS + 本地代理可用 | 已记录 | 先确认不会混入无关改动，再用临时 HTTPS remote + `http://localhost:64320` 代理推送并恢复/刷新 remote |
| KAFKA-EVENTBUS-015 | `SASL_SSL` drill readiness 误连 external listener | 已记录 | 容器内探活使用 internal listener，宿主机 Go 测试使用 external listener |
| KAFKA-EVENTBUS-016 | 认证 env 相对路径在子仓目录下失效导致测试 skip | 已记录 | 从父仓根目录加载相对 env，或在子仓目录使用绝对路径 |
| KAFKA-EVENTBUS-017 | 父仓 HTTPS push 直连 GitHub 443 超时 | 已记录 | 先确认 ahead commit 范围，再用 `http://localhost:64320` 代理重试 push |
| KAFKA-EVENTBUS-018 | 3 broker Zookeeper 模式被 broker JAAS 影响预检 | 已记录 | 生产级本机 drill 改用 KRaft controller quorum，避免把 Zookeeper client auth 混进 broker SASL_SSL 验收 |
| KAFKA-EVENTBUS-019 | tracked drill 文件写入固定测试密码会违背边界 | 已记录 | tracked compose / scripts 只保留占位符或 env 引用，disposable 密码运行时生成到 `.tmp` |
| KAFKA-EVENTBUS-020 | 真实 broker benchmark 自动校准样本过少 | 已记录 | 固定 `-benchtime=10x` 复测，并记录 `b.N` / topology / topic shape / producer 参数 |
| KAFKA-EVENTBUS-021 | 进程内 ordered commit lag 被误当 broker lag | 已记录 | consumer lag 看 Burrow / kafka-exporter / 托管 Kafka lag，`lag_estimate` 只作本进程辅助诊断 |
| KAFKA-EVENTBUS-022 | Context7 / Sarama 文档查询失败 | 已记录 | 不阻塞评审；改用本地 Sarama module cache、官方链接和成功子代理评审结论交叉确认 |

## 详细记录

### KAFKA-EVENTBUS-001：实现先于任务 plan 推进

- 发现时间：2026-05-17
- 现象：Kafka provider 代码刚开始推进后，用户调整执行规范，要求先停下并建立独立 plan 目录。
- 风险：半成品 provider 容易让后续误判为“已经实现”，也可能造成工作区不可编译。
- 处理：暂停代码实现，先创建本目录，后续从阶段 1 恢复。
- 后续防线：恢复代码前先更新 `进度表.md`，明确当前阶段、改动范围和验证命令。

### KAFKA-EVENTBUS-002：Sarama 版本查询超时

- 发现时间：2026-05-17
- 现象：`go list -m -versions github.com/IBM/sarama` 访问 `proxy.golang.org` 超时。
- 新证据：`go get github.com/IBM/sarama@v1.47.0` 首次执行卡在 `goproxy.cn/sumdb/sum.golang.org/supported` 连接超时。
- 已知事实：本机 module cache 存在 `github.com/IBM/sarama@v1.47.0`，本地 `xwl_bi` 也使用 `github.com/IBM/sarama v1.47.0`。
- 处理：第一版优先使用 `v1.47.0`，与 `xwl_bi` 经验保持一致；如果后续需要确认最新版，再使用本机代理或 GitHub release 资料做单独验证。
- 当前解法：设置 `HTTP_PROXY` / `HTTPS_PROXY` 为 `http://localhost:64320`，并将 `GOPROXY` 临时切到 `https://proxy.golang.org,direct` 后，`go get github.com/IBM/sarama@v1.47.0` 成功。
- 后续防线：不要因为版本列表查询失败就切换 Kafka client；依赖选型以已确认的计划为准。

### KAFKA-EVENTBUS-003：正式文档过大

- 发现时间：2026-05-17
- 现象：`simpletrack/docs/实施决策/` 下长期文档已经很大，频繁增量会拖慢代码推进。
- 处理：本任务所有过程性记录先写入 `simpletrack/docs/任务计划/kafka-first-eventbus/`。
- 后续防线：过程性记录只写本目录；任务全部执行完成后，再同步正式实施决策文档。

### KAFKA-EVENTBUS-004：commit / push 频率过高

- 发现时间：2026-05-17
- 现象：如果每一点小改动都提交、推送，会打断 Kafka provider 这类核心链路实现节奏；push 本身也可能受网络影响。
- 处理：阶段完成、改动较多、或进入阶段性评审前后再做本地 commit；push 保持低频。
- 后续防线：push 遇到网络问题时，优先使用 `http://localhost:64320` 本地代理重试多次，并把失败证据写入本文件。

### KAFKA-EVENTBUS-005：子代理通道返回 502

- 发现时间：2026-05-17
- 现象：阶段 1 启动 service 影响面和 xwl_bi 搬迁映射两个只读子代理时，本地 responses upstream 返回 `502 Bad Gateway`。
- 根目录参考：`docs/开发环境卡壳问题记录.md` 已有“子代理失败不能算完成”的同类处理原则。
- 处理：不把这两个子代理视为完成；主代理继续本地查代码推进，阶段性交付或评审门槛需要子代理时再缩小范围重试。
- 后续防线：如果阶段评审仍遇到 502，把失败证据和替代验证写清楚，不能冒充子代理审查完成。

### KAFKA-EVENTBUS-006：子代理未显式按 goal 模式启动

- 发现时间：2026-05-17
- 现象：阶段 1 第一次启动只读 explorer 时，父代理没有在子代理 prompt 里明确写“先创建 goal，结束时 update goal”。
- 新证据：KAFKA-HARDEN-002 阶段评审子代理也按普通只读评审 prompt 启动，未显式要求子代理 goal 模式；虽然后续 code-review 和 code-simplifier 均完成并返回有效结论，但该轮不能作为“子代理 goal 模式合规样例”。
- 风险：即使子代理成功返回，也不符合用户要求的全程 goal 模式。
- 处理：已在 `执行规范.md` 补充硬规则；后续所有子代理 prompt 第一条必须要求子代理创建本子任务 goal。对已经完成但未按 goal prompt 启动的子代理，不伪造合规记录，只使用其真实代码审查结论。
- 后续防线：父代理整理子代理输出时，同时检查子代理是否报告 goal 完成或未完成原因；如果子代理平台不支持创建 goal，也必须在 prompt 中要求它声明无法创建 goal 并按子任务目标清单执行。

### KAFKA-EVENTBUS-007：子代理被 goal budget 限制打断

- 发现时间：2026-05-17
- 现象：阶段性交付 code-review 子代理只完成 preflight，随后报告被 active thread goal budget 限制拦住，未进入逐文件审查。
- 风险：如果把“已启动子代理”或“完成 preflight”误当成代码审查完成，会违反阶段评审规则。
- 处理：明确不采纳该轮为完成审查；重新派发更小范围、禁止 MCP、只读的代码审查子任务。早期宽范围评审不能声明通过；后续针对 KAFKA-HARDEN-003 到 007 的窄范围 code-review 初审返回 `REQUEST CHANGES`，指出 stale pause state 和 TLS/SASL material 静默忽略；修复后复审返回 `APPROVE`，这一次才算有效审查证据。
- 后续防线：阶段评审子代理 prompt 要限制范围和工具面，优先让它输出少量高价值 file:line 结论，而不是先做宽泛准备工作。

### KAFKA-EVENTBUS-008：Git commit 钩子要求 OmX co-author trailer

- 发现时间：2026-05-17
- 现象：`analytics-core` 注释补强提交第一次被 PreToolUse hook 拦截，提示 Lore-format commit 还必须包含 `Co-authored-by: OmX <omx@oh-my-codex.dev>`。
- 处理：保留原 Lore commit 内容，追加 OmX co-author trailer 后重新执行，commit `2b4f38d` 成功并已推送。
- 后续防线：本任务和后续 SimpleTrack 子仓提交都默认把该 co-author trailer 放进 commit message，避免到提交阶段才被钩子打断。

### KAFKA-EVENTBUS-009：本地 Kafka compose 镜像源 403

- 发现时间：2026-05-17
- 现象：在 `src/analytics-core` 执行 `docker compose up -d kafka` 时，Docker 拉取 `docker.io/bitnami/kafka:3.9` 失败，错误为当前镜像源 `https://ustc-edu-cn.mirror.aliyuncs.com/v2/bitnami/kafka/manifests/3.9?ns=docker.io` 返回 `403 Forbidden`。
- 影响：`analytics-core` 自带 compose 的单节点 Kafka 暂时没有启动，不能把 `127.0.0.1:29092` 作为本轮真实 broker 证据。
- 临时处理：不扩大到 Docker Desktop / registry 配置排障；复用本机已运行的 `confluentinc/cp-kafka:7.5.0` 容器，对外地址 `127.0.0.1:9092`，完成 KAFKA-HARDEN-001 真实 broker 集成测试。
- 验证证据：`docker exec kafka ... --version` 输出 `7.5.0-ccs`；`Test-NetConnection 127.0.0.1 -Port 9092` 成功；`ANALYTICS_CORE_KAFKA_INTEGRATION=1 ANALYTICS_CORE_KAFKA_BROKERS=127.0.0.1:9092 go test ./eventbus/kafka -run 'TestKafkaIntegration' -count=1 -v` 通过。
- 后续防线：如果必须使用 `analytics-core` compose 自带 Kafka，再单独处理镜像源、预拉镜像或评估把本地开发镜像换成当前机器可获取的 Kafka 镜像；不要把镜像源 403 误判为 Kafka provider 代码失败。

### KAFKA-EVENTBUS-010：`Stats()` 诊断快照被误读为精确事实源

- 发现时间：2026-05-17
- 现象：KAFKA-HARDEN-004 为排障新增 `Bus.Stats()` 和 `Runtime.KafkaEventBusStats()` 后，architect 评审提醒该接口容易被未来使用方误当成精确 accounting、SLO 或 broker lag 事实源。
- 根因：provider 内部只能看到本进程处理状态、ordered committer high watermark 和当前 claim 快照；它不是 broker exporter，也不读取 Kafka broker 端完整 consumer group lag。
- 处理：runbook 的“观测入口”明确 `Stats()` 是诊断快照；待办事项和进度表也标注 `lag estimate`，避免写成精确 lag。
- 后续防线：如果后续接入 Prometheus 或告警，`Stats()` 只作为进程内症状信号；精确 consumer lag、broker ISR、controller、topic health 仍以 Kafka exporter / broker metrics 为准。

### KAFKA-EVENTBUS-011：子代理锚点查询再次 502

- 发现时间：2026-05-17
- 现象：KAFKA-PROD-001 启动只读子代理检查 `analytics-service` 测试与 OpenAPI 锚点时，responses upstream 返回 `502 Bad Gateway`。
- 影响：该子代理没有产出实质结论，不能算作 goal 模式子任务完成，也不能作为阶段评审证据。
- 处理：主代理按本地代码阅读继续推进，已自行完成 config / collectapi / runtime / OpenAPI / README 实现与测试。
- 后续防线：提交前仍需另起窄范围 code-review / simplifier / ai-slop-cleaner 审查；如果再次 502，必须在交付中明确“子代理审查未完成”，不能冒充通过。

### KAFKA-EVENTBUS-012：KAFKA-PROD-002/003/004 首轮子代理再次 502

- 发现时间：2026-05-17
- 现象：本轮 goal 启动后，首轮并行子代理 `Bacon` 和 `Ampere` 分别用于 KAFKA-PROD-003 / 004 调查，但都返回 responses upstream `502 Bad Gateway`。
- 影响：首轮两个子代理没有实质输出，不能算 goal-mode 子任务完成，也不能作为审查、映射或验收证据。
- 处理：不等待失败通道；主代理继续推进 KAFKA-PROD-002 的 metrics 实现，同时重发两个更窄的只读子任务。后续 `Leibniz` 成功返回认证 broker drill 结论，`Mendel` 成功返回多 broker / benchmark 结论，且都声明未改文件。
- 后续防线：最终报告只引用成功返回的子代理结论；对失败的 `Bacon` / `Ampere` 仅作为通道故障记录，不把它们包装成完成。

### KAFKA-EVENTBUS-013：broker outage 演练顺序错误

- 发现时间：2026-05-17
- 现象：第一次 broker outage 演练把 3 broker 临时集群中的一个 broker 先停掉，再创建 replication factor 3 的 topic，Kafka 返回 `Replication factor: 3 larger than available brokers: 2`。
- 根因：RF=3 topic 创建时需要 3 个可用 broker；先停 broker 等于把演练变成了 topic provisioning 失败，而不是 producer / consumer 在 broker outage 下的恢复验证。
- 处理：新增 `TestKafkaReplicatedIntegrationSurvivesBrokerOutage`，测试先在 3 broker 全部在线时创建 RF=3 / minISR=2 topic，再停止显式指定的 disposable broker container，随后执行 publish / consume / commit 验证。
- 后续防线：runbook 中把顺序写成硬规则；`ANALYTICS_CORE_KAFKA_OUTAGE_STOP_CONTAINER` 只允许指向一次性演练 broker，不能指向共享开发、测试或生产容器。

### KAFKA-EVENTBUS-014：analytics-core SSH push 失败后的代理推送

- 发现时间：2026-05-17
- 现象：`analytics-core` 子仓提交后，常规 SSH push 不稳定；当前任务要求 push 网络问题优先使用本机代理 `http://localhost:64320` 多试几次。
- 处理：确认子仓只剩无关 `.idea/` 未跟踪项后，使用临时 HTTPS remote / inline proxy 推送 `analytics-core` commits `06893f9` 与 `d835341`，再 fetch 刷新 `origin/main`，最终子仓 `origin/main` 对齐 `d835341`。
- 后续防线：子仓 push 失败时先看 `git status --short --branch` 和 staged 范围，避免把代理重试和无关文件混在一起；代理推送后恢复或核对 remote，确保父仓 gitlink 指向已推送 commit。

### KAFKA-EVENTBUS-015：SASL_SSL drill readiness 误连 external listener

- 发现时间：2026-05-17
- 现象：`Start-KafkaSaslSslDrill.ps1 -TimeoutSeconds 240` 启动后，Kafka broker 日志显示已经 `STARTED`，但脚本 readiness probe 一直失败；最后报 `Connection to node 1 (localhost/127.0.0.1:39093) could not be established` 和 `Timed out waiting for a node assignment. Call: listTopics`。
- 根因：probe 在容器内执行 `docker exec simpletrack-kafka-auth kafka-topics`，却连接 broker advertised external listener `127.0.0.1:39093`。这个地址对宿主机 Go client 是对的，但在容器内部会指向容器自己，不是 Docker 端口映射后的宿主机入口。
- 处理：把 readiness probe 的 bootstrap server 改为 internal listener `kafka-auth:9094`；宿主机测试 env 仍保留 `ANALYTICS_CORE_KAFKA_BROKERS=127.0.0.1:39093`。随后 `Start-KafkaSaslSslDrill.ps1 -TimeoutSeconds 120` 成功，加载 `C:\Users\admin\Documents\src\uba\.tmp\kafka-auth\certs\analytics-core-auth-env.ps1` 后，三项认证 integration test 全部 PASS。
- 后续防线：Kafka compose 同时暴露 internal/external listener 时，容器内探活使用 internal listener，宿主机或 Go 集成测试使用 external listener；不要把两类地址混用。

### KAFKA-EVENTBUS-016：认证 env 相对路径在子仓目录下失效

- 发现时间：2026-05-17
- 现象：在 `src/analytics-core` 子仓目录下执行 `. '.\.tmp\kafka-auth\certs\analytics-core-auth-env.ps1'` 会报 `The term '.\.tmp\kafka-auth\certs\analytics-core-auth-env.ps1' is not recognized`；随后 `go test` 因 `ANALYTICS_CORE_KAFKA_INTEGRATION` 未加载而全部 skip。
- 根因：`.tmp/kafka-auth` 位于父仓根目录，不在 `src/analytics-core` 子仓目录下；相对路径会落到错误位置。
- 处理：从父仓根目录加载相对 env，或在子仓目录使用绝对路径 `. 'C:\Users\admin\Documents\src\uba\.tmp\kafka-auth\certs\analytics-core-auth-env.ps1'`。只有看到三项 integration test `PASS`，才算认证 broker live 证据。
- 后续防线：runbook 同时写父仓根目录命令和子仓目录绝对路径命令；如果输出是 `SKIP` 而不是 `PASS`，不能作为验收证据。

### KAFKA-EVENTBUS-017：父仓 HTTPS push 直连 GitHub 443 超时

- 发现时间：2026-05-17
- 现象：父仓本地 commit `c31750e` 准备推送时，直接执行 `git push origin master` 失败，错误为 `Failed to connect to github.com port 443 after 21068 ms: Couldn't connect to server`。
- 处理：先用 `git status --short --branch` 和 `git log --oneline origin/master..HEAD` 确认只会推送 `c31750e`；随后设置 `HTTP_PROXY` / `HTTPS_PROXY` 并用 `git -c http.proxy=http://localhost:64320 -c https.proxy=http://localhost:64320 push origin master` 推送成功。
- 后续防线：父仓或子仓 push 遇到 GitHub 443 超时时，不要反复直连耗时；先确认 staged / ahead 范围，再使用本机代理 `http://localhost:64320` 重试。

### KAFKA-EVENTBUS-018：3 broker Zookeeper 模式被 broker JAAS 影响预检

- 发现时间：2026-05-17
- 现象：第一次实现 `local-auth-kafka-prod` 时采用 3 broker + Zookeeper compose，Kafka 容器启动预检阶段报 `No JAAS configuration section named 'Client' was found in specified JAAS configuration file: '/etc/kafka/secrets/kafka_server_jaas.conf'`，随后 readiness probe 超时。
- 根因：broker 需要 `KAFKA_OPTS=-Djava.security.auth.login.config=/etc/kafka/secrets/kafka_server_jaas.conf` 来启用 inter-broker `SASL_SSL`，但 Confluent Zookeeper 模式预检也会在同一 JVM 配置下创建 Zookeeper client；这个 client 尝试读取 `Client` JAAS 段，导致和本轮真正要验证的 broker TLS/SASL 路径混在一起。
- 处理：放弃本轮 Zookeeper 模式，改为 3 broker KRaft controller quorum；controller listener 保持 `PLAINTEXT`，internal/external broker listener 保持 `SASL_SSL`。随后 `Start-KafkaSaslSslProdDrill.ps1 -TimeoutSeconds 300` 启动成功，五条目标 integration tests 全部 PASS。
- 后续防线：本机生产形态认证 drill 优先使用 KRaft，减少无关 Zookeeper 认证变量；如果未来必须验证 Zookeeper 模式，需要单独提供 Zookeeper client JAAS 段并把它作为另一个 drill，不要和 KAFKA-PROD-005 混做。

### KAFKA-EVENTBUS-019：tracked drill 文件写入固定测试密码

- 发现时间：2026-05-17
- 现象：KAFKA-PROD-005 提交前 code-review 指出 `local-auth-kafka-prod` 初版 compose / material 脚本里写死了 `changeit`、`broker-secret`、`simpletrack-secret` 这类测试密码；虽然它们只用于本机 disposable drill，但和“证书、私钥、JAAS、密码不提交”的边界冲突。
- 根因：沿用了单 broker drill 里固定本地密码的写法，没有把“可提交脚本”和“运行时生成的本机材料”分清楚。
- 处理：tracked compose 改为从 `KAFKA_AUTH_PROD_STORE_PASSWORD` 读取 keystore/truststore/key password；`New-KafkaSaslSslProdMaterial.ps1` 运行时生成 disposable store / broker / client password，并写入 gitignored `.tmp/kafka-auth-prod/certs/compose-env.ps1`、`analytics-core-auth-prod-env.ps1`、JAAS 和 client properties。
- 后续防线：以后新增 drill 脚本时，tracked 文件只允许出现占位符或 env 引用；具体 broker/client 密码只能出现在 `.tmp`、本地 secret store 或部署系统里。提交前用 `rg 'changeit|simpletrack-secret|broker-secret|password="[^_]"'` 扫任务目录。

### KAFKA-EVENTBUS-020：真实 broker benchmark 自动校准样本过少

- 发现时间：2026-05-17
- 现象：KAFKA-PROD-006 在 3 broker `SASL_SSL` 环境下按验收命令执行 `go test ./eventbus/kafka -bench BenchmarkKafkaIntegrationPublish -benchmem -run '^$' -count=3 -v`，默认 Go benchmark 自动校准只跑了 `1` 到 `4` 次，结果约 `275.2ms/op`、`293.7ms/op`、`1005.9ms/op`，波动很大。
- 根因：真实 broker + Docker Desktop + TLS/SASL + SyncProducer + RF=3/minISR=2 的单次 publish 成本远高于 provider 内部 benchmark，Go 自动校准为了控制总耗时会选择很小的 `b.N`；样本太少时，topic 创建后的首轮 broker 状态、TLS/SASL 握手、Windows/Docker 资源波动都会放大到 `ns/op`。
- 处理：保留默认验收命令结果，另在同一环境补跑 `go test ./eventbus/kafka -bench BenchmarkKafkaIntegrationPublish -benchmem -run '^$' -benchtime=10x -count=3 -v`；固定 `10x` 复测收敛到约 `108.9-117.3ms/op`，更适合作为本机复测基线。
- 后续防线：真实 broker benchmark 记录必须同时写明 `b.N` 或 `-benchtime`、broker/topology/topic shape、认证模式和 producer 参数；默认自动校准结果只能说明验收命令完成，不能直接当 SLA 或跨机器容量结论。

### KAFKA-EVENTBUS-021：进程内 ordered commit lag 被误当 broker lag

- 发现时间：2026-05-17
- 现象：KAFKA-PROD-007 梳理 SLO 指标源时，`simpletrack_kafka_ordered_commit_lag_estimate` 容易被看成 consumer group lag 或生产 SLO 候选。
- 根因：这个字段来自 `analytics-service` 进程内 Kafka provider 的 ordered commit snapshot，只知道当前进程观察到的 high-water mark 和本地 `next_offset`；它不读取 broker 侧 committed offset 全貌，也不具备 broker/controller 复制和分区健康视角。
- 处理：在 `生产级认证与压测计划.md` 阶段 12 和 `生产运行手册与故障演练.md` 观测入口中新增指标事实源表，明确 consumer group lag 应看 Burrow、kafka-exporter、托管 Kafka lag 指标，或按 broker end offset + committed offset 计算；broker replication / offline / minISR / request latency 应看 broker JMX、exporter 或托管 Kafka metrics。
- 后续防线：凡是写 SLO、Grafana dashboard 或 Prometheus alert rules，都必须先接 broker/exporter 或托管 Kafka 指标；`analytics-service` 的 DLQ、retry、pause、worker queue、completion gate 和 ordered commit lag 只作为服务进程内辅助诊断。

### KAFKA-EVENTBUS-022：Context7 / Sarama 文档查询失败

- 发现时间：2026-05-18
- 现象：KAFKA-PROD-008 评审 Sarama SCRAM / OAuth / Kerberos 支持面时，Context7 library resolve / query 返回 `fetch failed`，不能作为本轮官方文档证据来源。
- 影响：如果继续等待外部文档工具，会拖住本轮路线评审；但直接凭印象写又会削弱“Kafka 机制是否真的被 Sarama 支持”的证据质量。
- 处理：改用本机 module cache `C:\Users\admin\go\pkg\mod\github.com\!i!b!m\sarama@v1.47.0` 读取 `config.go` 与 `gssapi_kerberos.go`：确认 SCRAM 需要 `SCRAMClientGeneratorFunc`，OAuth 需要 `AccessTokenProvider`，GSSAPI 需要 `GSSAPIConfig`。同时采纳 Arendt 子代理返回的官方链接列表作为人工可复查入口。
- 后续防线：依赖文档工具失败时，不把失败当作“库不支持”；优先回到已下载的 Go module 源码、`pkg.go.dev`、上游 examples 和目标云厂商文档做交叉证据。
