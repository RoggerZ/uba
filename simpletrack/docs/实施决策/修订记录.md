# SimpleTrack 实施决策修订记录

> 本文件记录实施决策库的完整修订流水。simpletrack/docs/实施决策/README.md 只保留索引、当前进度和维护规则。

# 修订流水

| 日期 | 修订内容 | 影响范围 |
| --- | --- | --- |
| 2026-05-11 | `simpletrack-saas` commit `1ab56a9` 把 readback evidence 的浏览器级 ready-state 证据补齐到真实页面闭环：`simpletrack-analytics-ready-state.spec.ts` 的 seeded mock 现在会为 Realtime / Events / Goals 返回 `query_evidence`，并在 Chromium 页面断言可见的 `Readback evidence` 文案，避免 `ec51160` 的页面 copy 只被 SSR 回归覆盖。目标验证 `corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas test -- readback-pages.test.tsx`、`corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-ready-state.spec.ts --project chromium --reporter=line`、`git diff --check -- apps/saas/tests/simpletrack-analytics-ready-state.spec.ts` 已通过；父仓本轮同步 commit `9329fa2` | P0-003、P0-004、P1-005D、P1-005E、simpletrack-saas、readback evidence、Playwright、Chromium、ready-state、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `ec51160` 把 readback evidence 从 server-side view model 推到 Realtime / Events / Goals 页面可见面：新增 `formatReadbackEvidence()`，页面以低权重文案展示 `queryEvidence.observedRows` 和 `queryEvidence.pressure`，让运维读回放不再只能从内部 API payload 看 observed row count / pressure。本轮不改变 `pressure` 决策逻辑，也不把 row-volume 升格为自动告警或 UI 状态。目标验证 `corepack pnpm --filter saas test -- readback-pages.test.tsx`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm exec oxfmt --check apps/saas/modules/simpletrack/lib/readback-evidence.ts`、`corepack pnpm exec oxlint apps/saas/modules/simpletrack/lib/readback-evidence.ts`、`git diff --cached --check -- apps/saas/modules/simpletrack/lib/readback-evidence.ts apps/saas/app/(authenticated)/(main)/(organizations)/[organizationSlug]/realtime/page.tsx apps/saas/app/(authenticated)/(main)/(organizations)/[organizationSlug]/events/page.tsx apps/saas/app/(authenticated)/(main)/(organizations)/[organizationSlug]/goals/page.tsx apps/saas/app/(authenticated)/(main)/(organizations)/[organizationSlug]/readback-pages.test.tsx` 已通过；目录级 `oxfmt` / `oxlint` 命中既有 workspace-wide 格式和 `.oxlintrc` root-config 问题，未作为本轮通过项；父仓本轮同步 commit `e941827` | P1-005D、P1-005E、simpletrack-saas、readback evidence、observed_rows、pressure、Realtime、Events、Goals、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `e2362ff` 把 `analytics-service` 已返回的 `query_evidence.observed_rows` 与 `pressure` 保留到 SaaS server-side readback helper：Realtime / Events / Goals 的 view model 现在会输出 `queryEvidence.observedRows` 与 `queryEvidence.pressure`，不再在 helper 层丢弃内部读回放证据；本轮只做服务端契约桥接，不新增 UI 渲染。目标验证 `corepack pnpm --dir src/simpletrack-saas --filter saas test -- analytics-readback-core.test.ts`、`corepack pnpm --dir src/simpletrack-saas --filter saas exec tsc --noEmit`、`corepack pnpm --dir src/simpletrack-saas exec oxfmt --check apps/saas/modules/simpletrack/lib/analytics-readback-core.ts apps/saas/modules/simpletrack/lib/analytics-readback-core.test.ts`、`corepack pnpm --dir src/simpletrack-saas exec oxlint apps/saas/modules/simpletrack/lib/analytics-readback-core.ts apps/saas/modules/simpletrack/lib/analytics-readback-core.test.ts`、`git diff --check -- apps/saas/modules/simpletrack/lib/analytics-readback-core.ts apps/saas/modules/simpletrack/lib/analytics-readback-core.test.ts` 已通过；Codex native 子代理真实复审结论为无阻塞问题，剩余风险是字段尚未进入用户可见 UI；父仓本轮同步 commit `65bf064` | P1-005D、P1-005E、simpletrack-saas、query_evidence、observed_rows、pressure、server-side contract bridge、父仓 gitlink |
| 2026-05-11 | `analytics-service` commit `4495cdf` 补齐 `query_evidence.observed_rows` 的内部 API 契约闭环：`api/openapi.yaml` 的 `QueryEvidence` schema 现在把 `observed_rows` 记为 required `int64` 字段，并明确它只表示本次 readback 执行结果基数，不代表扫描行估算；`internal/collectapi/handler_test.go` 还新增直接读取真实 `api/openapi.yaml` 的 drift test，锁住 Swagger served spec 不再落后于 handler 响应。目标验证 `go test ./internal/collectapi`、`go test ./...`、`go vet ./...`、`go test ./... -run Example -count=1`、`go doc -all ./internal/collectapi`、`golint ./...`、`git diff --check -- api/openapi.yaml internal/collectapi/handler_test.go` 已通过；子代理代码审查因 usage limit 未完成，因此本轮仅以主代理实证自查为准；父仓本轮同步 commit `46231ce` | P1-005D、P1-005E、analytics-service、OpenAPI、query_evidence、observed_rows、drift test、父仓 gitlink |
| 2026-05-11 | `analytics-service` commit `e83f8e5` 把内部 Realtime / Events / Goals readback 的执行结果基数显式透出为 `query_evidence.observed_rows`：Realtime / Events 返回实际结果行数，Goals 返回精确 count。该字段明确只表示本次执行命中的结果基数，不代表 ClickHouse 扫描行估算，也不改变当前仍按 query shape 评估 `pressure` 的策略。目标验证 `go test ./internal/collectapi`、`go test ./...`、`go vet ./...`、`go test ./... -run Example -count=1`、`go doc -all ./internal/collectapi`、`golint ./...`、`git diff --check -- internal/collectapi/query.go internal/collectapi/handler_test.go` 已通过；父仓本轮同步 commit `e2fa8e0` | P1-005D、P1-005E、analytics-service、query_evidence、observed_rows、readback evidence、父仓 gitlink |
| 2026-05-11 | `analytics-core` commit `5625e8d` 补齐提交前审查发现的 bool typed property filter 默认测试缺口：`TestEventQueryBuilderBuildsPropertyFilters` 现在在普通 query builder 单测中覆盖 `button` string、`plan` string、`is_paid` bool 三类 typed property filter，断言 SQL shape 包含 `bool_value = ?`，并要求 `EventQueryEvidence` 保持 3 条 value-free property evidence。目标验证 `go test ./storage/clickhouse -run 'TestEventQueryBuilder(BuildsPropertyFilters|CombinesScalarVisitSortAndPropertyFilters)' -count=1`、`go test ./storage/clickhouse -count=1`、`GOPROXY=https://proxy.golang.org,direct go test ./...`、`GOPROXY=https://proxy.golang.org,direct go vet ./...`、`go test ./... -run Example -count=1`、`go doc -all ./storage/clickhouse`、`golint ./...`、`git diff --check -- storage/clickhouse/query_builder_test.go` 已通过；父仓本轮同步 commit `77a7a7d` | P1.5-001、analytics-core、EventQueryBuilder、typed property filter、bool evidence、父仓 gitlink |
| 2026-05-11 | `analytics-core` commit `d850197` 继续修复真实 ClickHouse reader benchmark fixture 的 high property evidence 漂移：`BenchmarkEventReaderClickHouseExecution` 与 `TestEventReaderClickHouseExplain` 现在复用 `button` string、`score` number、`is_paid` bool 三类 typed property predicate，seeded event/user JSON、typed property rows 和结果断言都与 builder-only `EventQueryEvidence` 口径一致。目标验证 `go test ./internal/e2e -run '^$' -count=1`、`go test ./internal/e2e -run TestEventReaderClickHouseExplain -count=1 -v`、`GOPROXY=https://proxy.golang.org,direct go test ./...`、`GOPROXY=https://proxy.golang.org,direct go vet ./...`、`GOPROXY=https://proxy.golang.org,direct go test ./... -run Example -count=1`、`go doc -all ./internal/e2e`、`golint ./...`、`git diff --check -- internal/e2e/clickhouse_reader_benchmark_test.go` 已通过；`TestEventReaderClickHouseExplain` 在未设置 `ANALYTICS_CORE_CLICKHOUSE_BENCH=1` 时按预期 skip，真实 ClickHouse benchmark suite 未在本轮重跑；父仓本轮同步 commit `77a7a7d` | P1.5-001、analytics-core、ClickHouse reader benchmark、EventQueryEvidence、typed property filter、父仓 gitlink |
| 2026-05-11 | `analytics-core` commit `be47dfb` 修复 builder-only read-side benchmark 的 high property evidence 漂移：`BenchmarkEventQueryBuilderReadSideShapes/high_events_property` 的实际 query 重新对齐到 string / number / bool 三类 typed property predicates，避免 benchmark label 仍写高属性证据、实际却只跑 string-only 属性形状。目标验证 `go test ./storage/clickhouse -run '^$' -bench BenchmarkEventQueryBuilderReadSideShapes/high_events_property -benchtime=1x -count=1`、`go test ./storage/clickhouse -count=1`、`go test ./storage -count=1`、`GOPROXY=https://proxy.golang.org,direct go test ./...`、`go vet ./...`、`go test ./... -run Example -count=1`、`go doc -all ./storage/clickhouse`、`golint ./...` 已通过；父仓本轮同步 commit `23fc831` | P1.5-001、analytics-core、read-side benchmark、EventQueryEvidence、typed property filter、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `d463328` 把 `analytics-service` 的 token-local `write_keys` allowlist 真正接到 SaaS server-side readback helper：`analytics-readback-core` 现在会优先选择 `SIMPLETRACK_ANALYTICS_SERVICE_QUERY_TOKEN_<ROUTE>_BY_WRITE_KEY_JSON` 或 shared `..._BY_WRITE_KEY_JSON` 里的 source-specific token；一旦显式 map 存在但当前 Website `writeKey` 未被映射，就直接 fail closed，不再悄悄回退到更宽的 route/shared token。目标验证 `corepack pnpm --dir src/simpletrack-saas --filter saas test -- analytics-readback-core.test.ts`、`corepack pnpm --dir src/simpletrack-saas --filter saas exec tsc --noEmit`、`git diff --check -- README.md apps/saas/modules/simpletrack/lib/analytics-readback-core.ts apps/saas/modules/simpletrack/lib/analytics-readback-core.test.ts` 已通过；父仓本轮同步 commit `6972661` | P1-005D、P1-005E、simpletrack-saas、query token、write_key allowlist、fail-closed、父仓 gitlink |
| 2026-05-11 | `analytics-service` commit `a398362` 继续把内部 readback auth 从 route scope 收紧到 runtime source 边界：结构化 query token 现在支持可选 `write_keys` allowlist，主环境变量也新增 `ANALYTICS_SERVICE_QUERY_TOKEN_WRITE_KEYS`；HTTP 层会在 source resolution 之前先按 token route scope + `write_key` allowlist fail closed，并把“命中 source 外 write key”的拒绝单独打审计日志。目标验证 `go test ./internal/config ./internal/collectapi`、`go vet ./...`、`go test ./... -run Example -count=1`、`go test ./...`、`golint ./...`、`go doc -all ./internal/config`、`go doc -all ./internal/collectapi` 已通过；父仓本轮同步 commit `4a9fa65` | P1-005D、P1-005E、analytics-service、query token、write_keys allowlist、audit、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `c5b8254` 把 per-source `readback_policy` 真正落到 SaaS 控制面真源：`Website` schema 新增 `readbackRealtimeEnabled` / `readbackEventsEnabled` / `readbackPropertiesEnabled` / `readbackGoalsEnabled` 四个布尔列，`runtime-source` 不再硬编码全 `true`，而是透出 Website 持久化配置；Websites settings 也新增四个 readback access 开关，能够直接驱动 `analytics-service` 的 route-scoped fail-closed 行为，不再依赖测试夹具里的 mock runtime-source。目标验证 `corepack pnpm --filter @repo/database run generate`、`corepack pnpm --filter @repo/database run push`、`corepack pnpm --filter @repo/database run type-check`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm test -- website-management.test.ts website-database.test.ts websites-page.test.tsx`、`corepack pnpm test -- runtime-source.test.ts` 已通过；root `pnpm lint` 通过，但仍有未修改文件 `website-selector.test.tsx` 的既有 warning；父仓本轮同步 commit `3d82ac2` | P0-003、P0-004、P1-005C、P1-005E、simpletrack-saas、readback policy、runtime-source、Websites、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `bba3e18` 收口 authenticated analytics 浏览器全套回归的 Better Auth `429` 限流问题：新增共享 `auth-flow` helper，把 analytics browser specs 的真实 sign-up 流程统一到同一路径，并在 Playwright SaaS web server 环境显式注入 `AUTH_RATE_LIMIT_ENABLED=0`，同时保持 `packages/auth` 默认仍开启 rate limiting，避免把旁路扩大到非测试运行时；`runtime-config.test.ts` 也同步锁住“默认开启、仅显式 override 可关闭”的契约。目标验证 `corepack pnpm exec vitest run modules/auth/lib/runtime-config.test.ts`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas exec playwright test --project chromium --reporter=line` 已通过；Codex native 子代理第一次真实复审抓到“非生产默认关闭 rate limit”的范围回归，修复后再次复审结论 `APPROVE`；父仓本轮同步 commit `faadcb4` | P0-003、P0-004、simpletrack-saas、Better Auth、Playwright、authenticated analytics、full Chromium suite、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `b65582d` 继续补齐 authenticated analytics ready-state 的 Goals route-scoped structured `403` 合同：`analytics-readback-core.test.ts` 新增 Goals 结构化 `403` 单测；`readback-pages.test.tsx` 新增 SSR 回归，验证 Goals 禁用时保留 goal 定义、隐藏 verification count 并避免伪 `0`；`simpletrack-analytics-ready-state.spec.ts` 再把 Goals route-scoped `403` 场景补进真实浏览器闭环，并确认 Events / Realtime seeded ready-state 不受影响。目标验证 `corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas test -- analytics-readback-core.test.ts`、`corepack pnpm --filter saas test -- readback-pages.test.tsx`、`corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-ready-state.spec.ts --project chromium --reporter=line` 已通过；Codex native 子代理真实复审结论 `No blocking issues found`；本轮父仓同步 commit 已回填为 `0658478` | P0-003、P0-004、P1-005D、P1-005E、simpletrack-saas、Goals route-scoped disable、Realtime/Events isolation、Playwright、SSR、unit、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `110a96b` 继续补齐 authenticated analytics ready-state 的 route-scoped structured `403` 合同：`analytics-readback-core.test.ts` 新增 Realtime 与 Property catalog 结构化 `403` 单测；`readback-pages.test.tsx` 新增 SSR 回归，验证 Realtime 禁用时不复活伪 `0` 指标、Property suggestions 禁用时 Raw events 行仍可见；`simpletrack-analytics-ready-state.spec.ts` 再把两条 route-scoped `403` 场景补进真实浏览器闭环，并在 helper 中先等待 Better Auth 自动登录跳转，避免 sign-up 后第二条场景因登录竞争误红。目标验证 `corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas test -- analytics-readback-core.test.ts`、`corepack pnpm --filter saas test -- readback-pages.test.tsx`、`corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-ready-state.spec.ts --project chromium --reporter=line` 已通过；本轮父仓同步 commit 已回填为 `ce0853a` | P0-003、P0-004、P1-005D、P1-005E、simpletrack-saas、Realtime、Events、Properties、Playwright、SSR、unit、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `89c411c` 把 authenticated analytics ready-state 里的 route-scoped Events disable 合同补成真实代码闭环：`simpletrack-analytics-ready-state.spec.ts` 的 mock helper 不再硬编码 `200`，而是按 route 发送真实 `403`；`analytics-readback-core.ts` 在非 `2xx` 时会优先透传 `analytics-service` 结构化 JSON `error`，否则才回退到通用状态文案；`readback-pages.test.tsx` 与 `analytics-readback-core.test.ts` 也同步锁住 SSR / unit 契约。真实结果是 Events 页现在会在 `403` 下显示 `Events readback unavailable` 和原始服务消息 `events readback is disabled for this source`，同时保留 filters / property suggestions，而 Goals seeded ready count 继续保持不受影响。目标验证 `corepack pnpm --filter saas test -- analytics-readback-core.test.ts`、`corepack pnpm --filter saas test -- readback-pages.test.tsx`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-ready-state.spec.ts --project chromium --reporter=line` 已通过；Codex native 子代理真实复审结论 `APPROVE`；父仓随后同步 `src/simpletrack-saas` gitlink 与实施进度文档 | P0-003、P0-004、P1-005D、P1-005E、simpletrack-saas、Events route-scoped disable、structured service error、Playwright、SSR、unit、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `7911cac` 继续把 authenticated analytics seeded ready-state 浏览器闭环往前推一格：`simpletrack-analytics-ready-state.spec.ts` 抽出共享 signup / login / onboarding / Website / Goal helper 后改为 serial 执行，并在同一条 ready-state journey 里补上 route-scoped Events readback disabled 验证；真实合同是 Events 页显示 `Events readback unavailable`，同时带出原始服务消息 `events readback is disabled for this source`，property suggestions 仍保持可见，而 Goals seeded count 继续保持 ready，不会被 Events route disable 误伤。目标验证 `corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-ready-state.spec.ts --project chromium --reporter=line` 已通过；本轮未额外启动子代理审查，按低风险测试闭环直接收口；父仓随后同步 `src/simpletrack-saas` gitlink 与实施进度文档 | P0-003、P0-004、P1-005E、simpletrack-saas、Events route-scoped disable、Goals ready-state、Playwright、test hardening、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `902029e` 继续收口 authenticated analytics seeded ready-state 浏览器证据：`simpletrack-analytics-ready-state.spec.ts` 不再用 `body` 级宽泛文本匹配，而是把 Realtime 指标收窄到对应 metric card、把 Events 断言收窄到 `Raw events` 行、把 Goals 断言收窄到匹配 goal row，从而避免 Goals 表格严格定位器歧义，并让 ready-state 回归更接近真实 UI 合约。目标验证 `corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-empty-state.spec.ts tests/simpletrack-analytics-ready-state.spec.ts --project chromium --reporter=line` 已通过；本轮未额外启动子代理审查，按低风险测试闭环直接收口；父仓随后同步 `src/simpletrack-saas` gitlink 与实施进度文档 | P0-003、P0-004、P1-005E、simpletrack-saas、Realtime、Events、Goals、Playwright、test hardening、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `37bd0f3` 补上 authenticated analytics seeded ready-state 浏览器闭环：Playwright 统一使用 route-aware `analytics-readback-mock`，默认仍让 Realtime / Events / Goals 走失败态，只有按真实 Website `write_key` 显式 seed 后才返回成功数据；新增 `simpletrack-analytics-ready-state.spec.ts` 真实覆盖 sign-up、sign-in、onboarding、organization create、Website source create、提取真实 write key、seed Realtime / Events / Properties / Goals 读回放，并验证 Realtime、Events 和 Goals 可显示 ready 数据。目标验证 `corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-empty-state.spec.ts tests/simpletrack-analytics-ready-state.spec.ts` 已通过；Codex native 子代理本轮两次超时未完成真实复审，因此未把子代理审查记作已完成；父仓随后同步 `src/simpletrack-saas` gitlink 与实施进度文档 | P0-003、P0-004、P1-005E、simpletrack-saas、Realtime、Events、Goals、Playwright、ready-state、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `9d3bbc8` 把 property catalog 半失败态回归再往前推一步：analytics readback helper 改成按路由选择 analytics-service URL，避免属性目录 mock 串到其它读回放路由；Playwright property-catalog mock 补 `/healthz` readiness，保证运行时健康检查不会把浏览器回归误打红；`simpletrack-analytics-empty-state.spec.ts` 新增 Events 页 `Property suggestions unavailable` 与筛选控件可见性的真实浏览器断言。目标验证 `corepack pnpm --filter saas test -- analytics-readback-core.test.ts`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-empty-state.spec.ts --project chromium --reporter=dot` 已通过；Codex native 子代理本轮因 usage limit 未完成真实复审，因此未把子代理审查记作已完成；父仓随后同步 `src/simpletrack-saas` gitlink 与实施进度 / 源码解读文档 | P0-003、P0-004、simpletrack-saas、Events property catalog fail-soft、Playwright、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `829592b` 继续收口 authenticated Quickstart 新标签页可访问性：共享 `AnalyticsSetupEmptyState` 继续为 secondary Quickstart CTA 提供独立 accessible label，Websites 页内联 Quickstart helper link 也维持独立可读名称，避免 alert CTA 与 helper CTA 互相抢 locator；同时把 Playwright 里脆弱的 `quickstart-docs-page` 可见性断言改为 `quickstart` URL + 可见 step marker 验证，确保 docs popup 验收更稳。目标验证 `corepack pnpm --filter saas test -- readback-pages.test.tsx websites-page.test.tsx`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-empty-state.spec.ts --project chromium --reporter=dot` 已通过；DeepSeek 外部审查完成且无阻塞问题，Codex native 子代理真实复审结论 `APPROVE`，Gemini / Claude CLI 在本机外部评审链路上均失败并已如实记录 artifact；父仓随后同步 `src/simpletrack-saas` gitlink 与实施进度文档 | P0-003、P0-004、simpletrack-saas、Quickstart、Playwright、SSR 回归、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `0760713` 继续收口 authenticated Quickstart 外链一致性：共享 setup empty state 的 secondary Quickstart CTA 与 Websites 页创建区内联 Quickstart helper link 现在统一使用新标签页外链语义；`readback-pages.test.tsx` 继续锁住 Realtime / Events / Goals setup state 的 `target="_blank"` / `rel="noopener noreferrer"` 契约，`simpletrack-analytics-empty-state.spec.ts` 也新增 Websites 页“创建前内联 Quickstart link”浏览器断言，避免只锁住 alert/source-row CTA 却漏掉 helper copy。目标验证 `corepack pnpm --filter saas test -- readback-pages.test.tsx websites-page.test.tsx`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-empty-state.spec.ts --project chromium --reporter=dot` 均已通过；Codex native 子代理第一次真实复审抓到 shared CTA compile regression 和 helper-link 覆盖缺口，修复后再次复审结论 `APPROVE`；DeepSeek 外部审查完成且无阻塞问题；Gemini CLI 仍因 upstream `function_response.name: Name cannot be empty` 失败，已记录 artifact，不冒充完成 | P0-003、P0-004、simpletrack-saas、Websites、Quickstart、Playwright、SSR 回归、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `b8a8e01` 继续收口 authenticated Websites onboarding 路径：Website source 创建成功后的 success alert 现在会提供明确的 `Open Quickstart` CTA，write key 卡片也新增持久化 `Quickstart` CTA，并统一改为新标签页外链语义；`websites-page.test.tsx` 现分别锁住 alert CTA、row-level CTA、无 docs URL 时不渲染 CTA 的 SSR 契约，`simpletrack-analytics-empty-state.spec.ts` 也补上真实浏览器断言，验证 alert CTA 与 source row CTA 都存在且带 `target=\"_blank\"` / `rel=\"noopener noreferrer\"`。目标验证 `corepack pnpm --filter saas test -- websites-page.test.tsx`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-empty-state.spec.ts --project chromium --reporter=dot` 均已通过；Codex native 子代理真实复审发现 row-level CTA 覆盖缺口，已按意见补齐后再通过；DeepSeek 外部审查已完成并吸收外链语义建议；Gemini CLI 两次都因本机 CLI / upstream `function_response.name: Name cannot be empty` 失败，已记录 artifact，不冒充完成 | P0-003、P0-004、simpletrack-saas、Websites、Quickstart、Playwright、SSR 回归、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `8eab468` 继续收口 authenticated Goals readback fail-soft 契约：当页面上所有可见 goal 都命中同一条 non-ready readback failure 时，Goals 页现在会显示单条 `Goals readback unavailable` warning，并把逐行重复的 outage 文案收口为通用 `Verification count unavailable for this goal.` placeholder，继续避免假 `0` 计数；`readback-pages.test.tsx` 新增 SSR 回归覆盖共享 warning 和单次 outage 文案出现次数，`simpletrack-analytics-empty-state.spec.ts` 也更新为真实浏览器契约。目标验证 `corepack pnpm --filter saas test -- readback-pages.test.tsx`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-empty-state.spec.ts --project chromium --reporter=dot` 与 `git diff --check` 均已通过；Codex native 子代理本轮一次超时、一次命中 `503 No available channel for model gpt-5.4-mini`，因此未将失败重试记作审查完成 | P0-003、P0-004、P1-005E、simpletrack-saas、Goals、Playwright、SSR 回归、父仓 gitlink |
| 2026-05-11 | `simpletrack-saas` commit `6a3d677` 继续收口 authenticated analytics fail-soft 契约：Realtime 页面在 `service_error` 与 `not_configured` 等 non-ready 状态下不再展示伪 `0/0` 指标，而是显示 `Realtime readback unavailable` warning 和通用 live-stream placeholder；Events 页面在整条 readback outage 时不再把同一条 backend error copy 同时渲染到 alert 与 Raw events 卡片，而是改为单条 `Events readback unavailable` warning + 通用 placeholder，同时保留 filters；`readback-pages.test.tsx` 新增 SSR 回归覆盖 Realtime false-zero、防止 `not_configured` 误报 runtime recovery、Events 全 outage 单 warning；`simpletrack-analytics-empty-state.spec.ts` 也更新为真实浏览器契约。目标验证 `corepack pnpm --filter saas test -- readback-pages.test.tsx`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-empty-state.spec.ts --project chromium --reporter=dot` 与 `git diff --check` 均已通过；Codex native 子代理本轮多次命中 `429 Too Many Requests`，因此未将失败重试记作审查完成 | P0-003、P0-004、P1-005D、P1-005E、simpletrack-saas、Realtime、Events、Playwright、SSR 回归、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `b9a2d72` 继续补强 Events 半失败态回归：`readback-pages.test.tsx` 现不再只验证 property catalog `service_error` 时的提示和筛选栏，而是同时锁住 `Raw events` 卡片与真实事件行仍会渲染，避免后续只保住 warning、却把可用的数据表回归掉。目标验证目标文件 `oxfmt` / `oxfmt --check`、`corepack pnpm --filter saas test -- readback-pages.test.tsx` 与 `git diff --check` 均已通过 | P0-003、P0-004、P1-005D、simpletrack-saas、Events 半失败态、回归增强、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `246954d` 继续收口 authenticated analytics 的半失败态：Events 页面在事件读回放仍可用、但属性目录读回放失败时，会显示 `Property suggestions unavailable` 非阻塞提示，同时保留筛选表单和 Raw events 卡片；`readback-pages.test.tsx` 新增服务端渲染回归，锁住“property catalog service_error 不把 Events 页打回 setup 态”的契约。目标验证目标文件 `oxfmt` / `oxfmt --check`、`corepack pnpm --filter saas test -- readback-pages.test.tsx`、`corepack pnpm --filter saas exec tsc --noEmit` 与 `git diff --check` 均已通过；Codex verifier 子代理真实复审结论 `PASS` | P0-003、P0-004、P1-005D、simpletrack-saas、Events 半失败态、property catalog、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `edc7cbd` 继续收口 authenticated analytics 的真实浏览器回归：`simpletrack-analytics-empty-state.spec.ts` 现在在 analytics-service 不可用时额外验证 Events 页面仍保留 `Raw events` 标题、`Event name` 筛选输入和 `Apply filters` 按钮，避免页面退化回 setup 态；同时在 Websites 页验证 disabled source 会移除 source-scoped Realtime / Events / Goals 直达读回放链接，直到重新出现 enabled source。目标验证 `corepack pnpm exec oxfmt apps/saas/tests/simpletrack-analytics-empty-state.spec.ts`、`corepack pnpm exec oxfmt --check apps/saas/tests/simpletrack-analytics-empty-state.spec.ts`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-empty-state.spec.ts --project chromium` 与 `git diff --check -- apps/saas/tests/simpletrack-analytics-empty-state.spec.ts` 均已通过；Codex 子代理真实复审结论 `PASS` | P0-003、P0-004、P1-005E、simpletrack-saas、Playwright、Events fail-soft、disabled-source contract、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `d538e4a` 收口了 Supastarter workspace 配置噪音：`apps/saas/next.config.ts` 与 `apps/docs/next.config.ts` 现在显式把 `outputFileTracingRoot` / `turbopack.root` 锁到 `simpletrack-saas` 工作区，避免父仓 `uba` 的无关 lockfile 被 Next.js 推断成 workspace root；`apps/docs/app/layout.tsx` 新增受保护的 `metadataBase` 解析，只在显式 `NEXT_PUBLIC_DOCS_URL`、`VERCEL_URL` 或非生产本地开发时设置，避免生产元数据回退到 localhost。目标验证 `corepack pnpm exec oxfmt ...`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter docs run build` 与 `corepack pnpm --filter saas run build` 均已通过，Codex code-reviewer 子代理最终结论 `no blocking issues` | P0-003、P0-004、simpletrack-saas、Next.js workspace root、docs metadataBase、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `6a14cd9` 继续推进 authenticated analytics 浏览器回归：Playwright SaaS runtime 现在显式配置 `SIMPLETRACK_ANALYTICS_SERVICE_URL=http://127.0.0.1:9` 和测试 query token，用真实 server-side readback 路径触发 analytics-service 不可用；`simpletrack-analytics-empty-state.spec.ts` 在创建真实 Website source 后验证 Realtime、Events、Goals 都能 fail-soft 显示 service error，然后继续执行 disabled-source fallback 流程；目标验证 `oxfmt`、`saas` `tsc --noEmit`、目标 Playwright chromium 均已通过，Codex code-reviewer 子代理结论 `no blocking issues` | P0-003、P0-004、P1-005E、simpletrack-saas、Playwright、readback service_error、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `cb643e3` 继续收口 authenticated analytics 的浏览器级 fallback 回归：`simpletrack-analytics-empty-state.spec.ts` 现在把 disabled-source fallback 从只验证 Realtime 扩展到 Events 和 Goals，确保三个分析入口页都稳定显示 fallback notice，并保留同一个 `Manage websites -> edit_target` 深链契约；目标验证 `corepack pnpm exec oxfmt --check apps/saas/tests/simpletrack-analytics-empty-state.spec.ts`、`corepack pnpm --filter saas exec tsc --noEmit` 与 `corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-empty-state.spec.ts --project chromium` 均已通过，Codex code-reviewer 子代理结论 `no blocking issues` | P0-003、P0-004、P1-005E、simpletrack-saas、Playwright、disabled-source fallback、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `aff924e` 继续收口 authenticated analytics 的 Quickstart 浏览器回归：在已有真实跨 origin docs `/quickstart` 点击验证基础上，把 setup empty state 的 Quickstart CTA 覆盖从只验证 Realtime 扩展到 Events 和 Goals，确保三个分析入口页都真实离开 SaaS origin 并落到 docs app 的稳定 test id；目标验证 `corepack pnpm exec oxfmt --check apps/saas/tests/simpletrack-analytics-empty-state.spec.ts`、`corepack pnpm --filter saas exec tsc --noEmit` 与 `corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-empty-state.spec.ts --project chromium` 均已通过 | P0-003、P0-004、P1-005E、simpletrack-saas、Playwright、Quickstart docs 跳转、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `b3d2904` 继续收口 Quickstart docs 跳转回归的脆弱性：`apps/docs/content/docs/quickstart.mdx` 给可见内容补上稳定 `data-testid`，`simpletrack-analytics-empty-state.spec.ts` 改为断言 docs 页面稳定锚点而不是具体 heading 文案，避免跨项目内容改动把 SaaS 回归误打红；目标验证 `corepack pnpm exec oxfmt --check apps/saas/tests/simpletrack-analytics-empty-state.spec.ts`、`corepack pnpm --filter saas exec tsc --noEmit` 与 `corepack pnpm --filter saas exec playwright test tests/simpletrack-analytics-empty-state.spec.ts --project chromium` 均已通过 | P0-003、P0-004、P1-005E、simpletrack-saas、Playwright、docs quickstart、test hardening、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `d48660e` 继续把 Supastarter authenticated analytics Playwright 回归补实：在已有 sign-up、sign-in、onboarding、organization create、Realtime / Events / Goals setup empty state、Websites create、disabled-source fallback 和 `edit_target` 深链展开基础上，再把 Realtime setup empty state 的 Quickstart CTA 改为真实点击，并验证浏览器从 SaaS origin 跳到独立 docs app `/quickstart` 后渲染 Quickstart 内容，不再只检查 href；目标验证 `corepack pnpm exec oxfmt --check apps/saas/playwright.config.ts apps/saas/tests/simpletrack-analytics-empty-state.spec.ts`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas exec playwright test tests/login.spec.ts tests/simpletrack-analytics-empty-state.spec.ts` 与 `CI=1` 同命令均已通过；真实 Codex 子代理最终复审结论 `Merge-ready: Yes`，DeepSeek 给出非阻塞 comment，Gemini 仍只返回“请提供 diff”，失败原因沿用卡壳记录 | P0-003、P0-004、P1-005E、simpletrack-saas、Playwright、Quickstart docs 跳转、code review、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `10697eb` 把 Supastarter authenticated analytics 流程补进真实 Playwright e2e：新增 `simpletrack-analytics-empty-state.spec.ts`，真实覆盖 sign-up、sign-in、onboarding、organization create、Realtime / Events / Goals setup empty state、Websites create、disabled-source fallback 和 `edit_target` 深链展开；同时移除测试中对 Postgres 的 raw mutation 依赖，Playwright config 不再强绑本机 Chrome，并补齐 `PLAYWRIGHT_PORT` override / 非法值回退与 docs URL 测试环境注入；目标验证 `corepack pnpm exec oxfmt --check apps/saas/playwright.config.ts apps/saas/tests/simpletrack-analytics-empty-state.spec.ts`、`corepack pnpm --filter saas exec tsc --noEmit`、`corepack pnpm --filter saas exec playwright test tests/login.spec.ts tests/simpletrack-analytics-empty-state.spec.ts` 与 `CI=1` 同命令均已通过；真实 Codex 子代理复审结论 `No blocking issues found`，剩余说明仅为 docs 链接在测试环境下仍指向 SaaS base URL；父仓 gitlink 与进度文档第一次同步提交为 `b8139a9` | P0-003、P0-004、simpletrack-saas、Playwright、authenticated analytics、code review、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `075828d` 继续收口 authenticated analytics 页面：共享 `AnalyticsSetupEmptyState` 现在支持 `Websites + Quickstart` 双动作，`resolveEnabledWebsiteSelection` 在 fallback 中补充 `requestedWebsiteId`，并通过 `buildWebsiteSettingsHref()` 把 disabled-source fallback 深链统一收口到 Websites 设置页；`websites/page.tsx` 现在对任何合法 `edit_target` 都会展开目标 source，不再只在更新反馈态下生效；目标验证 `corepack pnpm --filter saas test -- readback-pages.test.tsx websites-page.test.tsx website-selection.test.ts website-management.test.ts`（142 tests）、`corepack pnpm --filter saas run type-check`、Codex 最终子代理复审 `APPROVE`、架构复审 `CLEAR` 均已通过；DeepSeek 外部审查 `APPROVE` 且只给低风险 UX 建议，Gemini 仍报 `function_response.name: Name cannot be empty`，artifact 已写入 `src/simpletrack-saas/.omx/artifacts/`；父仓本轮随后同步 `src/simpletrack-saas` gitlink、实施进度与源码解读文档 | P0-003、P0-004、P1-005E、simpletrack-saas、authenticated analytics UI、外部审查、code review、source-reading、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `2a33f00` 继续收口 authenticated readback 页面：新增共享 `AnalyticsSetupEmptyState` 与 `WebsiteSourceFallbackNotice`，把 enabled source 解析统一收口到 `website-selection.ts`；Realtime / Events / Goals 在无启用 source 时不再展示半可用 analytics UI，Realtime / Events 会跳过 source-bound readback，requested disabled source 回退到 enabled source 时给出显式提示；目标验证 `corepack pnpm --filter saas test -- readback-pages.test.tsx website-selection.test.ts`（140 tests）、`corepack pnpm --filter saas run type-check`、Codex 子代理最终复审 `APPROVE`、架构复审 `CLEAR` 均已通过；DeepSeek 外部审查给出 comment 后已由 fallback notice 收口，Gemini 仍只返回“请提供 diff”，Claude 仍报 `1m` 上下文限制，真实失败原因继续记录到 `docs/开发环境卡壳问题记录.md`；父仓本轮随后同步 `src/simpletrack-saas` gitlink、实施进度与源码解读文档 | P0-003、P0-004、P1-005E、simpletrack-saas、authenticated readback UI、外部审查、code review、source-reading、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `08b9acc` 收口了本地 authenticated SaaS 评审的 auth/mail runtime：development 默认不再强制 email verification，且只有 development 在未配置 mail provider 时才启用 console fallback；真实验证已覆盖 `POST /api/auth/sign-up/email` 返回 `200` 并写入 `better-auth.session_token`、`POST /api/auth/sign-in/email` 成功、带 cookie 访问 `/onboarding` 返回 `200`；目标 `oxfmt`、Vitest（131 tests）、`saas` / `@repo/auth` / `@repo/mail` type-check 均通过，Codex native 子代理最终复审 `APPROVE` 并已关闭；父仓随后同步 `src/simpletrack-saas` gitlink 与实施进度 / 源码解读文档 | P0-003、P0-004、simpletrack-saas、auth、mail、local dev、code review、source-reading、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `b1db9d5` 让 server-side analytics readback helper 按 `realtime / events / properties / goals` 优先选择 route-scoped query token，并保留 shared token 与 `ANALYTICS_SERVICE_QUERY_TOKEN_<ROUTE>` 兼容别名；目标验证 `corepack pnpm exec oxfmt --check ...`、`corepack pnpm --filter saas test -- analytics-readback-core.test.ts`（121 tests）、`corepack pnpm --filter saas type-check` 均通过；Codex 子代理最终复审 `APPROVE` 并已关闭，父仓随后同步 `src/simpletrack-saas` gitlink 与源码解读/实施进度文档 | P1-005D、P1-005E、simpletrack-saas、query token、route scope、code review、source-reading、父仓 gitlink |
| 2026-05-10 | `analytics-service` commit `580c547` 为内部 query token 增加 readback route scopes：支持在主 token 或轮换 token 上按 `realtime / events / properties / goals` 限制可访问的内部读回放族，并在 source resolution 之前执行 scope 判定；有效但越权的 token 现在稳定返回 `403 forbidden`。父仓同步回填 `docs/analytics-source-reading/README.md`、`collectapi-query-and-tracker-flow.md` 与实施决策 README，并更新 `src/analytics-service` 子模块 gitlink | P1-005D、P1-005E、analytics-service、query token、readback route scopes、source-reading、父仓 gitlink |
| 2026-05-10 | `analytics-core` commit `a99147f` 将 bounded scalar ClickHouse reader benchmark / explain 从单一 `24h` 扩成 `24h / 72h / 7d` 多窗口套件；补跑的真实证据显示 `24h` 与 `72h` 仍在 direct fact-table 中等观察区，而 `7d @ 1,000,000 rows` 才首次进入 `46-52ms/op` 压力区。父仓同步回填 `docs/analytics-source-reading/README.md`、`read-side-benchmark-baseline.md` 与实施决策 README，并更新子模块 gitlink | P1.5-001、analytics-core、ClickHouse benchmark、bounded scalar、source-reading、父仓 gitlink |
| 2026-05-10 | `analytics-service` commit `c08e1da` 根据 `analytics-core` `93cff0f` 之后补跑的 100k / 500k bounded 24h scalar Events 证据，撤回了 `24h => pressure=high` 的 service-side bounded scalar heuristic；`queryPressure` 重新回到 filter-count 主导，handler/planner 回归同步更新为 wide scalar `medium`、exact 24h bounded scalar `low`。子仓验证 `go test ./internal/collectapi`、`go test ./...`、`go vet ./...`、`golint ./...` 均通过，Codex 子代理对最终 diff 真实复审 `APPROVE` | P1.5-001、analytics-service、pressure bucket、bounded scalar Events、code review、source-reading |
| 2026-05-10 | 在 `analytics-core` `93cff0f` 基础上补跑 `ANALYTICS_CORE_CLICKHOUSE_BENCH_ROWS=100000` 的 bounded 24h scalar Events 真实证据：`TestEventReaderClickHouseExplain/medium_events_scalar_bounded_24h_window` 记录 `eligible_rows=86400`、`time_window_seconds=86400`、`Granules: 12/13`；对应 benchmark 三次约 `10.8ms/op`、`12.6ms/op`、`15.5ms/op`。结论是它已经是独立读形状，但当前仍不足以单独触发 projection / materialized view / 小时聚合表 | P1.5-001、analytics-core、ClickHouse explain、benchmark baseline、bounded 24h scalar Events |
| 2026-05-10 | `analytics-core` commit `93cff0f` 继续推进 P1.5-001：把 bounded 24h scalar Events benchmark / explain 场景收口为“只有 fixture > 86400 时才启用”，并把 `benchmarkEndTime` 改成精确排他上界，避免默认 10k 基线把 24h 场景伪装成 wide-window，同时确保 `QueryEvidence.TimeWindowSeconds` 对齐真实 `86400` 秒；Go 验证 `go test ./internal/e2e -run '^$'`、`go test ./...`、`go vet ./...`、`golint ./...` 均通过，Codex 子代理对最终 diff 真实复审 `APPROVE` | P1.5-001、analytics-core、ClickHouse reader benchmark、explain、code review、source-reading |
| 2026-05-10 | 将 `docs/analytics-source-reading/read-side-benchmark-baseline.md` 补齐到当前 P1.5-001 口径：明确 `analytics-service` `da852cd` 的 bounded Events `24h+ -> pressure=high` 只是 service-side triage heuristic，它引用的是 500k 宽时间窗 scalar Events 的观察结论，但不能单独作为新增 projection / materialized view / 小时聚合表的依据 | P1.5-001、source-reading、analytics-service、benchmark baseline、pressure heuristic |
| 2026-05-10 | 回填 `analytics-service` `da852cd` 落地后的父仓文档细节：将 `simpletrack/docs/实施决策/README.md` 的版本标记从 `pending commit` 改为父仓上一轮同步 commit `952f0a8`，并把 `docs/analytics-source-reading/collectapi-query-and-tracker-flow.md` 的 `query_evidence` 结构证据与 handler 回归证据更新到当前 `da852cd` commit 和最新行号，避免源码解读说明已更新但 citation 仍停留在旧服务版本 | P1.5-001、实施决策库、source-reading、analytics-service、citation hygiene |
| 2026-05-10 | `analytics-service` commit `da852cd` 继续推进 P1.5-001：在 `query_evidence` -> `pressure` 映射里加入“宽时间窗 bounded scalar Events”规则，bounded Events 在无 property join 且 `time_window_seconds >= 24h` 时会进入 `high` triage bucket；同时补齐 exact `24h` 边界、`from/to`-only bounded Events、recent scalar Events 和 wide scalar Events 的 planner-backed handler 回归。子仓验证 `go test ./internal/collectapi`、`go test ./...`、`go vet ./...`、`go test ./... -run Example -count=1`、`golint ./...` 均通过；Codex 子代理复审 `APPROVE`，架构子代理给出 `WATCH`，结论是 `24h` 阈值属于 service-side triage heuristic，应文档化但不下沉到 `analytics-core` | P1.5-001、analytics-service、wide-window scalar Events、pressure heuristic、code review、source-reading |
| 2026-05-10 | `analytics-service` commit `6f6f742` 为 P1.5-001 补上 service 边界回归：`/v1/events` handler 测试不再只依赖 stub reader，而是通过真实 `analytics-core` ClickHouse query builder 规划 Events 查询，验证 typed property filter 在 `168h` 边界窗口下通过、在 `192h` 宽窗口下稳定返回 core guardrail `400`；子仓验证 `go test ./internal/collectapi`、`go test ./...`、`go vet ./...`、`go test ./... -run Example -count=1`、`golint ./...` 均通过，Codex 子代理二次复审 `PASS`；父仓随后同步实施决策与源码解读文档 | P1.5-001、analytics-service、property filter、service boundary regression、code review、source-reading |
| 2026-05-10 | `simpletrack-saas` commit `ffdb254` 收口了 Websites 页面 UI：把“行内整块设置表单”改成“紧凑来源行 + write key 面板 + 右侧动作 + accordion 设置区”，并补上“设置提交成功或失败后默认重新展开刚才编辑的来源”，避免 server action 刷新后丢失上下文；目标验证 `oxfmt --check`、`corepack pnpm --filter saas test -- websites-page.test.tsx`（115 tests passed）和 `corepack pnpm --filter saas type-check` 已通过，系统 Chrome 截图沿用 `websites-refactor-collapsed-v2.png` / `expanded-v2.png`；DeepSeek 已基于实际代码片段给出 `APPROVE`，Gemini 最小 prompt 仍报 `function_response.name: Name cannot be empty`，Codex native verifier 子代理真实返回 `PASS` | P0-003、P0-004、simpletrack-saas、Websites UI、外部评审、代码审查 |
| 2026-05-10 | `simpletrack-saas` commit `9ebe9f4` 收口了 Supastarter 本地 authenticated 登录的 loopback 开发策略：`apps/saas/next.config.ts` 与 Better Auth 统一复用 `loopback-trusted-origins` helper，生产环境仍只信任配置的 `appUrl`；Vitest、type-check、Playwright+系统 Chrome 已验证 `localhost:3005` / `127.0.0.1:3005` 都能登录跳转；Codex 子代理代码复审 `APPROVE`，架构复审给 `WATCH` 后已继续收口到公开 package import，DeepSeek / Gemini 外部评审 artifact 已保存到 `src/simpletrack-saas/.omx/artifacts/`；父仓 gitlink 与文档回填为 `3b56959` | P0-003、P0-004、simpletrack-saas、authenticated 登录、本地开发环境、外部评审、父仓 gitlink |
| 2026-05-10 | `simpletrack-saas` commit `9c8ba37` 收口了 authenticated readback 页面 UI：Realtime / Events / Goals 的 Website selector 现在稳定显示选中站点文本并带 domain/id fallback，Events 卡片不再重复渲染同一条空态/错误信息，readback helper 把 `4xx` rejection、`5xx` 和 network failure 分成不同的产品态文案；目标 Vitest、type-check、Playwright+系统 Chrome 截图验证和 Codex 子代理复审已完成；DeepSeek 外部审查给出低风险建议后已回收，Gemini CLI 新的 `command line is too long` 与 `function_response.name: Name cannot be empty` 失败模式已补记到 `docs/开发环境卡壳问题记录.md`；父仓 gitlink 与文档回填 commit 为 `7db421b` | P0-003、P0-004、P1-005D、P1-005E、simpletrack-saas、UI 审查、开发环境卡壳记录、父仓 gitlink |
| 2026-05-10 | 回填 `src/simpletrack-saas` 子模块 gitlink 到 `d53de11`：marketing 公开站点的 `privacy-policy` / `terms` 已从 Supastarter 模板占位替换为 SimpleTrack 基线文案；DeepSeek 外部审查与 Codex 子代理审查已真实完成，Gemini/Claude CLI 失败原因转记到 `docs/开发环境卡壳问题记录.md`；父仓本轮文档 commit `4fe6374` | P0-004、P1-003、INFRA-001、simpletrack-saas、公开站点、外部审查、父仓 gitlink |
| 2026-05-10 | 对齐 `待评审事项.md` 与 `README.md` 的当前实施状态：R3-U3/U4/U5 改为已完成口径，P1-005 后续重点移除已落地的 quota / domain allowlist / internal traffic 配置项，`README.md` 移除过期父仓 commit 描述并收敛 UI 评审措辞；父仓本轮文档 commit `3101d07` | 实施决策库、待评审事项、当前进度、UI 评审、analytics-service |
| 2026-05-10 | 回填 `src/analytics-service` 子模块 gitlink 到 `3b8d27c`，并同步更新 `simpletrack/docs/实施决策/README.md`、`分阶段实施计划.md` 和 `docs/analytics-source-reading/README.md` 的当前版本标记；父仓本轮文档 commit `6db373d` | INFRA-001、P1-005E、实施决策库、源码解读、父仓 gitlink |
| 2026-05-10 | 同步 `docs/analytics-source-reading/collectapi-query-and-tracker-flow.md`、`data-flow-analysis.md`、`interfaces-and-formats.md` 和 `read-side-benchmark-baseline.md` 的 read-side 口径：typed property filters 必须显式 `from/to`，direct fact-table 默认只允许 7 天内窗口，`query_evidence` 只回显 value-free 形状。对应父仓提交覆盖 `39a0174`、`40e6c60`、`aa2ab27` | P1.5-001、source-reading、analytics-core、analytics-service、property filter、query evidence |
| 2026-05-10 | 同步 `docs/analytics-source-reading/read-side-benchmark-baseline.md` 的读侧基线结论：`analytics-core` commit `f84024a` 之后，typed property filter 默认只允许显式 `from/to` 且 direct fact-table 窗口不超过 7 天；后续观察候选收窄为宽时间窗 scalar Events 明细查询和 7 天内 typed property 过滤读路径。父仓文档 commit 已回填 `9a9b293` | P1.5-001、source-reading、analytics-core、property filter、query guardrail |
| 2026-05-09 | `analytics-core` commit `f84024a` 将 typed property filters 收口到 query-builder guardrail：属性过滤必须显式带 `from/to`，且 direct fact-table 查询窗口默认不超过 7 天；同时补齐 exact-boundary、missing-bound 和 wide-window 回归，子仓 README / `docs/read-side-optimization-policy.md` 已同步；本轮 `go test ./...`、`go test -run Example ./...`、`go vet ./...`、`golint ./...`、`go doc -all ./storage ./storage/clickhouse` 和 `git diff --check` 均通过，Codex 子代理真实复审 `PASS`；父仓文档 commit 已回填 `76589df` | P1.5-001、analytics-core、query guardrail、property filter、code review、ai-slop-cleaner、source-reading |
| 2026-05-09 | `analytics-service` commit `0248a80` 与 `simpletrack-saas` commit `e69cea6` 收口 `readback_policy` 控制面闭环：SaaS runtime-source 显式输出 Realtime / Events / Properties / Goals 四类 readback 开关，analytics-service 在 query token、write key、origin 校验后按 route fail closed，并对禁用路由返回稳定 `403 readback is disabled`；本轮 Go 测试、`go vet`、`go test -run Example`、`golint`、`@repo/api` Vitest / type-check、root lint 均通过，Codex 子代理真实复审 `PASS`；root `pnpm type-check` 因本机 Turborepo package-manager binary 解析问题失败，已记录到 `docs/开发环境卡壳问题记录.md`；父仓文档 commit 已回填 `1a81b75` | P1-005C、P1-005D、P1-005E、analytics-service、simpletrack-saas、readback policy、code review、source-reading |
| 2026-05-09 | `simpletrack-saas` commit `9c56e96` 将 Goal readback fan-out 用 `MAX_WEBSITE_GOALS` 在 Prisma query 与 Goals 页面双层收口到 25，避免单页对 `/v1/goals` 做无界线性 fan-out；目标 Vitest、type-check、lint 已通过，Codex 子代理复审 `PASS`，DeepSeek 外部审查仅给低风险提示；Gemini/Claude 审查失败与 DeepSeek CLI 恢复情况已记录到 `docs/开发环境卡壳问题记录.md`；父仓文档回填 commit `b5ec366` | P1-005E、simpletrack-saas、Goal、fan-out、code review、DeepSeek、source-reading |
| 2026-05-09 | 收口 Goal 最小闭环 `WATCH` 项中的事件名契约复制风险：`analytics-core` commit `1ea78f3` 为 `collect.ValidateEventName` 增加 accepted/rejected contract samples，`simpletrack-saas` commit `6416a51` 用同一组样例锁定 Goal 保存前校验，覆盖 digit-first、mixed-case、长度边界和非法前导符号；真实子代理复审结论为 `WATCH`，无阻塞缺陷，剩余风险是 Go/TS 仍非生成式共享契约；父仓 commit `3480d5e` | P1-005E、Goal、event_name、analytics-core、simpletrack-saas、code review、source-reading |
| 2026-05-09 | Goals readback UI 状态拆分已随 `simpletrack-saas` commit `e86ba88` 推送：ready 且有数据显示 `Has data` + 数字，ready 但无数据显示 `No data` + `0`，服务失败显示 `Readback error` + `-`；目标 Vitest、type-check、lint、Codex 子代理审查和 Gemini UI 评审均通过，DeepSeek 按用户明确指令临时跳过并已记录到卡壳问题；父仓 rebase 后已同步 gitlink 和文档，父仓 commit `b672e96` | P1-005E、Goals readback UI、simpletrack-saas、code review、Gemini、DeepSeek 临时豁免、source-reading |
| 2026-05-09 | 补充 Windows SSH / GitHub 推送排障结论：用户交互式 PowerShell 已验证 `ssh -F config_simpletrack` 能认证到 `RoggerZ`，说明 GitHub key 与授权可用；后续已完成 `analytics-core` `b820f7e`、`analytics-service` `f60ac59`、`simpletrack-saas` `e86ba88` 的远端同步，并在父仓 commit `b672e96` 回填实施进度 | INFRA-001、Windows SSH、GitHub push、Q&A、开发环境卡壳记录 |
| 2026-05-09 | P1 Goal 最小闭环补跑收口验证：`analytics-core` commit `b820f7e` 通过 `go test ./storage/...`；`analytics-service` commit `f60ac59` 通过 `go test ./internal/collectapi`、`go test ./...`、`go vet ./...`、`golint ./...`；`simpletrack-saas` commit `461900a` 通过 Goal/readback 目标 Vitest、`@repo/database` type-check、`saas` type-check 和 root lint；后续 Goals readback UI 状态拆分已推进到 `simpletrack-saas` commit `e86ba88`，父仓文档由 commit `b672e96` 回填 | P1-005E、verification、analytics-core、analytics-service、simpletrack-saas、Git ACL |
| 2026-05-09 | P1 Goal 最小闭环进入真实实现：`analytics-core` 新增 `EventCountQuery` / `BuildEventCountQuery` / `CountEvents`，`simpletrack-anaysitics-service` 新增内部 `/v1/goals` 读回放入口，`simpletrack-saas` 新增 Goal 数据模型、创建校验、组织内 Goals 页面和服务端 readback helper；版本标记：`analytics-core` commit `b820f7e`，`analytics-service` commit `f60ac59`，`simpletrack-saas` commit `461900a`，后续 UI 状态拆分推进到 `simpletrack-saas` commit `e86ba88`、父仓 commit `b672e96`；真实架构审查结论为 `WATCH`，后续真实代码质量复审结论为 `PASS`，剩余风险是 Goals 页面按目标数量线性 fan-out count readback，且尚未做 live browser / e2e 全链路验证 | P1-005E、analytics-core、analytics-service、simpletrack-saas、Goal、readback、code review、source-reading |
| 2026-05-09 | 补齐 `analytics-core` 子仓 README 与 `docs/read-side-optimization-policy.md`：把 Events recent-window / wide-window scalar/property 场景和真实 `EventQueryPlan` 时间边界 preflight 写入子仓策略文档；子仓提交 `7c31eb2` 已推送，父仓同步 gitlink | P1.5-001、analytics-core、read-side policy、source-reading、submodule |
| 2026-05-09 | P1.5-001 将 `analytics-core` ClickHouse reader benchmark / explain 进一步拆成 Events recent-window 与 wide-window scalar/property 场景；子仓提交 `caf314d` 已推送，并在执行前断言真实 `EventQueryPlan.QueryEvidence()` 与 `plan.Args` 都保留 `from/to` 时间上下界。500k 行复测显示近期 Realtime / Events 稳定，宽时间窗 Events 与 typed property 过滤继续观察，当前仍不新增 projection / materialized view / 小时聚合表 | P1.5-001、analytics-core、ClickHouse、EventReader、benchmark、explain、code review、ai-slop-cleaner、source-reading |
| 2026-05-08 | 根据补充结对审查和 `$ai-slop-cleaner` 流程修复 `analytics-core` query evidence 快照边界：`NewEventQueryPlan` 和 `QueryEvidence()` 现在会复制 `PropertyFilters` slice，避免调用方修改返回值污染 canonical evidence；同时新增 value-free evidence example 和不可变性回归测试；子仓提交 `b693d62` 已推送 | P1.5-001、analytics-core、query evidence、property filter、code review、ai-slop-cleaner、source-reading |
| 2026-05-08 | P1.5-001 完成 100k 行 typed property 过滤读路径复测：在 `analytics-core` commit `4393bbd` 上重新执行 ClickHouse explain 与 `BenchmarkEventReaderClickHouseExecution`，确认 `property_filters` evidence 只暴露 scope/name/value_type/operator，不暴露 value；high property 三次约 `29.8-32.8ms/op`，与上一轮 `31.3-34.2ms/op` 同一观察区，继续保持 direct fact table，不新增 projection / materialized view / 小时聚合表 | P1.5-001、analytics-core、ClickHouse、query evidence、property filter、benchmark、source-reading |
| 2026-05-08 | P1.5-001 补齐 typed property filter 的 query evidence 形状：`analytics-core` 的 `EventQueryEvidence` 现在记录属性过滤的 scope/name/value_type/operator，不记录实际 value；`simpletrack-anaysitics-service` 内部 readback API 与 OpenAPI 透出 `property_filters`，用于后续判断 projection / materialized view / 小时聚合表，不从 SQL 字符串或 URL 参数反推查询形状；子仓提交 `4393bbd`、`64b0bda` 已推送 | P1.5-001、analytics-core、analytics-service、query evidence、property filter、OpenAPI、source-reading |
| 2026-05-08 | P1.5-001 锁定 Events filter builder 的 catalog selection 行为：`simpletrack-saas` 将属性选中逻辑抽成 `resolveCatalogSelectionPatch` 纯函数，覆盖 catalog-backed scope/type 回填、null 属性 equals-only、freeform 属性名保留和手工覆盖已选 catalog 属性；未引入 jsdom/testing-library，子仓提交 `a48ce44` 已推送 | P1.5-001、simpletrack-saas、Events、property catalog、filter builder、source-reading |
| 2026-05-08 | P1.5-001 把 `/v1/properties` 接入 `simpletrack-saas` Events filter builder：SaaS 服务端新增 property catalog readback wrapper，Events 页面并行读取事件明细和属性目录，把 source-scoped event/user property selector 传给筛选控件作为建议；内部 query token 仍只在服务端使用，非法 catalog payload 会返回 service error，避免契约漂移被伪装成空字典；子仓提交 `5147f78` 已推送 | P1.5-001、simpletrack-saas、Events、property catalog、filter builder、source-reading |
| 2026-05-08 | P1.5-001 把属性目录治理从写入链路补齐到内部读回链路：`analytics-core` 新增 source-scoped `PropertyCatalogReader` / `PropertyCatalogQuery` 和 MySQL `ListPropertyCatalogEntries`，`simpletrack-anaysitics-service` 新增内部 `/v1/properties`，复用 query token 与 write key source resolution，供后续 UI filter builder 读取 event/user property selector 和类型；子仓提交 `91ac1db`、`89608bf` 已推送 | P1.5-001、analytics-core、analytics-service、property catalog、readback API、OpenAPI、source-reading |
| 2026-05-08 | P1.5-001 把 ClickHouse explain 证据纳入仓库工作流：`analytics-core` 新增 opt-in `TestEventReaderClickHouseExplain`，复用 reader benchmark 的 routed table fixture 和 sealed query plan；当前 100k 行下 high property 场景已观察到 `CreatingSets`、3 个 `event_id in ... set` 和包含 `visit_id` 的主键条件，但仍只作为 typed property 路径的下一条观察证据，不触发立即新增 projection / MV / 小时聚合表 | P1.5-001、analytics-core、ClickHouse、explain、source-reading |
| 2026-05-08 | P1.5-001 补充 100k 行 ClickHouse reader pressure run：low realtime 约 11.6-13.5ms/op，medium scalar events 约 10.7-11.6ms/op，high property events 约 31.3-34.2ms/op；下一条重点观察候选调整为 typed property 过滤读路径，但仍需 query plan、ClickHouse explain、目标数据量和回归计划后再决定是否引入物理结构 | P1.5-001、analytics-core、ClickHouse、property filter、benchmark、source-reading |
| 2026-05-08 | P1.5-001 记录真实 ClickHouse reader benchmark 基线：`BenchmarkEventReaderClickHouseExecution` 在本机 ClickHouse 25.3 上跑通，low realtime 约 8.6-10.4ms/op，medium scalar events 约 8.6-9.7ms/op，high property events 约 15.2-16.3ms/op；当前结论是不因此新增 projection / MV / 小时聚合表，只作为后续对比基线 | P1.5-001、analytics-core、ClickHouse、EventReader、benchmark、source-reading |
| 2026-05-08 | P1.5-001 继续补齐 query shape evidence：`analytics-core` 的 `EventQueryEvidence` 新增 effective limit、offset、time lower/upper bound 和 bounded time window；`simpletrack-anaysitics-service` 的内部 readback API 与 OpenAPI 同步透出这些字段，后续评审 projection / MV / 小时聚合表时不再从 SQL 或 URL 参数反推查询形状 | P1.5-001、analytics-core、analytics-service、query evidence、readback API、OpenAPI |
| 2026-05-08 | `analytics-core` 新增 `docs/read-side-optimization-policy.md`，把 ClickHouse projection、materialized view、小时聚合表的引入门槛固化到子仓：新增物理结构前必须提供产品能力、`QueryEvidence`、benchmark、预期数据量、写入影响和回归计划，且 `pressure=high` 不能单独作为依据 | P1.5-001、analytics-core、ClickHouse 读侧规范、benchmark、projection、materialized view、小时聚合表 |
| 2026-05-08 | `analytics-core` 补齐 GORM `CreateInBatches` 与 native ClickHouse 写入对照 benchmark：单事件路径按 route + JSON marshal + insert 公平对比，100 行批量路径按 insert-only 对比；当前结果显示 GORM 可用但在单事件与 100 行批量场景下整体慢于 native `PrepareBatch` 且分配更多，因此事件热路径继续保持 native writer 优先 | P1.5-001、analytics-core、ClickHouse、GORM、batch writer、benchmark |
| 2026-05-08 | `analytics-core` 新增 `collect.Handler` 热路径 benchmark，分开测量 normalize+publish、session/visit identity resolver、identity + client enrichment 三种路径；计时区排除 handler/stage/request 构造和后置断言，本地基线约为 3.2-6.1µs/op、11.0-12.0µs/op、15.2-18.2µs/op | P1.5-001、analytics-core、collect、benchmark |
| 2026-05-08 | `analytics-core` 新增 Redis Stream EventBus benchmark，覆盖 collect publish 路径和 ingestion subscribe/decode/ack 路径；计时区排除消息预生成、预发布和 consumer group 创建，并等待 pending=0 后再停止计时；本地 Redis 基线约 publish 0.65-0.71ms/op、subscribe+ack 1.5-1.83ms/op | P1.5-001、analytics-core、Redis Stream、EventBus、benchmark |
| 2026-05-08 | `analytics-core` 新增真实 ClickHouse `BatchWriter.WriteEvent` 写入热路径 benchmark，使用预生成事件避免 fixture 构造污染计时，并用 bounded context 与最终行数校验确认成功写入；当前单事件 native batch 写入基线约 5.8-6.4ms/op | P1.5-001、analytics-core、ClickHouse、BatchWriter、benchmark |
| 2026-05-08 | `analytics-core` 新增真实 ClickHouse `EventReader` 执行 benchmark，使用本地 ClickHouse、路由事件表和属性表验证 low realtime、medium scalar events、high property events 三类查询，并用 checked teardown 防止物理表残留 | P1.5-001、analytics-core、ClickHouse、EventReader、benchmark |
| 2026-05-08 | `analytics-core` 新增 builder-only read-side shape benchmark，覆盖 low realtime、medium scalar events、high property events 三类查询，并把 query evidence 断言前置到计时前，作为后续真实 ClickHouse 压测前的稳定基线 | P1.5-001、analytics-core、benchmark、压测基线 |
| 2026-05-08 | 将 `pressure` 的 OpenAPI 与代码注释措辞统一为 read-side triage bucket，避免后续误解成硬阈值、SLA 或自动扩缩容信号；同步更新 source-reading 的 analytics-service commit 证据 | P1.5-001、analytics-service、OpenAPI、source-reading |
| 2026-05-08 | 继续推进 P1.5-001 的 read-side evidence 语义：为 `query_evidence` / `pressure` 新增 Q&A，明确 `pressure` 只是 low / medium / high 的 triage 桶，不是 SLA 或自动扩缩容信号；`simpletrack-anaysitics-service` 的压力分档补了单测，并同步更新 source-reading 说明 | P1.5-001、analytics-service、Q&A、source-reading |
| 2026-05-08 | P1.5-001 继续把 read-side evidence 送到服务层：`analytics-core` 新增 `EventReaderWithEvidence` / `EventQueryResult` 作为可选兼容接口，`simpletrack-anaysitics-service` 的 `/v1/events` / `/v1/realtime` 响应开始返回 `query_evidence` 与 `pressure`，并保留旧 `EventReader` fallback | P1.5-001、analytics-core、analytics-service、query evidence、readback API |
| 2026-05-08 | P1.5-001 将属性目录治理接入 ingestion 热路径：`analytics-core` 新增 `PropertyCatalogingEventWriter`，在主事件写入成功后做 replay-safe catalog upsert；`simpletrack-anaysitics-service` 默认启用 `ANALYTICS_SERVICE_PROPERTY_CATALOGING`，本地初始化可随 `MySQLAutoMigrate` 创建 `property_catalog`，生产形态缺表时启动期 fail-closed，避免 Redis 消息被反复 nack | P1.5-001、analytics-core、analytics-service、property catalog、ingestion runtime |
| 2026-05-08 | 将实施决策库 README 的完整修订流水拆到独立 `修订记录.md`，README 只保留文档索引、当前进度、实施计划和维护规则；同时补充 `guardrails` Q&A，说明它在 ClickHouse 读侧中的含义是 query limit、filter cap、allowlist 和统一 query builder 等查询护栏 | 文档治理、README、修订记录、P1.5-001、guardrails |
| 2026-05-08 | P1.5-001 继续推进属性字典治理：`analytics-core` 新增 storage-neutral `PropertyCatalogEntry` / `PropertyCatalog` 契约与 MySQL `property_catalog` adapter，先把观测到的 event/user property 选择器、类型和 first_seen / last_seen 治理模型立住，不引入 replay-sensitive 统计字段 | P1.5-001、analytics-core、property catalog、metadata governance |
| 2026-05-08 | P1.5-001 继续补 query plan evidence：`storage.EventQueryPlan` 增加结构化 `EventQueryEvidence`，记录 Events/Realtime 查询家族、事实表读路径、当前优化策略、属性表参与情况、过滤数量和排序证据，为后续是否引入 projection / MV / 小时聚合表提供可测试依据 | P1.5-001、analytics-core、EventQueryPlan、query evidence |
| 2026-05-08 | P1.5-001 开始实施第一步：`analytics-core` 将 ClickHouse 读侧的 query limit、filter cap 和 property allowlist 收口到 adapter-owned `readSidePolicy`，保持 `EventQueryBuilder` / `EventReader` 外部契约不变，并通过子代理代码审查与 Go 测试 | P1.5-001、analytics-core、ClickHouse 读侧规范、readSidePolicy |
| 2026-05-08 | 将 ClickHouse 读侧长期方案提升为实施规范：固化唯一读侧入口、分层推进顺序、禁止事项、projection / MV / 小时聚合表引入门槛和验收规则，并同步写入 `AGENTS.md` 防止后续偏离方向 | P1.5-001、analytics-core、ClickHouse 读侧规范、AGENTS.md |
| 2026-05-08 | 细化 P1.5-001 ClickHouse 读侧优化取舍：明确 projection、materialized view、小时聚合表和属性治理的适用边界，推荐长期分层路线为“先做属性治理和 query plan 约束，再按稳定查询引入 MV / 小时聚合表 / projection” | P1.5-001、analytics-core、ClickHouse 读侧优化 |
| 2026-05-07 | P1-005D 与 P1-005 收口为已完成：Realtime / Events 内部读接口、query routes、query token 轮换、property filter 白名单、visit_id 透传、SaaS readback helper、Website selector、repeatable `property_filter` 和组合查询回归均已验证；更复杂的聚合分析与 Breakdown/Compare 维持到 P1.5/P2 | P1-005、P1-005D、analytics-service、simpletrack-saas、Events、Realtime |
| 2026-05-07 | P1-005B 与 P1-005C 收口为已完成：collect runtime service 已具备 Fiber `/collect`、write key/source enforcement、Redis durable enqueue、可选同进程 ingestion worker、启动 schema surface 校验和本地/小部署 routed table auto-create；SaaS control-plane runtime enforcement 已具备 HTTP resolver、ETag 条件重验证、disable/delete 后撤权、runtime-source API、Websites CRUD/enable/disable/delete、active source quota 和 runtime source 字段回写 | P1-005B、P1-005C、analytics-service、simpletrack-saas、runtime source、control plane |
| 2026-05-07 | P1-002 与 P1-002C 收口为已完成：session/visit resolver 已覆盖缺失时服务端派生、显式 SDK `visit_id` 保留、ClickHouse event / `_properties` 持久化、Realtime / Events 读侧返回、SaaS runtime-source `visit_salt` / `visit_window_seconds` 输出和 Events URL/readback 查询态；salt 轮换、cookie/no-cookie、server identity、Sessions 专页和 retention 产品化继续放 P1.5/P2 | P1-002、P1-002C、analytics-core、analytics-service、simpletrack-saas、visit_id |
| 2026-05-07 | P1-002B 收口为已完成：`simpletrack-saas` 已验证 Websites 表单会把 additional origins、Bot user agents、Internal CIDRs、Internal IPs 和 fingerprint 开关完整回写 runtime source；`analytics-service` 已验证 internal IP 过滤审计不记录 raw IP；过滤统计与可视化不阻塞 P1，降到 P1.5/P2 评审 | P1-002B、P1-005B、P1-005C、simpletrack-saas、analytics-service、internal traffic |
| 2026-05-07 | `analytics-core` 补齐 Events 组合查询计划回归：同一个 ClickHouse query plan 同时覆盖时间窗、`event_name`、`distinct_id`、`visit_id`、分页、`event_name` 排序、event/user property filters，并验证业务值继续通过 bound args 参数化；子仓提交 `7c29667` 已推送 | P1-005D、analytics-core、ClickHouse、EventQueryBuilder |
| 2026-05-07 | P1-005D 补齐 Events 组合查询回归：`simpletrack-anaysitics-service` 验证 `event_name`、`distinct_id`、`visit_id`、分页、排序和 repeatable `property_filter` 会进入同一个 `EventListQuery`；`simpletrack-saas` 验证同一组条件会序列化为单个 readback 请求；子仓提交 `09656b6`、`bce3335` 已推送 | P1-005D、analytics-service、simpletrack-saas、Events、query combination |
| 2026-05-07 | `simpletrack-anaysitics-service` 的 Events readback API 文档与当前实现对齐：OpenAPI 将 `property_filter` 标为 repeatable array query parameter，并补齐 `sort_field=event_name`；README 示例展示 `event_name` + 多个 `property_filter` 的组合查询；子仓提交 `3672fb9` 已推送 | P1-005D、analytics-service、OpenAPI、Events、property filter |
| 2026-05-07 | `simpletrack-anaysitics-service` README 的本地 runtime source 示例补齐 `visit_salt` 和 `visit_window_seconds`，并明确 `session_salt` / `visit_salt` / `client_hash_salt` 都是 server-only runtime secrets；子仓提交 `958b26c` 已推送 | P1-005B、P1-002C、analytics-service、deployment params、visit_id |
| 2026-05-07 | `simpletrack-saas` 将 P1 quota 从单个 source limit helper 收口为明确的 `SimpleTrackPlanLimits` 策略：当前只限制 active website source，Free=1、Pro/Lifetime=5、Enterprise=不限，事件量、留存期、成员数和用量计费不提前进入 P1；子仓提交 `41f8d4d` 已推送 | P1-005C、simpletrack-saas、Websites、quota、plan limits |
| 2026-05-07 | `simpletrack-saas` 的 Websites 设置区补齐可见标签：Source name、Primary origin、Additional origins、Bot user agents、Internal CIDRs、Internal IPs 都从仅靠 placeholder / aria-label 改为明确配置项，便于 P1 internal traffic 配置在控制面被理解和维护 | P1-002B、P1-005C、simpletrack-saas、Websites、internal traffic、runtime source |
| 2026-05-07 | `analytics-service` 补 internal traffic 过滤审计回归：通过可信代理头触发 internal IP filter，确认日志只记录 event/source 边界和 `reason=internal ip`，不记录原始客户端 IP | P1-002B、P1-005B、analytics-service、internal traffic、audit、privacy |
| 2026-05-07 | 用当前模型复审 repeatable `property_filter` 链路后，补齐 `simpletrack-saas` 前端与 URL normalization 的共享 5 条上限：组件禁用第 6 条新增，query-state 也会截断超额直达 URL，避免页面发出会被 `analytics-service` 拒绝的查询 | P1-005D、simpletrack-saas、Events、property filter、query-state |
| 2026-05-07 | `analytics-service` 的 Events query boundary 补 repeatable `property_filter` 回归：`/v1/events` 现在有服务层测试确认多个 property_filter 参数会按顺序进入 `analytics-core` 查询计划，和 `simpletrack-saas` 的多条件 Events 页面保持同一契约 | P1-005D、analytics-service、query parser、property filter |
| 2026-05-07 | `simpletrack-saas` 的 Events 页从单条 property filter 升级为 repeatable `property_filter` 多条件查询：page/query-state/readback 统一切到数组模型，支持新增/删除过滤行并输出重复 query 参数，同时保留 legacy 单字段回退兼容 | P1-005D、simpletrack-saas、Events、query builder |
| 2026-05-07 | `analytics-core` 的 P1-002B enrich 边界继续收口：新增 browser / OS / device 的可插拔 UA 派生，以及基于离线 MaxMind mmdb 的 geo enrichment 接口；`simpletrack-anaysitics-service` 可通过 `ANALYTICS_SERVICE_GEOIP_MMDB_FILE` 提供地理库文件，collect 侧继续保持框架无关 | P1-002B、collect、analytics-core、analytics-service、geo、browser、device |
| 2026-05-07 | 明确 SimpleTrack 仍处于新建项目阶段，`src/analytics-core`、`src/simpletrack-saas` 和 `src/analytics-service` 的 schema 调整只走初始化 / 建表 / 启动校验路径，不提前引入迁移 SQL、backfill、兼容分支或完整迁移框架；生产级迁移逻辑留到真实上线后单独评估 | 仓库治理、schema、P1-005B、P1-005C |
| 2026-05-07 | `visit_id` 从 readback provisional 字段升级为 canonical analytics visit key：`analytics-core` 增加 VisitResolver、存储字段、writer/reader/query 支持；`simpletrack-anaysitics-service` runtime config 增加 `visit_salt` / `visit_window_seconds`；`simpletrack-saas` runtime-source 输出 visit 配置并让 Realtime / Events 使用真实存储字段 | P1-002C、P1-005C、P1-005D、analytics-core、analytics-service、simpletrack-saas |
| 2026-05-07 | `simpletrack-anaysitics-service` 补充 runtime source resolver 与 HTTP resolver cache 边界注释，作为 `/tracker.js`、`/v1/realtime`、`/v1/events` 源码解读的稳定证据基线；子仓提交 `825e366` 已推送，父仓随后同步 gitlink 和 `docs/analytics-source-reading/collectapi-query-and-tracker-flow.md` 引用 | P1-005B、P1-005C、P1-005D、analytics-service、源码解读、submodule |
| 2026-05-07 | `analytics-core` 的 `collect/httpapi` 和 `simpletrack-anaysitics-service` HTTP runtime 已从 fasthttp 迁移到 Fiber v3：core 保持 `collect.Handler` 框架无关，service 改为 `collectapi.NewApp` 返回 Fiber app，内部 Events / Realtime 路由支持配置且默认保留 `/v1/events`、`/v1/realtime`，CORS 改用 Fiber middleware，并新增 Swagger UI + OpenAPI 文件；子仓提交 `58668c9`、`1e8f283` 已推送，父仓随后同步 gitlink | P1-002、P1-005B、P1-005D、analytics-core、analytics-service、Fiber、Swagger、submodule |
| 2026-05-05 | `simpletrack-saas` 的 Events 页补时间窗口预设：在现有事件名 / distinct id / 单条 property filter / 排序 / 分页基础上，再补 `30m / 6h / 24h / 7d` 时间窗口查询态；窗口会进入服务端 href、分页链接与 readback 请求，不再固定锁死最近 24 小时，子仓提交 `fa822d1` 已推送，父仓随后同步 gitlink | P1-005D、simpletrack-saas、Events、time-window、submodule |
| 2026-05-05 | `simpletrack-anaysitics-service` 补齐 control-plane runtime revoke 回归：新增 HTTP resolver revalidation 驱动的 handler 级测试，验证已缓存 source 在控制面 disable 或 delete 后，会立即拒绝新的 `/collect` 与内部 `/v1/realtime` 访问，不必等待服务重启；子仓提交 `8ae0b78` 已推送，父仓随后同步 gitlink | P1-005C、P1-005D、analytics-service、runtime-revocation、submodule |
| 2026-05-05 | `simpletrack-saas` 的 `Websites` 控制面补齐最小 CRUD 与剩余 runtime source 设置收口：在 create + enable / disable + 编辑基础上，再补 delete 动作和 `include client fingerprint` 编辑通路；`allowedOrigins`、`bot user agents`、`internal CIDRs`、`internal IPs` 和 fingerprint 开关都可回写 Website runtime source config，子仓提交 `5574dd2` 已推送，父仓随后同步 gitlink | P1-005C、simpletrack-saas、Websites、runtime-source、submodule |
| 2026-05-05 | `simpletrack-saas` 的 `Websites` 控制面继续前进到更完整的 runtime source 维护：在 create + enable / disable + origin 编辑基础上，再补 `bot user agents`、`internal CIDRs`、`internal IPs` 的编辑入口；这些配置会直接落回 Website runtime source config，子仓提交 `f0be432` 已推送，父仓随后同步 gitlink | P1-005C、simpletrack-saas、Websites、runtime-source、submodule |
| 2026-05-05 | `simpletrack-saas` 的 `Websites` 控制面继续前进到最小可维护状态：在 create + enable / disable 基础上，再补 name / primary origin / additional origins 的编辑入口；编辑路径会保留 secondary `allowedOrigins`，不会把多 origin runtime config 压回单 origin，子仓提交 `44b7a35` 已推送，父仓随后同步 gitlink | P1-005C、simpletrack-saas、Websites、runtime-source、submodule |
| 2026-05-05 | `simpletrack-saas` 的 `Websites` 控制面继续前进到最小可维护状态：在真实 source 管理与 enable / disable 基础上，再补 name/domain 编辑入口；编辑路径会保留 secondary `allowedOrigins`，不会因保存表单把多 origin runtime config 压成单 origin，子仓提交 `b049751` 已推送，父仓随后同步 gitlink | P1-005C、simpletrack-saas、Websites、runtime-source、submodule |
| 2026-05-05 | `simpletrack-saas` 的 Website source limit 实现与文档口径对齐：disabled source 创建不再占用 active source limit，并补 `createWebsiteWithinLimit()` 直达回归；子仓提交 `74248a6` 已推送，父仓随后同步 gitlink | P1-005C、simpletrack-saas、Websites、quota、submodule |
| 2026-05-05 | `simpletrack-saas` 的 `Websites` 页继续前进到真实 runtime source 管理：在真实组织 Website 数据和最小创建 source 表单基础上，再补 enable / disable 动作，readback 按钮与 enabled 状态保持一致；创建与启用路径都通过 serializable 事务 helper + `P2034` 重试守住 active source limit，子仓提交 `dcc1db8` 已推送，父仓随后同步 gitlink | P1-005C、P1-005D、simpletrack-saas、Websites、runtime-source、submodule |
| 2026-05-05 | `simpletrack-saas` 的 `Websites` 页已从 mock 列表切到真实组织 Website 数据，并补最小创建 source 表单、write key 展示、Quickstart / Realtime / Events 入口；创建路径通过 serializable 事务 helper + `P2034` 重试守住 active source limit，子仓提交 `5d165c6` 已推送，父仓随后同步 gitlink | P1-005C、P1-005D、simpletrack-saas、Websites、runtime-source、submodule |
| 2026-05-04 | `simpletrack-saas` 的 Realtime / Events 页面开始直接展示 provisional `visit_id`，把已确认可用的 readback 字段前移到产品界面；`analytics-readback-core` 同步把 visit 维度纳入 view model，并补页面级回归；子仓提交 `236d179` 已推送，父仓随后同步 gitlink | P1-005D、simpletrack-saas、visit_id、readback UI、submodule |
| 2026-05-04 | `analytics-core` 的 readback `visit_id` 改为独立 128-bit digest，并补 30 分钟 UTC bucket 边界 / 归一化测试；`simpletrack-anaysitics-service` 的 Realtime / Events 继续透传该字段，两个子仓提交已推送，父仓随后同步 gitlink | P1-002C、P1-005D、visit_id、readback、submodule |
| 2026-05-04 | `src/analytics-service` 已接入远端 `simpletrack/anaysitics-service` 仓库：本地 `main` 已推送，父仓开始登记 `src/analytics-service` 子模块；同步把 `AGENTS.md` 的子仓提交顺序和 SSH 规则扩展到第三个子仓库 | 仓库治理、P1-005、analytics-service、submodule、SSH |
| 2026-05-04 | `simpletrack-anaysitics-service` 的内部 Realtime / Events 读回放补 query token 生命周期与审计基础：`ANALYTICS_SERVICE_QUERY_TOKENS_JSON` 现在支持带 `id`、`not_before`、`expires_at` 的结构化凭据；运行时会拒绝未生效或已过期 token，并对命中轮换 token、过期 token、未来 token 和未知 token 记录审计日志 | P1-005D、simpletrack-anaysitics-service、query token lifecycle、audit、Realtime、Events |
| 2026-05-04 | `simpletrack-anaysitics-service` 的内部 Events 读回放补属性过滤入口：runtime source config 新增 `allowed_property_filters`，`/v1/events` 接收 URL 编码 JSON `property_filter`，并在服务层和 `analytics-core` ClickHouse query builder 双重白名单校验后才执行查询 | P1-005D、simpletrack-anaysitics-service、property filters、query whitelist、analytics-core |
| 2026-05-04 | `simpletrack-anaysitics-service` 的内部 Realtime / Events 读回放补 query token 轮换支持：保留 `ANALYTICS_SERVICE_QUERY_TOKEN` 单 token 兼容，同时新增 `ANALYTICS_SERVICE_QUERY_TOKENS_JSON` 短窗口 allowlist，可在 SaaS 切换内部读 token 时接受当前 token 和上一版 token | P1-005D、simpletrack-anaysitics-service、query token rotation、Realtime、Events |
| 2026-05-03 | `simpletrack-saas` 的 Website selector 改为 client-safe DTO：页面只把 `id/name/domain/enabled` 传给浏览器，完整 Website row 仍留在服务端 readback 路径；`website` query 参数切换组织内 enabled source，禁用 source 仍保持 fail-soft | P1-005D、simpletrack-saas、Website selector、server/client boundary |
| 2026-05-03 | `simpletrack-saas` 的 Realtime / Events 页面补真实 Website 选择器初版：页面读取组织下 Website 列表，通过 `website` query 参数切换 enabled source；服务端 readback helper 只接受组织内启用 source，禁用或无 source 仍保持 fail-soft | P1-005D、simpletrack-saas、Website selector、Realtime、Events |
| 2026-05-03 | `simpletrack-saas` 的 Realtime / Events 页面接入 `simpletrack-anaysitics-service` 内部读回放初版：页面服务端按组织查启用 Website，使用内部 query token 调 `/v1/realtime` 与 `/v1/events`，未配置服务、无启用 source 或服务异常时保持 fail-soft 空态 | P1-005D、simpletrack-saas、simpletrack-anaysitics-service、Realtime、Events |
| 2026-05-03 | `simpletrack-saas` 的 Events 页面补齐服务端筛选与分页：`event_name`、`distinct_id`、`sort_field`、`sort_direction`、`limit`、`offset` 均走白名单归一化，`hasMore` 驱动上一页 / 下一页，内部 query token 仍不下发到浏览器 | P1-005D、simpletrack-saas、Events、query whitelist、pagination |
| 2026-05-03 | `simpletrack-saas` 的 Events 页面再补重复 query 参数与空页分页硬化：重复参数取首值后归一化，空页但 `offset > 0` 时仍显示 Previous / Next 控件，避免书签或重复参数把服务器渲染页打崩 | P1-005D、simpletrack-saas、Events、query normalization、pagination hardening |
| 2026-05-03 | `simpletrack-anaysitics-service` 的内部 Events / Realtime 读回放开始落地：新增 `/v1/realtime` 与 `/v1/events`，通过内部 bearer token 保护并复用 `analytics-core` 的 `EventReader`，同时补齐浏览器/SaaS 页面调用需要的 query `OPTIONS` preflight | P1-005D、simpletrack-anaysitics-service、Events、Realtime、query API、CORS |
| 2026-05-03 | `simpletrack-anaysitics-service` 增加 `ANALYTICS_SERVICE_SOURCE_RESOLVER=http` 控制面 HTTP resolver，可用 bearer token 向 SaaS 控制面读取 runtime source config，并用短 TTL 缓存 + `ETag` 条件重验证降低热路径控制面压力且避免 disabled/source salt/origin 变更被陈旧缓存掩盖；控制面 URL 默认必须是 HTTPS，本地 loopback HTTP 需要显式开启 `ANALYTICS_SERVICE_CONTROL_PLANE_ALLOW_INSECURE_LOOPBACK=true`；同进程 ingestion 仍要求 `ANALYTICS_SERVICE_SOURCES_JSON` 作为启动 schema surface，并拒绝 HTTP resolver 返回未在启动阶段校验过的 source | P1-005C、simpletrack-anaysitics-service、SaaS control-plane runtime enforcement |
| 2026-05-03 | `simpletrack-anaysitics-service` 为显式开启的 ingestion worker 增加可选 ClickHouse routed table auto migration；默认仍先校验 schema 并 fail-closed，本地/小部署可用 `ANALYTICS_SERVICE_CLICKHOUSE_AUTO_MIGRATE=true` 创建当前 runtime config 内所有启用 source 所需的 event / `_properties` 表 | P1-005B、simpletrack-anaysitics-service、ClickHouse schema、运行时装配 |
| 2026-05-03 | 按服务命名要求把运行时服务展示名统一为 `simpletrack-anaysitics-service`；继续推进 P1-005B，服务可显式开启同进程 ingestion worker，装配 Redis Stream、MySQL checkpoint guard、ClickHouse native writer 和 typed property indexing；同时强制 session/client salt 由服务端 runtime config 提供，并在 ingestion 启动时校验 ClickHouse 表就绪 | P1-005、P1-005B、simpletrack-anaysitics-service、运行时装配、隐私 salt、ClickHouse schema |
| 2026-05-03 | `simpletrack-saas` 落地内部 runtime-source API，并补齐 Website 运行时配置模型和 write key -> runtime source config 的读取路径；`simpletrack-anaysitics-service` 现在可按 write key 从控制面读取 server-only salts、allowlist 和 traffic 配置 | P1-005C、simpletrack-saas、simpletrack-anaysitics-service、SaaS control-plane runtime enforcement、Website 模型 |
| 2026-05-03 | 建立 `src/analytics-service` 本地仓库，明确 `analytics-core` 作为 Go 第三方库而非独立业务服务；core 公共 API 调整为根目录包，Browser SDK 从 core 移到 `simpletrack-anaysitics-service` 静态交付，docs/quickstart 改为 write key 接入 | P1-005、analytics-service、analytics-core、Web tracker SDK、控制面/数据面边界 |
| 2026-05-03 | 在根目录 `AGENTS.md` 固化代码结对规则：代码改动必须由主代理实现、Codex native 子代理基于 `AGENTS.md` 复审当前 diff，提交前处理阻塞问题；纯文档更新默认不强制子代理 | 仓库治理、代码审查、协作规范 |
| 2026-05-03 | 在 `analytics-core` 把 typed property rows 接入 ingestion 热路径：新增 `PropertyIndexingEventWriter`、`PropertyWriteGuard`、MySQL `property_indexing_status`，属性写入只 reclaim 明确 failed checkpoint，processing 视为结果不明以避免重复追加；同步子仓提交到 `0586ee6` | analytics-core、P1-002A、ClickHouse、属性索引、跨表幂等、代码审查规则 |
| 2026-05-02 | 在 `analytics-core` 为 Events 查询新增 typed property filter：属性 scope/name/type/value 进入 allowlist + 绑定参数，ClickHouse 使用 tuple `IN` 半连接查询属性表；真实 e2e 复验修复 correlated `EXISTS` 外层 alias 卡点；同步子仓提交到 `ae5c21c` | analytics-core、P1-002D、P1-002A、Events 查询、ClickHouse 查询安全 |
| 2026-05-02 | 在根目录 `AGENTS.md` 固化提交规范：按 `$git-commit-cn` 的范围核对、分路径说明、提交后推送流程执行，但提交信息统一使用英文并保留必要 Lore trailer | 仓库治理、提交规范、协作规范 |
| 2026-05-02 | 修复 `analytics-core` e2e 冷启动依赖 readiness 问题，为 Redis/MySQL/ClickHouse 连接增加重试窗口，并新增真实 ClickHouse `PropertyBatchWriter` e2e 写入读取验证；同步子仓提交到 `2cc83e1`，排查记录写入 `docs/开发环境卡壳问题记录.md` | analytics-core、P1-002A、P1-002E、本地运行依赖、ClickHouse |
| 2026-05-02 | 在 `analytics-core` 新增 `EventPropertyWriter` 契约和 ClickHouse `PropertyBatchWriter`，可把 typed property rows 写入同源路由的 `_properties` 物理表；暂不并入事件写入热路径，等待跨表重试/幂等语义评审；同步子仓提交到 `5698f11` | analytics-core、P1-002A、ClickHouse、事件属性、用户属性、属性物理写入 |
| 2026-05-01 | 在 `analytics-core` 新增 storage-neutral typed property rows 和 `FlattenEventProperties`，把 event/user properties 从原始 map 稳定展开为可供 `EventWriter`、属性字典和属性过滤复用的逻辑记录；同步子仓提交到 `f64ed3c` | analytics-core、P1-002A、事件属性、用户属性、属性展开 |
| 2026-05-01 | 在 `analytics-core` 为 collect 阶段新增事件属性/用户属性入口约束：属性 key 形状、数量、标量类型、字符串长度和有限数字校验；同步子仓提交到 `9224961` | analytics-core、P1-002A、collect、事件属性、用户属性 |
| 2026-05-01 | 在 `analytics-core` 为 Events 查询继续补齐类型化过滤字段/operator 白名单、filter 数量上限和 `ErrInvalidEventQuery` 错误分类；同步子仓提交到 `cd9e48f` | analytics-core、P1-002D、Events 查询、ClickHouse 查询安全 |
| 2026-05-01 | 在 `analytics-core` 为 Events 查询新增类型化排序字段和方向白名单，避免后续 UI 排序参数直接穿透 SQL；同步子仓提交到 `3afaf4c` | analytics-core、P1-002D、Events 查询、ClickHouse 查询安全 |
| 2026-05-01 | 安装并补跑 `golint ./...`，修正 `analytics-core` e2e helper 的 `context.Context` 参数顺序；同步子仓提交到 `0538a0b` | analytics-core、P1-002E、本地验证、代码规范 |
| 2026-05-01 | 在 `analytics-core` 新增 opt-in 端到端测试，真实验证 collect -> Redis Stream -> ingestion -> ClickHouse -> Realtime/Events reader，并将 P1-002E 标记为已完成；同步子仓提交到 `4931f15` | analytics-core、P1-002、Realtime、Events、本地运行依赖 |
| 2026-05-01 | 将 Umami 源码深解中可被 `analytics-core` 吸收的优化点排入实施计划和评审表，新增事件属性、client enrich、session/visit、查询白名单、Realtime/Events 验收、Web SDK 和 ClickHouse 读侧优化任务 | analytics-core、Umami 参考资产、P1/P1.5/P2 计划、待评审事项 |
| 2026-05-01 | 为 Umami P1 源码实现参考补充 `Q&A/` 概念解释，覆盖 identify、Prisma schema、字段白名单、Core Web Vitals、SDK 取舍、bot/IP 过滤、Zod、storage dispatch、session 隐私机制和 JSON 属性风险 | Umami 参考资产、analytics-core、SimpleTrack docs/quickstart、文档体系 |
| 2026-05-01 | 在 `simpletrack/docs/umami/docs/源码实现参考/` 落地 Umami P1 数据管道源码分章节深解，补齐整体架构、tracker、collect、写入、模型、Realtime/Events、过滤查询和 SimpleTrack / `analytics-core` 启发 | Umami 参考资产、analytics-core、Realtime、Events、文档体系 |
| 2026-05-01 | 将 Umami 官方 GitHub 源码克隆为 `references/umami/` 只读参考快照，新增源码实现审阅和参考映射文档 | Umami 参考资产、analytics-core、Realtime、Events、仓库治理 |
| 2026-05-01 | 调整 `analytics-core` 本地 compose 默认端口，避开本机 Redis/MySQL 端口冲突，并记录 Docker 卡壳处理；同步子仓提交到 `c7aa2cb` | analytics-core、本地运行依赖、开发环境 |
| 2026-05-01 | 在 `analytics-core` 新增 Redis Stack、MySQL、ClickHouse 本地 `docker-compose.yml` 和 README 运行说明；同步子仓提交到 `0bd1cc4` | analytics-core、本地运行依赖、端到端验证 |
| 2026-05-01 | 在 `analytics-core` 落地 `storage.EventReader` 和 ClickHouse/GORM 查询执行器，让 Realtime/Events query plan 能扫成 `EventRecord`；同步子仓提交到 `a072275` | analytics-core、Realtime、Events、ClickHouse 查询 |
| 2026-05-01 | 明确 `analytics-core` 的 `ingestion.Processor` 是 P1 worker 边界，补充 Run 级测试和 Example；同步子仓提交到 `a22ab6e` | analytics-core、ingestion worker、队列消费 |
| 2026-05-01 | 在 `analytics-core` 落地 `EventQueryBuilder` 查询契约和 ClickHouse/GORM Events、Realtime query plan 边界；同步子仓提交到 `7ab7b12` | analytics-core、Realtime、Events、ClickHouse 查询 |
| 2026-05-01 | 在 `analytics-core` 落地 GORM/MySQL `IngestionStatusGuard` 与 `ingestion_status` 幂等状态表，并强化函数体阶段注释强制规范；同步子仓提交到 `71f5ae3` | analytics-core、MySQL/GORM、代码规范 |
| 2026-05-01 | 澄清 `collect.Handler` 是事件上报核心处理器而非 HTTP 路由函数，并将“污染”表述改为“框架耦合 / 边界穿透”；同步 `analytics-core` 子仓到 `0c6bf8c` | analytics-core、HTTP collect API、协作规范 |
| 2026-05-01 | 在 `analytics-core` 落地 ClickHouse native batch `EventWriter`、`EventWriteGuard` 幂等边界和 Go 结构体/接口注释强制规范 | analytics-core、ClickHouse、代码规范 |
| 2026-04-30 | 评估 xwl_bi 的 Fiber 与 fasthttp/fasthttprouter HTTP 栈；后续已改为 `analytics-core` 的 collect HTTP API 使用 Fiber v3，并保持 `collect.Handler` 框架无关 | analytics-core、HTTP collect API、协作规范 |
| 2026-04-29 | 创建实施决策库，写入总方向、待评审方向和维护规则 | 决策管理 |
| 2026-04-29 | 增加修订记录、实施计划完成列表、当前进度和下一步动作 | 决策管理、阶段推进 |
| 2026-04-29 | 明确 xwl_bi 不整仓改名，改为抽取分析数据面核心仓库的待评审方向 | 数据面架构 |
| 2026-04-29 | 确认 P1 新建 `analytics-core` 独立核心仓库，前期 Redis Stream 先行，KafkaBus 保留 | 分析数据面、P1 底座 |
| 2026-04-29 | 将 P1 范围补充为包含产品官网 / Marketing Site / 公开站点 | P1 产品交付 |
| 2026-04-29 | 增加 `template-src` 本地付费 SaaS 模板对比，收敛为 Supastarter + MakerKit 两个核心候选 | SaaS 模板选型 |
| 2026-04-29 | 确认生产 SaaS 模板先选择 Supastarter for Next.js，MakerKit 降为 B2B 对照和备选 | SaaS 模板选型 |
| 2026-04-29 | 新增 `analytics-core` 实施方案，纳入 xwl_bi analyze/code-review 证据，并补充 Umami、Litlyx 参考边界 | 分析数据面、P1 实施 |
| 2026-04-29 | 支付路线改为先按 Supastarter 支持的 provider 接入，KYC/KYB、发票税务、退款拒付等放到上线前后置检查 | 支付与商业化 |
| 2026-04-30 | 补齐 analytics-core 评审 Q&A：GitHub 组织、tenant/project/source、ack/重试/死信、consumer offset、acceptance status、GORM query builder 和 UI 策略 | 分析数据面、协作规范、Q&A |
| 2026-04-30 | 确认 ClickHouse 表策略直接采用方案 B，事件写入热路径使用原生 batch writer，入库必须按 event_id 幂等去重 | analytics-core、ClickHouse、数据入库 |
| 2026-04-30 | 本地创建 `src/analytics-core` 独立仓库骨架，并从 Supastarter 初始化 `src/simpletrack-saas` 工作副本；远端推送受 GitHub 权限或仓库创建状态阻塞 | P1 底座、SaaS 工作副本 |
| 2026-04-30 | 在 `src/simpletrack-saas` 完成 Supastarter P1 页面草案 spike：挂载 Websites、Realtime、Events 到组织内导航并通过 saas type-check | Supastarter spike、P1 产品层 |
| 2026-04-30 | 为 `analytics-core` 增加 Redis Stream 集成测试，并用 `redis/redis-stack:latest` 验证 publish / consume / ack / pending=0 | analytics-core、EventBus、Redis Stream |
| 2026-04-30 | 修复 SimpleTrack 专用 SSH 身份，成功推送 `analytics-core` 与 `simpletrack-saas` 远端，并将二者作为父仓子模块挂载 | 仓库治理、P1 底座 |
| 2026-04-30 | 固化 Windows SSH 仓库权限配置、专用 key 初始化流程和 `core.sshCommand` 规则，并补充 SimpleTrack 目录 README | 仓库治理、协作规范、文档入口 |
| 2026-04-30 | 在 Supastarter Websites 草案页接入 UI-only subscription gate，验证 Free plan source 限制、锁定态和升级入口 | Supastarter spike、订阅限制、P1 产品层 |
| 2026-04-30 | 将 Supastarter marketing/docs 占位内容替换为 SimpleTrack 产品介绍、定价语义和 docs/quickstart，并记录 Windows 验证环境要求 | P1 公开产品入口、Supastarter spike、协作规范 |
| 2026-04-30 | 完成 Supastarter marketing/docs/mail-preview 浏览器截图验证，替换可见模板占位，并将本地 PostgreSQL Docker 配置对齐到 SimpleTrack | P1 公开产品入口、邮件预览、SaaS 控制面数据库 |
| 2026-04-30 | 在 `analytics-core` 落地 Redis Stream pending 优先重试、MaxAttempts 死信队列和 ingestion 幂等处理边界，并通过 `go test ./...` | analytics-core、EventBus、ingestion |
| 2026-04-30 | 将本地 `xwl_bi` 后端源码与关键文档复制为 `references/xwl_bi-backend/` 只读临时参考快照，供 `analytics-core` 实现对照使用 | 分析数据面、参考资产、仓库治理 |
| 2026-04-30 | 在 `analytics-core` 落地 collect 请求标准化、`EventWriter` 写入接口和 ClickHouse `TableRouter`，启动 P1-002 数据管道最小闭环实现 | analytics-core、collect、storage、ClickHouse |
| 2026-04-30 | 新增 xwl_bi 后端架构参考映射，明确快照主要参考模块边界、启动装配、消费链路、ClickHouse 写入/查询分层和分析服务拆分 | analytics-core、xwl_bi 参考、后端架构 |
| 2026-04-30 | 在 `analytics-core` 落地 `collect.Handler`，完成 collect 请求标准化到 EventBus 发布的最小链路 | analytics-core、collect、EventBus |

## 从 README 文档索引移出的早期修订条目

| 日期 | 修订内容 | 影响范围 |
| --- | --- | --- |
| 2026-05-08 | `analytics-core` 500k 行 ClickHouse reader benchmark 已纠正 Realtime 场景口径：短窗口 `low_realtime_recent_window` 与宽时间窗 `low_realtime_wide_since` 分开记录；当前结论是不新增 ClickHouse projection、materialized view 或小时聚合表，继续观察宽时间窗 Events 和 typed property 过滤读路径；同时在 `AGENTS.md` 明确代码提交前必须显式执行 `$ai-slop-cleaner` bounded cleanup/review 流程 | P1.5-001、analytics-core、ClickHouse 读侧、协作规范 |
| 2026-05-07 | `visit_id` 长期方案定稿：不再依赖 readback 临时派生，改为 collect 写入前确定、ClickHouse event / `_properties` 表持久化、Realtime / Events 直接读取存储字段；同步新增 `session_id / visit_id / distinct_id` Q&A | P1-002C、P1-005C、P1-005D、visit_id |
| 2026-05-06 | `simpletrack/prototype/simpletrack-enterprise-mvp/` 的页面数据契约已重新收口到当前原型页：`/onboarding`、`/dashboard`、`/events`、`/goals`、`/settings`，`team` / `funnels` / `insights` 退回 P2/P3 候选，不再写进 P1 页面 contract | P0-001、页面 contract、enterprise-mvp、P1 收口 |
| 2026-05-04 | 历史过渡：`analytics-core` 的 readback 结果曾先行带上 provisional `visit_id`，用于让 Realtime / Events UI 提前验证展示价值；该口径已被 2026-05-07 的持久化 `visit_id` 方案替代 | P1-002C、P1-005D、visit_id、readback |
| 2026-05-03 | 浏览器 SDK 已补齐 DNT opt-in 隐私开关和 UTM/click id 白名单；随后完成边界纠偏：SDK 不再属于 `analytics-core`，改由 `simpletrack-anaysitics-service` 静态托管，quickstart 改为 `data-write-key` 接入 | P1-002B、P1-002C、P1-004、P1-005、DNT、UTM/click id、Web tracker SDK、docs/quickstart |
| 2026-05-03 | P1-004 Web tracker SDK 最短链路已完成，后续归属调整为产品运行时层：`analytics-core` 只保留 Go library 能力，`simpletrack-anaysitics-service` 承接 `/tracker.js` 和 `/collect` | P1-004、P1-005、Web tracker SDK、analytics-core、analytics-service |
| 2026-05-03 | 在 `analytics-core` 落地 P1-002B/C 第一版：新增 collect pre-queue `Stage` 管道、盐化窗口 `SessionResolver`、client UA/referrer/IP hash 派生属性、bot/internal traffic 过滤；HTTP 层默认不信任 `X-Forwarded-For` / `X-Real-IP`，只有显式 `WithTrustedProxyHeaders()` 才启用可信代理头；子仓提交 `9c06b0d` 已推送，并通过结对审查、全仓 Go 验证和真实 e2e | analytics-core、P1-002B、P1-002C、collect、隐私、bot/internal traffic |
