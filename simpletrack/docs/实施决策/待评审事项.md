# SimpleTrack 待评审事项

> 状态：持续更新  
> 最近更新：2026-05-04

## R1：Supastarter 1 天 SimpleTrack spike

状态：模板已确定，接入细节待评审

已确定：

- 生产 SaaS 模板先选择 Supastarter for Next.js。
- MakerKit 只保留为 B2B 企业控制面对照和备选。
- ShipAny、MkSaaS、ShipFast、Mkdirs 作为本地参考模板，不再作为第一梯队生产底座。

当前判断：

- `template-src/ai-supastarter-template` 已有本地源码，可核验 marketing、saas、docs、mail-preview、多支付、organizations、admin 等模块。
- 不直接修改 `template-src/ai-supastarter-template`；SimpleTrack 产品实现进入 `src/simpletrack-saas` 工作副本。
- `src/simpletrack-saas` 已从本地 Supastarter 模板初始化，并已挂载 Websites、Realtime、Events 三个组织内页面草案。
- `src/simpletrack-saas` 远端已推送到 `git@github.com:simpletrack/simpletrack-saas.git`。
- `pnpm --filter saas run type-check` 已通过；执行前需要用临时 `DATABASE_URL` 生成 Prisma client。
- 当前等待继续验证 subscription gate、邮件模板和 marketing/docs。
- Supastarter 同时承接产品官网、docs、SaaS 控制面、邮件预览和多支付 provider，更适合 P1 先落地。
- MakerKit 如果未来在组织/RBAC、Super Admin、测试、文档、企业控制台 UI 上明显领先，可以作为备选重开评审，但不阻塞当前路线。

评审问题：

- Supastarter 中新增 Website 页面入口是否顺畅：本地页面草案已验证，数据模型和 subscription gate 待继续。
- Realtime / Events 页面能否自然放进 `apps/saas` 的 dashboard shell：已完成本地挂载和 type-check，截图级 UI 评审待继续。
- subscription gate、组织隔离、Admin、邮件模板是否容易维护。
- `apps/marketing` 和 `apps/docs` 是否能承接产品介绍、定价/订阅入口和 docs/quickstart。
- 许可证是否允许 SimpleTrack 商业 SaaS、私有仓库、闭源修改和团队协作。

下一步：

- 在 `src/simpletrack-saas` 工作副本中继续 1 天 SimpleTrack spike 的剩余项：subscription gate、邮件模板、marketing/docs 预览。
- MakerKit 保留资料级跟踪，不作为当前阻塞项。

## R2：支付与 Merchant of Record 路线

状态：后置评审，上线前逐项处理

已确定：

- 先按 Supastarter 已支持的支付 provider 接入。
- 当前本地 Supastarter provider 包含 Stripe、Lemon Squeezy、Polar、Creem、Dodo Payments。
- Paddle 暂不作为 P1 必接，除非后续模板或商业计划明确需要。

当前倾向：

- P1 阶段优先顺着 Supastarter 的支付抽象跑通订阅入口和 subscription gate。
- KYC/KYB、退款、拒付、发票、税务处理、费用结构等不作为 P0/P1 早期阻塞，放到真实上线收费前逐项确认。
- Stripe 技术能力强，Lemon Squeezy / Polar / Dodo Payments / Creem 等更接近 MoR 或出海友好路线，最终以实际开通能力和主体条件为准。

上线前后置检查：

- 当前主体是否能通过 KYC/KYB。
- 是否支持订阅、退款、拒付、发票、税务处理。
- 是否能与候选 SaaS 模板顺畅集成。
- 费用结构是否适合早期产品。
- 支付 provider 是否支持目标销售地区和个人开发者主体。

说明：

- KYC / KYB 解释见 `simpletrack/docs/Q&A/KYC-KYB是什么.md`。
- 个人开发者发票和税务处理解释见 `simpletrack/docs/Q&A/个人开发者如何处理发票和税务.md`。

## R3：`analytics-core` 实施设计细节

状态：已确定要做，设计细节待评审，高优先级

当前判断：

- 已确定 P1 新建独立核心仓库 `analytics-core`。
- 本地独立仓库已挂载在 `src/analytics-core`，并已提交最小 Go module、事件契约、EventBus、DirectBus、RedisStreamBus 和 KafkaBus 包边界。
- 已用 `redis/redis-stack:latest` 真实容器验证 RedisStreamBus 的 publish / consume / ack，消费后 pending count 为 0。
- `src/analytics-core` 远端已推送到 `git@github.com:simpletrack/analytics-core.git`。
- `github.com/1340691923/xwl_bi` 是原作者上游仓库，不能通过整仓改名混淆上游来源、旧业务后台、SimpleTrack 新业务和核心能力边界。
- `analytics-core` 只关注分析数据面能力：事件分析、漏斗分析、留存分析、路径分析、LTV 分析、归因分析、分群、会话、元数据、实时数据、事件属性、用户属性。
- `analytics-core` 已明确为 Go 第三方库，外部服务通过根目录公共包引用；不作为 SimpleTrack 业务服务运行，也不托管 Browser SDK。
- `src/analytics-service` 已创建本地仓库，作为 `simpletrack-anaysitics-service` 运行时数据面服务；它读取控制面配置并执行 write key、Origin/CORS、internal traffic、bot 过滤和 collect 调用 core。
- `analytics-core` 支持 Kafka、MySQL、Redis、ClickHouse 技术栈。
- 前期使用 Redis Stream 替代 Kafka，但不删除 Kafka 代码，保留 KafkaBus 作为后续高吞吐实现。
- 代码层面需要剔除 `xwl_` 变量、函数、包和业务命名前缀，按更专业、业务无关的方向修改命名和结构。
- 旧的 xwl_bi Vue2 后台界面不考虑复用。
- 初版实施方案见 `simpletrack/docs/实施决策/analytics-core实施方案.md`。
- 参考产品边界：Umami 参考分析对象体系，Litlyx 参考接入教育、Raw Events 和 Show test data。
- Umami 源码结构深解已落地到 `simpletrack/docs/umami/docs/源码实现参考/`，其中对 `analytics-core` 有直接价值的优化项已排入计划表：事件属性模型、client info enrich、bot/IP 过滤、session/visit resolver、查询白名单、Realtime/Events 端到端验收、Web tracker SDK、ClickHouse 读侧优化和 performance metrics。

评审问题：

- `analytics-core` 的模块目录如何划分。
- 哪些 xwl_bi 代码保留、重命名、重构、删除或重写。
- ClickHouse、MySQL、Redis、Kafka 的依赖边界如何抽象。
- Redis Stream 和 KafkaBus 的接口如何保持一致。
- 核心仓库如何保持业务无关，不引入定价、团队、订阅、账单等 SimpleTrack 控制面概念。
- SimpleTrack 产品层如何把核心仓库能力映射到 `Workspace + Site`。
- 是否能保证站点级数据隔离和外部 SaaS 安全边界。
- `simpletrack-anaysitics-service` 如何读取 SaaS 控制面配置，同时避免重复实现站点、权限、套餐和订阅 CRUD。

### Umami 源码启发的新增评审项

| 编号 | 评审项 | 待拍板问题 | 当前倾向 | 影响阶段 |
| --- | --- | --- | --- | --- |
| R3-U1 | 事件属性与用户属性存储模型 | custom event 的 `properties` 和 identify 的 `user_properties` 是拆成 typed rows、使用 ClickHouse Map/JSON，还是采用原始 JSON + 高频属性展开的混合模型？ | P1 已确定采用“原始 JSON 保留 + typed rows 索引”的混合模型：collect 落地属性入口约束，storage 提供 typed row 展开，ClickHouse property writer 写 `_properties` 表，`PropertyIndexingEventWriter` 接入 ingestion 热路径，MySQL `property_indexing_status` 防重复；P1.5/P2 继续评审属性字典、ambiguous processing 恢复、projection/物化视图/去重策略 | P1-002A 已完成，P1.5-001 |
| R3-U2 | 属性字典和字段白名单范围 | 哪些属性允许进入过滤、排序、breakdown？属性名、类型冲突、大小写差异由谁治理？ | `collect.Normalize` 已限制属性 key 和标量值；`EventQueryBuilder` 已落地标准字段白名单和 typed property filter allowlist，页面层不能直接把字符串字段传进 SQL；P1 先通过代码 allowlist 保底，属性字典治理来源、展示策略、类型冲突处理放 P1.5/P2 | P1.5-001、P1-002D |
| R3-U3 | session/visit 隐私策略 | salt 如何轮换？IP 是否落库？是否支持 no-cookie 模式？Sessions 和 retention 什么时候产品化？ | P1 已落地可替换 resolver：缺失 `session_id` 时派生盐化匿名 session，缺失 `visit_id` 时在写入前派生 canonical visit key；`visit_id` 已进入事件契约、ClickHouse event / `_properties` 表、writer、reader 和 query builder；原始 IP/UA 不落库；`simpletrack-anaysitics-service` runtime config 已要求 server-only `session_salt` / `visit_salt` / `visit_window_seconds` / `client_hash_salt`，禁止从公开 write key 派生；浏览器 DNT opt-in 已避免 active DNT 下持久化本地身份 | P1-002C 进行中，salt 轮换和高级产品化放 P1.5/P2 |
| R3-U4 | client info enrich 边界 | IP、UA、browser、os、device、geo、utm、click id 在 collect 做、ingestion 做，还是由上层产品传入？ | P1 第一版已落地 collect 轻量 enrich：UA/referrer 受限进入 properties，IP 只存盐化 hash；浏览器 SDK 已自动收集 allowlisted UTM/click id；复杂 geo、browser/os/device 继续放评审，ClickHouse writer 不做解析 | P1-002B 进行中 |
| R3-U5 | bot/IP/internal traffic 过滤 | P1 是否只做配置级过滤，还是需要产品 UI？过滤失败是丢弃、标记，还是进入 dead-letter / audit？ | P1 第一版已落地 bot UA 与 internal CIDR/IP 过滤，过滤发生在 EventBus publish 前，HTTP 以 accepted filtered 响应隐藏噪声；浏览器 DNT opt-in 已落地；产品 UI、allow/deny 配置来源、internal traffic 与 audit 后续评审 | P1-002B 进行中 |
| R3-U6 | Realtime/Events 查询安全边界 | Realtime 固定窗口、Events 分页上限、字段白名单、排序白名单、operator enum 的最小规则是什么？ | 规则进入 `EventQueryBuilder` 类型与测试，不把安全约束放在页面层约定；属性过滤已改为 ClickHouse 可执行的 tuple `IN` 半连接并通过真实 e2e，避免依赖 dry-run 和 correlated alias | P1-002D、P1-002E |
| R3-U7 | ClickHouse 读侧优化节奏 | 是否在 P1 就做 materialized view、projection、小时聚合表和高频属性索引？如何同步到方案 B 的多物理表？ | P1 先完成明细写入和读侧验收；P1.5 基于真实查询、benchmark 和 explain 决策。当前 500k 复测已证明短窗口 Realtime 稳定，后续观察宽时间窗 Events 明细查询和 typed property 过滤读路径；没有稳定 query pattern、benchmark、explain 和回归计划时，不新增 MV/projection/aggregate | P1.5-001 |
| R3-U8 | Web SDK 与多语言 SDK 路线 | Browser SDK 属于 core library、`simpletrack-anaysitics-service` 静态资产，还是独立 SDK 仓库？React/Next/Node/mobile SDK 是否进入 P1？ | P1 已纠偏：`analytics-core` 不托管 SDK；Browser SDK 当前由 `simpletrack-anaysitics-service` 的 `/tracker.js` 静态交付，docs/quickstart 使用 `data-write-key`；React/Next/Node/mobile SDK、CDN 版本化、CSP 文档和 performance metrics 后续评审 | P1-004 已完成、P1-005、P2 |
| R3-U10 | `simpletrack-anaysitics-service` 与 SaaS 控制面配置边界 | write key、domain allowlist、internal traffic、quota、server-only privacy salts 等配置由谁创建，谁在运行时执行？ | 已确定 CRUD 在 `simpletrack-saas`，runtime enforcement 在 `simpletrack-anaysitics-service`；`simpletrack-saas` 内部 runtime-source API 已落地，HTTP resolver 只用 bearer token 按 write key 读取 runtime source config，并用短 TTL 缓存 + `ETag` 条件重验证降低控制面压力且避免 stale auth state，不创建/修改 SaaS 配置；控制面 URL 默认必须是 HTTPS，本地 loopback HTTP 需显式 opt-in；同进程 ingestion worker 只消费运行时事件，不拥有配置生命周期，且 HTTP 返回 source 必须落在启动 `ANALYTICS_SERVICE_SOURCES_JSON` schema surface 内；ClickHouse 表默认缺失时启动失败，本地/小部署可显式开启 `ANALYTICS_SERVICE_CLICKHOUSE_AUTO_MIGRATE=true` 创建当前 runtime config 内所有启用 source 的 routed tables；生产级迁移、回滚和批量 DDL 仍需单独评审 | P1-005C |
| R3-U11 | Events / Realtime 内部读回放接入 | `/v1/realtime` 与 `/v1/events` 已落地，SaaS 页面也已通过 server-side helper 和 client-safe Website selector 初版接入；当前 Events 已补白名单查询和分页，重复 query 参数与空页偏移也已硬化，query token 已支持当前 token + 上一版 token 的短窗口轮换 allowlist，并可附带 `id`、`not_before`、`expires_at` 生命周期元数据；运行时会拒绝过期/未来 token，并对轮换命中和拒绝场景打审计日志；属性过滤白名单来源已先放到 runtime source config 的 `allowed_property_filters`；后续筛选交互如何继续扩展仍需评审 | 目前由 `simpletrack-anaysitics-service` 内部 bearer token 保护并复用 `analytics-core` 的 `EventReader`；SaaS 页面按组织查启用 Website 后使用 write key 调内部服务，token 不下发浏览器；Website selector 通过 `website` query 参数切换组织内 enabled source，禁用 source 仍 fail-soft；Events 属性过滤通过服务层 source whitelist + `analytics-core` ClickHouse query builder whitelist 双重兜底；剩余关注点是更复杂的查询筛选交互，以及 token 生成/分发是否继续停留在部署环境还是后续交给控制面运维工具 | P1-005D |
| R3-U9 | Performance metrics 范围 | LCP、INP、CLS、FCP、TTFB 是 P1 必须，还是 P2 诊断增强？作为事件类型、属性组还是独立模型？ | P1 预留字段和属性承接能力，不阻塞 pageview/custom event；P2 再决定独立模型 | P2-001 |

下一步：

- R3-U1 的 P1 写入组合已完成：property writer 已并入 ingestion 热路径，跨表幂等语义采用 event status + property indexing status 两段 checkpoint；ambiguous `processing` 不自动恢复，后续按 P1.5/P2 运维策略评审。
- R3-U3 到 R3-U5 的 P1 第一版已落地：session 派生、持久化 visit_id、client 派生属性、bot/internal traffic 过滤、浏览器 DNT opt-in 和 allowlisted UTM/click id 均已实现；剩余 geo、internal traffic 产品配置、audit 策略和 salt 轮换继续评审。
- P1-002E 已完成最小端到端验证：collect -> Redis Stream -> ingestion -> ClickHouse event/property tables -> Realtime/Events reader -> property filter；冷启动依赖 readiness 已修复并记录排查过程，后续作为回归入口保留。
- P1-004 浏览器 SDK 最短链路已完成并迁出 `analytics-core`；多语言和框架 SDK、CDN 版本化、CSP 文档和 performance metrics 进入后续阶段评审。
- P1-005 已建立本地 `src/analytics-service` 仓库，服务展示名统一为 `simpletrack-anaysitics-service`；当前已补可选同进程 ingestion worker、本地/小部署 ClickHouse routed table auto migration、HTTP source resolver，以及 query token 生命周期/审计基础，`simpletrack-saas` 内部 runtime-source API 也已落地，后续重点是 quota、domain allowlist、internal traffic 产品配置、salt 轮换和更复杂的 Events/Realtime 查询交互。

## R4：企业分析控制台 UI 是否可落地

状态：待评审

当前判断：

- 可落地，但不能只看模板首页截图。
- 必须确认模板的 dashboard shell、layout、table、filter、form、nav、empty state、charts 是否足够可定制。
- 已确定先选择 Supastarter for Next.js，因此 UI 评审优先围绕 Supastarter 的 SaaS app、admin、organizations、payments、settings 和 shared components 展开。
- MakerKit 仍作为 B2B 企业控制面对照，重点看未来是否有明显领先证据。
- MkSaaS 的 data-table 组件可作为 Events table 参考；ShipAny 更偏 AI SaaS 快速模板；ShipFast/Mkdirs 不作为第一底座。

初步评审：

| 模板 | 企业分析控制台 UI 支撑判断 | 说明 |
| --- | --- | --- |
| Supastarter for Next.js | 已确定先选 | 本地有 `apps/saas`、`apps/marketing`、`apps/docs`、`apps/mail-preview`，并有 admin、organizations、payments、settings、shared 组件和多支付 provider。 |
| MakerKit Next.js | B2B 对照/备选 | 可能强在更细组织/RBAC、Super Admin、按席位/用量计费、测试和文档；但当前没有本地源码，不阻塞 Supastarter 路线。 |
| MkSaaS | 组件参考 | 本地有 Next 16、Better Auth、Stripe、Fumadocs 和较完整 data-table 组件，可参考 Events table，但组织/多支付/企业治理证据弱于 Supastarter。 |
| ShipAny | 快速 AI SaaS 参考 | 本地 template-two 有 Better Auth、RBAC 脚本、Stripe/Creem/PayPal 和 Fumadocs，更偏 AI SaaS 快速启动。 |
| ShipFast | 非第一选择 | Next 14、NextAuth、Mongo、Stripe、Mailgun/Crisp 方向，企业分析控制台证据弱。 |

评审问题：

- 模板是否使用 shadcn/ui、Tailwind、Radix、Ant Design 或其他成熟组件系统。
- 页面密度是否可调。
- 是否有稳定的 sidebar、breadcrumbs、tabs、table、filter bar、settings form。
- 颜色、圆角、阴影、营销式卡片是否容易收敛。

下一步：

- 对候选模板做 UI 可改造性截图评审。
- 在原型中固化 SimpleTrack 控制台规范。

## R5：免费方案与付费方案差异

状态：待评审

当前判断：

- 免费方案通常不一定功能更少，但经常在功能深度、产品化完整度、文档、升级支持、企业控制面和维护确定性上弱于成熟付费模板。

评审问题：

- 免费方案是否只是代码参考，还是能直接承接生产控制面。
- 免费方案缺失的能力需要多少开发成本补齐。
- 付费方案的许可证、更新、支持是否值得成本。
