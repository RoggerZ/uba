# SimpleTrack 分阶段实施计划

> 状态：已确定基础方向，细节持续更新  
> 最近更新：2026-05-11
> 目标：先圈定功能，再按阶段逐步细化后端和生产实现。

> 当前版本标记：`analytics-core` commit `5625e8d`；`analytics-service` commit `4495cdf`；`simpletrack-saas` commit `6c24ba1`；父仓本轮文档 commit `f2f04ad`。

## 总纲

SimpleTrack 分成两条主线：

1. **商业控制面**：登录、组织、订阅支付、邮件、Admin、AI 基础设施，优先由成熟 SaaS 模板或成熟服务承接。
2. **分析数据面**：tracker、collect API、事件校验、实时写入、事件存储、查询聚合、目标、漏斗、路径、归因，属于 SimpleTrack 的核心自研价值，P1 起通过独立仓库 `analytics-core` 承接。

阶段推进原则：

- P0 确认底座和原型，避免选型反复。
- P1 先把数据管道和公开产品入口跑通，不扩成完整分析平台。
- P2 再补分析增强能力。
- P3 再补企业治理和高级商业化能力。

## P0：产品与底座确认

状态：已确定要做，正在推进

目标：

- 建立可评审、可交互、可迁移到生产方向的 Next.js 原型。
- 确认 Supastarter 接入 spike 和必要许可证核验清单。
- 确认 SimpleTrack 的商业控制面和分析数据面边界。

已确定范围：

- 原型继续走 Next.js。
- 不在原型内继续自建登录、支付、邮件、组织、后台、AI 基础设施。
- 付费 SaaS 模板已确定先选择 Supastarter for Next.js；MakerKit 保留为 B2B 企业控制面对照和备选。
- ShipAny、MkSaaS、ShipFast、Mkdirs 作为 `template-src` 本地参考，不再作为第一梯队生产底座。
- 视觉方向采用低装饰、高密度、信息层次稳定的企业分析控制台。

交付物：

- `simpletrack/prototype/simpletrack-enterprise-mvp/`：可交互原型。
- `simpletrack/docs/原型驱动的页面数据契约与接口草案.md`：基于 `simpletrack-enterprise-mvp` 的页面级 contract；当前页面集合收口为 onboarding、dashboard、events、goals、settings，旧 Team/Funnels/Insights 原型页不进入 P1 contract。
- `simpletrack/docs/SimpleTrack技术栈与SaaS模板选型评估.md`：技术栈与模板选型评估。
- `simpletrack/docs/实施决策/`：已确定和待评审决策库。
- `simpletrack/docs/Q&A/`：概念解释和评审问答。
- `simpletrack/docs/实施决策/付费SaaS模板本地对比.md`：本地模板源码对比。
- `simpletrack/docs/实施决策/analytics-core实施方案.md`：`analytics-core` P1 抽取和实施方案。
- `src/analytics-core/`：`analytics-core` 独立仓库子模块，远端为 `git@github-simpletrack:simpletrack/analytics-core.git`；当前定位为可被 Go 服务引用的业务无关第三方库，公共包位于根目录，已验证 Redis Stream publish / consume / ack，并已落地 ClickHouse native batch writer、GORM/MySQL ingestion status guard、Events/Realtime query plan、ClickHouse query reader、ingestion worker 边界、本地 Redis/MySQL/ClickHouse compose、冷启动 readiness 重试和 opt-in 端到端验收测试。
- `src/analytics-service/`：`simpletrack-anaysitics-service` Go 仓库，远端为 `git@github.com:simpletrack/anaysitics-service.git`；当前已推送远端并开始登记父仓子模块；负责运行时 `/collect`、`/tracker.js`、write key、Origin/CORS、internal traffic、bot 过滤、调用 `analytics-core`、可选同进程 ingestion worker、内部 `/v1/realtime` / `/v1/events` / `/v1/properties` / `/v1/goals` readback，以及本地/小部署 ClickHouse routed table auto migration。
- `src/simpletrack-saas/`：从 Supastarter 初始化的 SimpleTrack SaaS 工作副本子模块，远端为 `git@github-simpletrack:simpletrack/simpletrack-saas.git`；已挂载 Websites、Realtime、Events、Goals 页面，在 Websites 页完成真实 source 列表与最小创建表单，Events 页已接入 server-side readback、repeatable property filters、`/v1/properties` 属性目录建议和 `visit_id` 可见列，Goals 页已接入关键事件创建和 `/v1/goals` 24h count readback；`9c8ba37` 先收口了 Realtime / Events / Goals 的 Website selector 文案、Events 重复消息，以及 `4xx` / `5xx` / network failure 的 readback page copy，`37bd0f3` 补上 authenticated seeded ready-state 浏览器闭环：用真实 Website `write_key` 为 Realtime / Events / Properties / Goals seed 读回放成功数据，验证 ready-state UI 不依赖假成功态；`902029e` 再把 ready-state Playwright 断言收窄到 card / row 级证据，避免 Goals 表格严格定位器歧义；`7911cac` 先把 route-scoped Events disable 叠加进 success journey，`89c411c` 再把这条路径修正为真实 `403 + structured error` 闭环，`110a96b` 继续把 Realtime / Property catalog 的 route-scoped structured `403` 合同补齐到 unit、SSR 和 Playwright 三层，`b65582d` 又把 Goals route-scoped structured `403` 的定义保留、计数隐藏，以及 Realtime / Events seeded ready-state 隔离性补进同一三层闭环，`bba3e18` 再把 analytics browser specs 的共享 sign-up helper、Playwright 进程内 Better Auth rate-limit override、runtime-config 单测和完整 Chromium suite 一并收口为真实闭环，而最新 `c5b8254` 又把 per-source `readback_policy` 真正持久化到 Website schema、runtime-source 和 Websites settings；父仓本轮同步 commit 已回填 `3d82ac2`；已替换 marketing/docs/mail-preview 可见模板占位，并把 `privacy-policy` / `terms` 从模板占位替换为 SimpleTrack 基线文案后通过审查；`template-src/ai-supastarter-template` 保持模板源，不直接修改。
- `references/xwl_bi-backend/`：本地 `xwl_bi` 后端只读临时参考快照，用于 `analytics-core` 实现对照。
- `references/umami/`：Umami 官方 GitHub 源码只读参考快照，用于 tracker、collect、事件/会话模型、Realtime/Events 查询和 ClickHouse schema 实现对照；`simpletrack/docs/umami/docs/源码实现参考/` 已按 P1 数据管道拆分为架构、目录、tracker、collect、写入、模型、读侧查询、过滤查询、时序图、对照清单和 Q&A 概念解释。
- `simpletrack/README.md`：SimpleTrack 文档和原型资料入口。

验收标准：

- 评审时能点击主要页面和流程。
- 页面结构能迁移到成熟 SaaS 模板的 dashboard shell。
- 已明确哪些能力暂时 mock，哪些能力未来交给模板或服务。
- 已列出购买或采用模板前的一天 spike 验收项。
- Supastarter purchases helper 能在 SimpleTrack 页面中驱动 Free plan source 限制和升级入口。
- Supastarter marketing/docs/mail-preview 能承接 SimpleTrack 产品介绍、定价语义、docs/quickstart 和基础邮件预览内容。
- 公开站点的 `privacy-policy` 与 `terms` 已替换模板占位，不再把明显的模板默认法务页面暴露给外部访客。
- `analytics-core`、`simpletrack-saas` 已作为独立子仓库完成远端推送和父仓子模块挂载。
- SimpleTrack 专用 SSH key、`github-simpletrack` Host、`config_simpletrack` 和仓库级 `core.sshCommand` 已形成可复现配置说明。
- `xwl_bi` 后端参考快照已进入父仓，但明确作为只读临时资产，不作为活跃模块开发。
- Umami 官方源码快照已进入父仓，但明确作为只读参考资产，不作为活跃模块开发，也不直接复制源码；P1 数据管道源码深解文档和 Q&A 已补齐给 SimpleTrack 和 `analytics-core` 的启发。

## P1：数据管道活了 + 公开产品入口

状态：已确定为第一阶段核心目标

一句话目标：

- 客户站点上的页面浏览和自定义事件，能够被 SDK 采集，经 collect API 写入数据面，并在 Realtime 与 Events 中被看到；同时用户能通过产品官网了解产品、查看定价/订阅入口，并按 docs/quickstart 完成接入。

P1 必做：

- `analytics-core` 独立核心库：从 xwl_bi 抽取分析数据面核心，仓库名不带 `simpletrack` 或 `xwl`，作为 Go 第三方库被 `simpletrack-anaysitics-service` 引用。
- `simpletrack-anaysitics-service` 运行时服务：负责 `/collect`、`/tracker.js`、write key 解析、Origin/CORS、internal traffic、bot 过滤和调用 core；不负责控制面配置 CRUD。
- Tracker SDK：支持页面浏览、基础自定义事件、write key 接入和静态托管。
- Collect API：由 `simpletrack-anaysitics-service` 接收事件、执行 runtime source config 校验，再调用 `analytics-core` 标准化和发布。
- Event validation：校验 site、event name、timestamp、session/user 标识。
- Visit identity：`visit_id` 在 collect 写入前确定，进入 ClickHouse 事件表和属性表，Realtime / Events 直接读取存储字段。
- Event storage：写入事件存储，优先围绕 ClickHouse 或可替换事件存储设计。
- Realtime view：能看到最近事件和基础实时指标。
- Events table：能查询事件列表、时间、页面、来源、用户标识、属性摘要。
- Website / Project 设置：能创建站点、生成安装 snippet、查看接入状态。
- Goal 最小闭环：定义一个关键事件，并通过 Goals 页面 / dashboard 读回放显示是否有数据。
- 产品官网 / Marketing Site / 公开站点：类似 `https://umami.is/` 的公开入口，包含产品介绍、定价/订阅入口、docs/quickstart。

P1 明确不做：

- Team / RBAC。
- Revenue / Attribution 产品化页面。
- Session Replay / Performance。
- Boards / Share / API Key。
- Funnels / Journeys 产品化页面。
- 旧 `simpletrack-umami-inspired` 中的 Team / Funnels / Insights 独立页面。
- 复杂自定义报表编辑器。

P1 后端细化方向：

- 数据模型先围绕 `workspace`、`site`、`event`、`goal`、`usage` 设计；`analytics-core` 内部保持业务无关，不写入定价、团队、订阅、账单概念。
- `analytics-core` 的能力边界包含：事件分析、漏斗分析、留存分析、路径分析、LTV 分析、归因分析、分群、会话、元数据、实时数据、事件属性、用户属性。
- `analytics-core` 支持 Kafka、MySQL、Redis、ClickHouse 技术栈。
- collect 层已确定把输入请求标准化为 `EventEnvelope`，并校验 `id`、`tenant_id`、`project_id`、`source_id`、`source_type`、`event_name`、`distinct_id` 和时间戳；`collect.Handler` 已负责标准化后发布到 EventBus。
- collect HTTP API 已迁移为 Fiber v3 适配层；Fiber 只做路由、JSON 解码、状态码和响应适配，不把框架上下文传入 `collect.Handler` 或分析核心逻辑。
- 事件队列必须抽象为 interface，避免第一版被 Kafka 绑定。
- 前期可以使用 Redis Stream 替代 Kafka，但不删除 Kafka 代码，后续保留 KafkaBus 作为高吞吐实现。
- Redis Stream 消费语义已确定为 pending 优先：只有 ingestion 写入成功后才 ack；失败消息保留 pending 并重试；达到 `MaxAttempts` 后进入死信队列并 ack 原消息。
- ingestion 入库必须按事件唯一键保持幂等，重复消费同一事件时视为成功处理，不能写出两份事件明细。
- ClickHouse 表策略直接采用方案 B：按 `tenant_id / project_id / source_id` 路由到物理事件表，但上层仍通过统一 `events` 逻辑模型和 query builder 查询。
- ClickHouse 事件明细高吞吐写入优先使用 `clickhouse-go/v2 PrepareBatch` 原生批量写入；GORM `CreateInBatches` 作为低频写入或压测对照，不作为默认事件入库热路径。
- 入库必须做幂等去重，同一 `(tenant_id, project_id, source_id, event_id)` 重复消费时不能在数据库存两份事件；当前已落地 `EventWriteGuard` 接口边界和 GORM/MySQL `IngestionStatusGuard` 真实状态守卫，后续 worker 必须复用这条链路。
- Realtime 与 Events 查询已建立 `storage.EventQueryBuilder` 契约，ClickHouse adapter 通过 GORM ClickHouse dry-run query builder 输出 query plan；后续查询执行器必须复用这套字段白名单、表路由和过滤边界。
- Realtime 与 Events 查询已建立 `storage.EventReader` 契约，ClickHouse adapter 通过 GORM Raw 执行 query plan 并返回 `EventRecord`，产品层不得直接扫描 ClickHouse 动态表。
- `ingestion.Processor` 已明确为 P1 worker 边界：EventBus adapter 负责 ack/nack，Processor 负责调用 `storage.EventWriter`，真实 worker 入口不得复制一套消费写入逻辑。
- 本地运行依赖已固定在 `src/analytics-core/docker-compose.yml`：Redis 使用 `redis/redis-stack:latest`，MySQL 使用 `mysql:8.4`，ClickHouse 使用 `clickhouse/clickhouse-server:25.3`；默认映射到高位端口以避开本机已有 Redis/MySQL。
- 从 xwl_bi 抽取时不做整仓改名；应剔除 `xwl_` 变量、函数、包和业务命名前缀，按更专业、业务无关的方向重命名和重构代码结构。
- SimpleTrack 引用该核心仓库，并在自己的产品层处理定价、团队、订阅、账单、onboarding 和企业控制台。
- 配置的 CRUD 在 `simpletrack-saas`，配置的运行时执行在 `simpletrack-anaysitics-service`：write key、domain allowlist、internal traffic、quota 等不能在两个服务里重复拥有生命周期。
- 旧的 xwl_bi Vue2 后台界面不考虑复用。
- 产品参考采用“双参考”：Umami 参考分析对象体系、事件语义、Realtime/Events/Funnels/Journeys/Retention/Segments 边界；Litlyx 参考短接入链路、Raw Events 验收、Product 空态/示例态/真实态和 `Show test data` 教育方式。
- Umami 源码参考采用“只读实现对照”：可参考 `tracker`、`api/send`、`event_data`、Realtime/Events 查询和 ClickHouse schema；但 P1 仍保持 `analytics-core` 的 Go collect、EventBus、ingestion worker、`EventWriter`、`EventReader` 和 `EventQueryBuilder` 边界。
- 产品官网和 docs 先使用 Supastarter 的 `apps/marketing`、`apps/docs`；如果效果满足，直接使用；如果不满足，再轻量定制。

### Umami 源码启发的 `analytics-core` 优化排期

这些优化来自 `simpletrack/docs/umami/docs/源码实现参考/` 的源码结构梳理，落地原则是：P1 只补数据管道闭环必须项，P1.5/P2 再做性能和高级分析增强。

| 排期 | 优化项 | 给 SimpleTrack 的启发 | 给 `analytics-core` 的启发 | 状态 |
| --- | --- | --- | --- | --- |
| P1-002A | 事件属性与用户属性模型 | Events 页面不能只显示事件名，还要能看到 custom event 属性和 identify 用户属性摘要 | P1 已落地属性入口契约、`EventPropertyRecord` / `FlattenEventProperties` typed row 展开、ClickHouse `PropertyBatchWriter`、`PropertyIndexingEventWriter` 热路径组合和 MySQL `property_indexing_status` guard；真实 e2e 已证明属性可随 ingestion 写入 `_properties` 表，并能被 allowlisted property filter 精确过滤；processing ambiguous 不自动重试以避免重复属性行 | 已完成 |
| P1-002B | client info enrich 与 bot/IP 过滤 | 产品上可以提供 internal traffic / bot 过滤开关，避免接入验收被无效流量污染；DNT 作为显式隐私开关，不默认影响现有接入 | 第一版已用 `collect.Stage` 落地 UA/referrer/盐化 IP hash 派生属性和 bot/internal traffic 过滤；当前又补齐 browser / OS / device 的可插拔 UA 派生，以及 `GeoResolver` 接口和 `ANALYTICS_SERVICE_GEOIP_MMDB_FILE` 离线 mmdb 装配；HTTP 默认不信任 forwarded IP，需显式可信代理开关；浏览器 SDK 已支持 opt-in DNT，active 时不发送且不持久化 identity，并自动收集 allowlisted UTM/click id；bot 与 internal IP filter 的审计回归已补齐，日志只记录过滤 reason 和 event/source 边界，不写 raw IP；Websites 设置区已把 Bot user agents、Internal CIDRs、Internal IPs 做成可见配置项，表单提交和 runtime source 回写已有回归覆盖；不进入 ClickHouse writer | 已完成；过滤统计、过滤原因报表和配置变更审计放 P1.5/P2 |
| P1-002C | session/visit resolver | 用户看到的是 visitors/sessions/active users，不需要理解内部匿名 hash；DNT active 时不应生成持久浏览器身份 | `SessionResolverStage` 已派生缺失的 `session_id`，使用 salt + 时间窗口 + identity 输入，原始 IP/UA 不落库；`VisitResolverStage` 已按长期方案补齐缺失的 canonical `visit_id`，显式 SDK `visit_id` 会被保留，并把 `visit_id` 写入 `EventEnvelope`、ClickHouse event 表和 `_properties` 表；Realtime / Events 直接读取存储字段，不再依赖 readback 临时派生；`simpletrack-saas` runtime-source 会输出 server-only `visit_salt` 与 `visit_window_seconds`；浏览器 DNT opt-in 已避免 DNT active 时创建持久浏览器 identity | 已完成；salt 轮换、cookie/no-cookie、Sessions 专页和 retention 产品化放 P1.5/P2 |
| P1-002D | 查询字段白名单与过滤构建 | Events / Realtime 的过滤体验要稳定，非法字段不应该打到数据库 | `EventQueryBuilder` 已落地类型化排序字段/方向白名单、过滤字段/operator 白名单、filter 数量上限、typed property filter allowlist 和 `ErrInvalidEventQuery`；属性过滤使用 ClickHouse tuple `IN` 查询属性表，并通过真实 e2e 验证 | 已完成 |
| P1-002E | Realtime/Events 最小端到端验收 | Realtime 是“活性检测”，Events 是“原始证据”，P1 必须可复验 | 已用本地 Redis/MySQL/ClickHouse 跑通 collect -> EventBus -> ingestion -> ClickHouse -> `EventReader`，覆盖 pageview、自定义事件属性、user properties、property writer、属性索引热路径和属性过滤精确排除非匹配事件；冷启动依赖 readiness 已补重试 | 已完成 |
| P1-004 | Web tracker SDK 最短链路 | docs/quickstart 已改为 `data-write-key` snippet，Browser SDK 由 `simpletrack-anaysitics-service` 的 `/tracker.js` 静态交付；继续支持 auto pageview、manual track、identify、debug、关闭自动采集、DNT、UTM/click id 和 snippet queue | SDK 已从 `analytics-core` 移出，避免 core library 承担客户端发布职责；事件进入 `simpletrack-anaysitics-service` 后由 write key 映射到 `analytics-core` 的 `collect.Request` / `EventEnvelope` | 已完成 |
| P1-005 | SimpleTrack Anaysitics Service | `simpletrack-saas` 管理配置，`simpletrack-anaysitics-service` 执行运行时校验；这让 SaaS 控制面不接收高频事件，也不直接写 ClickHouse | `src/analytics-service` 已推送远端 `simpletrack/anaysitics-service` 并登记父仓子模块；当前已实现 Fiber runtime app、`/healthz`、`/tracker.js`、`OPTIONS /collect`、`POST /collect`、`MemoryResolver`、HTTP source resolver、write key、Origin/CORS、scope override、bot/internal traffic 过滤、collect 调用 core、可选 ingestion worker 装配、内部 Events/Realtime/Properties/Goals 读接口、query route 配置、Swagger UI / OpenAPI 文件、query token 轮换 allowlist、结构化生效/过期时间、route scopes、可选 `write_keys` allowlist、source-scoped 属性过滤白名单、source-scoped 属性目录读回和 Goal count readback；`simpletrack-saas` 内部 runtime-source API、Websites 控制面 CRUD 和 Realtime/Events/Goals 页面读回放已落地 | 已完成；复杂聚合分析和高级分析对象放 P1.5/P2 |
| P1-005A | `analytics-core` Go library API | SimpleTrack 后端服务通过 Go module 引用 core，而不是把 core 当独立业务服务部署 | 公共 API 调整为根目录包：`contracts`、`collect`、`eventbus`、`ingestion`、`storage`；外部服务不 import `internal` | 已完成 |
| P1-005B | collect runtime service | `/collect` 是运行时数据面入口，应由 `simpletrack-anaysitics-service` 负责，避免 `simpletrack-saas` 接收高频事件 | 已完成 Fiber runtime 迁移、collect runtime 单测，并新增显式开启的同进程 ingestion worker：Redis Stream 消费、MySQL `ingestion_status` / `property_indexing_status` guard、ClickHouse native event writer、typed property indexing，以及默认开启的 MySQL `property_catalog` 属性目录治理；启动默认校验启用 source 的 ClickHouse event/property 表存在，避免健康服务吞事件后持续 dead-letter；本地/小部署可用 `ANALYTICS_SERVICE_CLICKHOUSE_AUTO_MIGRATE=true` 创建当前 runtime config 内所有启用 source 的 routed event / `_properties` 表后再校验；`ANALYTICS_SERVICE_MYSQL_AUTO_MIGRATE=true` 会初始化 `property_catalog`，否则属性目录默认开启时要求该表已存在并在启动期 fail-closed；README 本地运行示例已补 `visit_salt` 与 `visit_window_seconds`，和当前运行时校验对齐 | 已完成；部署级 schema migration/rollback 规范留到真实上线后再评审 |
| P1-005C | SaaS 配置 runtime enforcement | write key、domain allowlist、internal traffic、quota 的管理在 SaaS，执行在 `simpletrack-anaysitics-service` | 已落地 `simpletrack-saas` 内部 runtime-source API；`simpletrack-anaysitics-service` 的 HTTP resolver 只按 write key 读取 runtime source config，不创建/修改 SaaS 配置，并用 bearer token + 短 TTL 缓存 + `ETag` 条件重验证控制热路径依赖且避免 stale auth state；控制面 URL默认必须是 HTTPS，本地 loopback HTTP 需要显式 opt-in；同进程 ingestion 会把 HTTP 返回 source 绑定到启动 `ANALYTICS_SERVICE_SOURCES_JSON` schema surface；runtime source config 必须携带 server-only `session_salt`、`visit_salt`、`visit_window_seconds` 和 `client_hash_salt`，不能用公开 write key 派生；`Websites` 页现已使用真实组织 Website 数据并提供最小 source 创建、编辑、enable/disable 和 delete 入口，创建与启用动作都通过 serializable 事务 helper + `P2034` 重试守住 active source limit，disabled source 不再占用 active source limit，编辑动作会保留 secondary `allowedOrigins` 不被单字段表单覆盖，并可回写 `bot user agents`、`internal CIDRs`、`internal IPs`、`includeClientFingerprint` 和 `readback_policy` 四类开关，且这些 runtime source 字段已有可见 label；P1 quota 策略已明确只执行 active website source 上限，Free=1、Pro/Lifetime=5、Enterprise=不限；事件量、留存期、成员数和用量计费不提前进入 P1；`analytics-service` 已补 handler 级回归，验证 cached source 在控制面 disable/delete 后会立即拒绝 collect/readback | 已完成；salt 轮换、runtime source 变更审计和更细的配置历史放 P1.5/P2 |
| P1-005D | Events / Realtime 查询 API | `simpletrack-anaysitics-service` 负责内部读回放，不把查询逻辑放回 SaaS 控制面 | 已提供内部 `/v1/realtime` 与 `/v1/events` 读接口，bearer token 保护并映射到 `analytics-core` 的 `EventReader`；默认路径保留，同时支持 `ANALYTICS_SERVICE_EVENTS_PATH` 与 `ANALYTICS_SERVICE_REALTIME_PATH` 配置；Realtime 使用短窗口，Events 已补 `event_name`、`distinct_id`、`visit_id`、`limit`、`offset`、`sort_field`、`sort_direction` 白名单、`hasMore` 分页和 `30m / 6h / 24h / 7d` 时间窗口预设，并继续沿用显式时间范围；query routes 已由 Fiber CORS middleware 处理 preflight；Swagger UI / OpenAPI 文件已接入；响应现在透传持久化 `visit_id`，且 SaaS 页面已把该字段暴露为可见列，并补页面级回归覆盖；`simpletrack-saas` Realtime / Events 页面已通过 server-side helper 和 client-safe Website selector 初版接入，内部 query token 不暴露到浏览器；重复 query 参数和空页偏移已补回归硬化；Events 页已升级为 repeatable `property_filter` 多条件查询，page/query-state/readback 统一切到数组模型，并共享 5 条上限以匹配 `analytics-service` 的服务边界；`analytics-service` 也补了 repeatable `property_filter` 的服务层回归；OpenAPI 已把 `property_filter` 标为 repeatable array，并补齐 `event_name` 排序枚举；服务端已支持 `ANALYTICS_SERVICE_QUERY_TOKENS_JSON` 短窗口轮换 allowlist，并可附带 `id`、`not_before`、`expires_at`、route scopes 和可选 `write_keys` allowlist；运行时会拒绝过期/未来 token，并在 source resolution 之前先按 token route scope + `write_key` allowlist fail closed，同时记录轮换命中和拒绝审计日志；`simpletrack-saas` 的 readback helper 现在还支持 route/shared `write_key` token maps，显式 map 命中时优先选择 source-specific token，缺项时不再回退到更宽 token；属性过滤通过 runtime source config 的 `allowed_property_filters` 和 `analytics-core` query builder 双重白名单落地；当前已补组合查询回归，覆盖 SaaS 请求序列化、service query mapping 和 core ClickHouse query plan 三层的标量过滤、`visit_id`、分页、排序、时间窗口和 repeatable property filters 同时存在；`e83f8e5` 先给 Realtime / Events 的 `query_evidence` 增加 `observed_rows`，`4495cdf` 又把它补进 OpenAPI `QueryEvidence` schema，并加上真实 spec drift test，避免 served Swagger contract 落后于 handler 响应，`e2362ff` 再把同一字段与 `pressure` 保留进 SaaS server-side readback view model，`ec51160` 把 observed row count / pressure 以低权重文案显示到 Realtime / Events / Goals 页面，`1ab56a9` 又把这段 evidence copy 锁进真实 Chromium ready-state 页面断言，而最新 `7d909aa` 继续锁住 route-scoped structured `403` 下失败页不残留旧 evidence、未受影响页保留各自 evidence 的浏览器级合同 | 已完成；更复杂聚合分析、Breakdown/Compare/Funnels/Journeys 放 P1.5/P2 |
| P1-005E | Goal 最小闭环读回放 | Goal P1 只做“关键事件是否进入数据管道”的最小验证，不提前做 Funnels/Journeys | 已新增 `analytics-core` `EventCountQuery` / `BuildEventCountQuery` / `CountEvents`，Goal count 继续走 `EventQueryBuilder` / `EventReader`，不在 SaaS 或 handler 拼 ClickHouse SQL；`simpletrack-anaysitics-service` 新增内部 `/v1/goals`、`ANALYTICS_SERVICE_GOALS_PATH`、OpenAPI 和服务层回归，按 query token + write key 解析 source 后做精确 count；`e83f8e5` 先把 Goals 响应里的 `query_evidence.observed_rows` 固定为本次执行的精确 count，方便把 query shape 与结果基数并排观察，但不把 count 直接升格为 `pressure` 决策输入；`4495cdf` 再把同一字段补进 OpenAPI schema，并用真实 spec drift test 保证 Swagger contract 同步反映 Goals/Realtime/Events 的 observed row metadata；`simpletrack-saas` 新增 `Goal` 模型、组织内 Goals 页面、事件名校验、按 Website 列表、server-only `/v1/goals` readback helper 和并发唯一约束冲突回归；Goals readback UI 已把 ready/no-data 显示为 `No data` + `0`，把 service failure 显示为 `Readback error` + `-`；事件名契约复制风险已用 `analytics-core` `1ea78f3` 与 `simpletrack-saas` `9c56e96` 的一致样例测试缓解，覆盖 digit-first、mixed-case、长度边界和非法前导符号；`simpletrack-saas` `9c56e96` 又把单页 Goal readback fan-out 用 `MAX_WEBSITE_GOALS` 在 Prisma query 与页面 `Promise.all` 两层收口到 25；随后 `simpletrack-saas` `9c8ba37` 又收口了 Realtime / Events / Goals 的 selector 文案、Events 重复消息，以及 `4xx` / `5xx` / network failure 的 readback 状态文案，`37bd0f3` 再补上 sign-up -> Website create -> seeded ready-state 的真实 Playwright 成功路径，`902029e` 进一步把 ready-state 断言收窄到 card / row 级证据，`7911cac` 先证明 route-scoped Events disable 不会误伤 Goals ready count，`89c411c` 再把这条路径修正为真实 `403 + structured error` 合同，`110a96b` 继续把 Realtime / Property catalog 的 route-scoped structured `403` 合同补齐到 unit、SSR 和 Playwright 三层，`b65582d` 又把 Goals route-scoped structured `403` 的定义保留、计数隐藏，以及 Realtime / Events seeded ready-state 隔离性补进同一三层闭环，`bba3e18` 再把 analytics browser specs 的共享 sign-up helper、Playwright 进程内 Better Auth rate-limit override 和完整 Chromium suite 稳定性一并收口，`c5b8254` 与 `a398362` 先把 route-scoped readback access 推进到控制面真源 + query token 可选 `write_keys` allowlist 双层收口，`d463328` 再把 SaaS helper 对齐到按 `write_key` 选 token 与显式缺项 fail closed，`e2362ff` 又把 Goals helper 的 `query_evidence.observed_rows` / `pressure` 保留到 SaaS server-side view model，`ec51160` 再把同一 evidence 显示到 Goals 页面，`1ab56a9` 又把 ready-state 里的可见 evidence 文案锁进真实 Chromium 页面断言，而最新 `7d909aa` 继续验证 Goals route-scoped `403` 后不会残留旧 evidence，且 Events / Realtime 的 seeded evidence 不受影响；版本标记：`analytics-core` `1ea78f3`，`analytics-service` `4495cdf`，`simpletrack-saas` `7d909aa`，父仓 `9da0b08` | 已完成；更大规模批量 Goal count、Goal 趋势、转化率、Funnels/Journeys 和更窄 readback auth 放 P1.5/P2 评审 |
| P1.5-001 | ClickHouse 读侧优化与属性治理 | 数据量变大后，Realtime、Events、Breakdown 不能靠全表扫维持体验 | 长期方案采用分层路线：先做属性治理和 query plan 约束，保持 `EventQueryBuilder` / `EventReader` 为唯一事件读侧入口；当前已开始以 `readSidePolicy` 收口查询护栏（guardrails，见 `simpletrack/docs/Q&A/guardrails是什么意思.md`），以 `EventQueryEvidence` 记录 query family、read path、optimization、effective limit、offset、time lower/upper bound、time window、属性表参与、过滤数量和 value-free property filter shape，并新增 `PropertyCatalog` / MySQL catalog adapter / `PropertyCatalogingEventWriter` / `PropertyCatalogReader` 作为属性字典治理基础；`simpletrack-anaysitics-service` 已将 property catalog 接入 ingestion 运行时，并新增内部 `/v1/properties`，支持通过 query token + write key 读取 source-scoped 属性目录元数据；`simpletrack-saas` 已把 `/v1/properties` 接入 Events filter builder，筛选字段建议来自 source-scoped property catalog，浏览器不接触内部 query token，非法 catalog payload 会作为 service error 暴露；`pressure` 已作为 query evidence 的 triage 桶写入 Q&A；`analytics-core` 已新增 builder-only read-side shape benchmark、真实 ClickHouse EventReader benchmark、真实 ClickHouse BatchWriter benchmark、GORM `CreateInBatches` 对照 benchmark、Redis Stream publish / subscribe+ack benchmark、`collect.Handler` 热路径 benchmark，以及 opt-in `TestEventReaderClickHouseExplain` explain 证据测试，覆盖典型读形状、当前单事件写入热路径、GORM/native 写入差异、队列 publish/ack 成本、不含 Redis 的 normalize / identity / client enrich 成本，以及当前 ClickHouse explain 路径；2026-05-08 已记录真实 ClickHouse reader 基线，10k 行下 low realtime 约 8.6-10.4ms/op、medium scalar events 约 8.6-9.7ms/op、high property events 约 15.2-16.3ms/op，100k 行 pressure run 两轮下 high property 稳定到约 29.8-34.2ms/op；2026-05-09 的 500k 行复测已把 Realtime 和 Events 都拆成 recent-window 与 wide-window 场景，近期 Realtime / Events 约 7.9-9.0ms/op，宽时间窗 scalar / typed property Events 约 40.3-44.1ms/op；explain 进一步显示高属性过滤场景出现 value-free property filter shape、`CreatingSets`、3 个 `event_id in 5000-element set` 和包含 `visit_id` 的主键条件；补充结对审查后，`analytics-core` 已通过 `$ai-slop-cleaner` 范围修复把 `PropertyFilters` evidence 改成快照语义，并补 value-free example 与不可变性回归测试；当前结论是事件热路径继续使用 native `PrepareBatch`，GORM 留在 query builder、低频管理写入或对照压测路径；`src/analytics-core/docs/read-side-optimization-policy.md` 已把 projection / materialized view / 小时聚合表的 evidence、benchmark、explain、适用边界和验证命令固化为引入门槛；`simpletrack-anaysitics-service` 内部 readback API 和 OpenAPI 已透出这些 query shape 字段，且不会回显属性过滤值 | 进行中，继续观察宽时间窗 Events 明细查询和 typed property 过滤读路径；没有 query evidence + benchmark + explain + 回归计划时，不新增 ClickHouse 物理结构 |
| P2-001 | Performance metrics | LCP、INP、CLS、FCP、TTFB 对产品体验有价值，但不阻塞 P1 收数 | 可作为事件类型或属性组承接，P2 再决定是否独立建模 | 暂缓 |

P1.5-001 补充说明：500k 行复测已把 Realtime 基线拆成短窗口 `low_realtime_recent_window` 和宽时间窗 `low_realtime_wide_since`，并把 Events 基线拆成 `medium_events_scalar_recent_window` / `medium_events_scalar_wide_window` / `high_events_property_recent_window` / `high_events_property_wide_window`。短窗口 Realtime 与近期 Events 当前稳定。随后 `analytics-core` commit `f84024a` 又把 typed property filters 收口到 query-builder guardrail：必须显式带 `from/to`，且 direct fact-table 查询窗口默认不超过 7 天；`a99147f` 把 bounded scalar 扩成 24h / 72h / 7d 多窗口证据；`be47dfb` 又把 builder-only high property benchmark 的实际 typed property filters 对齐到 string / number / bool 预期 evidence；`d850197` 再把真实 ClickHouse reader benchmark / explain fixture 的 allowlist、filters、seeded JSON、typed property rows 和断言同步到同一 typed property 形状；最新 `5625e8d` 补齐普通 query builder 单测里的 bool typed property filter 和 value-free `is_paid` bool evidence。因此当前观察候选收窄为宽时间窗 scalar Events 明细查询和 7 天内 typed property 过滤读路径；没有稳定 query pattern、benchmark、explain 和回归计划时，不新增 ClickHouse projection、materialized view 或小时聚合表。父仓本轮同步 commit `77a7a7d`。

P1-005C/D/E 补充说明：`analytics-service` commit `a398362`、`e83f8e5`、`4495cdf` 和 `simpletrack-saas` 当前基线 `7d909aa` 已把 `readback_policy` 从待评审风险收口为已落地控制面契约。`simpletrack-saas` 的 runtime-source 显式输出 Realtime / Events / Properties / Goals 四类 readback 开关，且这些开关现在由 Website 持久化字段与 Websites settings 真正持有；`analytics-service` 在 token、write key、origin 校验后按路由 fail closed，并在 source resolution 之前先按 query token route scope + 可选 `write_keys` allowlist 拒绝越权读请求；`simpletrack-saas` server-side readback helper 现在也能按 route/shared `write_key` token maps 选择更窄 bearer token，并在显式 map 缺项时直接 fail closed；如果策略缺失、对应路由为 false，或 token 不允许当前 `write_key`，内部读接口返回稳定 `403`，不会进入 `EventReader` 或属性目录 reader。`e83f8e5` 先给 Realtime / Events / Goals 的 `query_evidence` 补上 `observed_rows`，让运维读回放可以把 query shape 和本次实际返回条数 / count 并排看见；`4495cdf` 又把这个字段补进 OpenAPI schema，并用直接读取真实 `api/openapi.yaml` 的 drift test 防止 Swagger served spec 漂移；`e2362ff` 再把同一组 evidence 保留到 SaaS server-side helper 的 view model，`ec51160` 又把它露到页面，避免 helper 层和产品可见面继续丢掉 `observed_rows` / `pressure`，`1ab56a9` 又把同一文案锁进真实 Chromium ready-state 页面断言，避免 evidence 只停留在 SSR 层，而 `7d909aa` 继续验证 route-scoped structured `403` 时失败页不会残留旧 evidence、未受影响页仍保留各自 evidence；该字段仍只代表执行结果基数，不代表扫描放大，也还没有把 row-volume 收口进 `pressure` 决策。`37bd0f3` 已补上第一条 authenticated seeded ready-state live browser evidence，`902029e` 进一步把这条成功路径的断言收窄到更稳定的 card / row 级证据，`7911cac` 先把 route-scoped Events disable 合同并入 success journey，`89c411c` 再把这条路径修正为真实 `403 + structured error` 闭环，`110a96b` 继续把 Realtime 与 Property catalog 的 route-scoped structured `403` 合同补齐到 unit、SSR 和 Playwright 三层，`b65582d` 又把 Goals route-scoped structured `403` 的定义保留、计数隐藏，以及对 Realtime / Events seeded ready-state 的隔离性补齐到同一三层闭环，`bba3e18` 再把 analytics browser specs 的共享 sign-up helper、Playwright 进程内 Better Auth rate-limit override 与完整 Chromium suite 稳定性收口，而 `c5b8254` + `a398362` + `e83f8e5` + `4495cdf` + `d463328` + `e2362ff` + `ec51160` + `1ab56a9` + `7d909aa` 继续把 route-scoped disable 从 mock-only 成功路径推进到“控制面真源 + token 允许的 write key + SaaS 端按 source 选更窄 token + 响应回显实际结果基数 + SaaS 保留同一证据字段 + 页面可见 evidence + 浏览器可见 evidence + fallback evidence 隔离 + OpenAPI 不再漂移”的九层闭环；最新 `ce48c6a` 又把组织首页从模板页推进为 dashboard 摘要页，复用同一套 server-side readback 展示 source health、Realtime intake、24h Events、pageview-only Top pages 和 Activation goal，并把直接 reject 的 readback 降级为页面内 `service_error`。因此下一步除继续评审是否还需要比 `write_key` 更细的内部读权限外，也要评审这些 `write_key` token maps 是否继续停留在部署环境，还是后续交给控制面/运维工具管理，并单独评审是否需要把 row-volume 纳入读侧 triage。

P0-003/P0-004 补充说明：`simpletrack-saas` commit `6c24ba1` 已删除 `ce48c6a` dashboard 替换后剩余的孤儿 `OrganizationStart` 模板组件，避免 `New clients`、`Revenue`、`Churn` 这类模板假指标残留在生产代码里；后续如果要扩展组织首页，必须继续基于 SimpleTrack readback 或明确测试 fixture，而不是复活 Supastarter 模板指标。最新 `8898422` 又把 seeded dashboard 真实 readback 与 Events structured `403` fail-soft 浏览器闭环补上，父仓 gitlink 与文档同步 commit `b2f1522`。

验收标准：

- 新站点复制 snippet 后能上报 pageview。
- 手动触发自定义事件后能进入 Events。
- Realtime 能显示最近事件，不需要刷新很久才看到。
- 自定义事件属性和 identify 用户属性能以明确模型进入存储，并至少在 Events 中可见或可确认已入库。
- Realtime 与 Events 查询必须复用 `EventQueryBuilder` / `EventReader`，并验证字段白名单、分页上限和非法 filter 处理。
- session/visit、client enrich、bot/IP 过滤进入 P1 时必须作为可测试 stage 实现；当前已落地 collect pre-queue stage，browser / OS / device 和 geo 也作为 enrich stage 边界补齐，`visit_id` 已按长期方案写入存储并由读侧直接读取，仍不允许绕过 EventBus 或直接在 HTTP route 写库。
- Web tracker SDK P1 最短链路必须能通过 docs/quickstart 接入；当前改为 `data-write-key`，由 `simpletrack-anaysitics-service` 托管 `/tracker.js` 并通过 write key 覆盖 tenant/project/source，SDK 不再属于 `analytics-core`。
- `simpletrack-anaysitics-service` 必须证明客户端伪造 tenant/project/source 不可信、blocked Origin 被拒绝、CORS preflight 正常、bot/internal traffic 不发布、valid write key 能调用 core publish；runtime source config 可来自本地 memory 或 SaaS HTTP resolver，但两者都不能拥有配置 CRUD；HTTP resolver 默认只走 HTTPS，本地 loopback HTTP 需要显式 opt-in；显式开启 ingestion 时必须复用 `analytics-core` 的 Redis Stream、MySQL guard、ClickHouse writer 和 property indexing 边界，并把 HTTP 返回 source 限定在启动 schema surface 内；默认在 ClickHouse 表缺失时 fail startup，只有显式打开 `ANALYTICS_SERVICE_CLICKHOUSE_AUTO_MIGRATE=true` 才能先创建当前 runtime config 内所有启用 source 的 routed tables。
- 关闭或删除站点后，上报被拒绝或标记为不可用。
- P1 页面不出现 P2/P3 功能入口造成范围膨胀。
- `analytics-core` 已建立独立仓库边界、模块目录、命名规范和 EventBus 抽象。
- `analytics-core` Redis Stream 已覆盖 pending 优先重试、死信队列和幂等 ingestion processor，并通过自动化测试。
- `analytics-core` collect 标准化、`collect.Handler`、Fiber `POST /collect` 入口、storage `EventWriter` 接口、ClickHouse `TableRouter`、native batch `BatchWriter`、GORM/MySQL `IngestionStatusGuard`、`PropertyIndexingEventWriter`、MySQL `property_indexing_status` guard、Events/Realtime query plan builder、typed property filter、ClickHouse query reader、ingestion worker 边界、本地运行依赖和 opt-in e2e 测试已落地，并通过自动化测试；Redis Stream 集成测试和完整 P1 数据管道 e2e 已在 Docker Redis/MySQL/ClickHouse 上通过，冷启动 ClickHouse native handshake EOF、correlated `EXISTS` 属性过滤和属性索引跨表幂等卡点均已复验修复。
- `analytics-core` 实施方案中的 xwl_bi 代码映射、风险清单、命名映射和验收标准已被评审。
- Umami 官方源码快照、源码审阅文档和实施映射已就位，可为 P1-002 的事件属性、Realtime/Events 查询和 ClickHouse schema 验收提供对照。
- 产品官网能展示产品价值、定价/订阅入口和接入文档入口。
- 产品官网、docs quickstart 和 mail-preview 已完成浏览器截图级验证；后续只做轻量文案和视觉微调，不再阻塞 P1 数据管道。

## P2：分析能力增强

状态：已确定为第二阶段方向，具体范围待评审

目标：

- 从“能收数”推进到“能回答关键产品问题”。

候选范围：

- Funnels。
- Journeys / Paths。
- Segment filtering。
- Custom event schema。
- Email report。
- Basic alerts。
- Saved views。
- 留存分析，是否纳入 P2 需结合 xwl_bi 复用成本评审。

P2 原则：

- 不做全量 BI 平台。
- 每个分析页面只回答一个核心问题。
- 高级能力可以来自 xwl_bi，但必须经过产品化裁剪。

## P3：企业能力

状态：已确定为后续阶段，不进入 P1

目标：

- 支撑更高客单价、更复杂团队和更稳定的商业化运营。

候选范围：

- Team / RBAC。
- Audit log。
- API key。
- Shared dashboards。
- Revenue / Attribution。
- Admin support tools。
- SSO 或企业认证扩展。
- 更细的套餐、用量、超额、账单管理。

P3 原则：

- 只有当 P1/P2 证明产品价值后再展开。
- 优先使用成熟 SaaS 模板和成熟服务的控制面能力。
- 自研重点仍然放在分析数据面，而不是重写企业账号体系。

## 阶段边界表

| 能力                    | P0  | P1         | P2   | P3   |
|-----------------------|-----|------------|------|------|
| Next.js 可交互原型         | 必做  | 维护         | 维护   | 维护   |
| SaaS 模板选型             | 必做  | 定型         | 使用   | 使用   |
| 产品官网 / Marketing Site | 信息架构 | 必做         | 增强   | 增强   |
| 登录/组织/支付/邮件           | 评估  | 模板承接或 mock | 模板承接 | 扩展   |
| `analytics-core` 核心仓库 | 边界确认 | 必做         | 增强   | 增强   |
| Tracker SDK           | 契约  | 必做         | 增强   | 增强   |
| Collect API           | 契约  | 必做         | 增强   | 增强   |
| Realtime              | 原型  | 必做         | 增强   | 增强   |
| Events                | 原型  | 必做         | 增强   | 增强   |
| Goals                 | 原型  | 最小闭环       | 增强   | 增强   |
| Funnels               | 不做  | 不做         | 必做候选 | 增强   |
| Journeys / Paths      | 不做  | 不做         | 候选   | 增强   |
| Team / RBAC           | 不做  | 不做         | 候选   | 必做候选 |
| Revenue / Attribution 产品能力 | 不做  | 不做         | 不做   | 候选   |
