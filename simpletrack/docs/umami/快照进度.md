# Umami Cloud 快照进度

## 当前阶段总览

| 阶段 | 目标 | 必拍编号范围 | 当前状态 | 当前结论 | 待验证问题 |
| --- | --- | --- | --- | --- | --- |
| Phase 01 | 登录、建站、拿到 website id 和安装入口 | `P01-*` | 已完成 | 成功登录 Cloud，创建 `simpletrack-local / localhost` 网站，并在设置页拿到 `website id` 与 tracking code | 无 |
| Phase 02 | 跑通 tracking code 与首批 pageview | `P02-*` | 已完成 | demo 能加载 tracker，settings 页已补成上半区 / tracking code / share / danger zone 四组截图，并补到 Overview 有数据态 | 无 |
| Phase 03 | 跑通事件与属性查看 | `P03-*` | 已完成 | Events 页已补 full layout / Activity / Properties / Filter / 日期下拉，并补到 Events 图表与 Properties 的有数据态 | 若需要更多属性维度，可继续补 `variant / surface` |
| Phase 04 | 记录 Dashboard 基础流程 | `P04-*` | 已完成 | 已补真正的 `Dashboard` 和 `Edit Dashboard`，并与 `Boards` 分开记录 | 无 |
| Phase 05 | 建立组件矩阵证据链 | `P05-*` | 已完成 | 组件选择弹窗、7 种组件预览、过滤弹窗、匹配下拉、Segments 空态、组件落板效果已留档 | 其它组件落板效果可在后续迭代继续扩展 |
| Phase 06 | 记录 Reports 与高级报告页 | `P06-*` | 已完成 | `Funnels / Journeys / Retention / Realtime` 页面和日期下拉已留档，Realtime、Funnels、Journeys 都已补到有数据态 | 无 |
| Phase 07 | 三倍样本下的流量与行为洞察 | `P07-*` | 已截图，有数据态 | 2026-04-25 已完成 `P07-B00` 到 `P07-S07` 截图并重新脱敏；修正为真实 Chrome UA 后，`Sessions / Realtime / Performance / Compare / Breakdown / Filter` 已出现可解释数据；`Goals` 已创建 `Checkout Completed Goal`，结果为 `49 / 1.73k`、转化率 `3%`；2026-04-26 补充 `P07-S08`，证明已保存 Segment 可应用到 Compare 并重算指标 | 继续观察不同日期范围和筛选组合下的稳定性 |
| Phase 08 | 三倍样本下的增长与商业化洞察 | `P08-*` | 已截图，有数据态，Replays 受限 | 2026-04-25 已完成 `P08-B00` 到 `P08-S09` 及 `P08-S05A / P08-S06A` 截图并重新脱敏；`Funnels / Journeys / Retention / Segments / Cohorts / UTM / Revenue / Attribution` 均有可解释证据；`Replays` 明确提示需要 `Business plan` | `Replays` 受套餐限制；Revenue 数值是多轮重跑后的累积站点结果 |

## Dashboard 组件采集表

| 组件名称 | 预览已拍 | 下拉态已拍 | 配置态已拍 | 落板效果已拍 | 已写入操作流 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| Events chart | 是 | 不适用 | 是 | 否 | 是 | 仅保留预览态 |
| Metrics bar | 是 | 不适用 | 是 | 否 | 是 | 仅保留预览态 |
| Metrics table | 是 | 不适用 | 是 | 否 | 是 | 仅保留预览态 |
| Text | 是 | 不适用 | 是 | 否 | 是 | 仅保留预览态 |
| Website chart | 是 | 不适用 | 是 | 是 | 是 | 已实际保存到 board |
| Weekly traffic | 是 | 不适用 | 是 | 否 | 是 | 仅保留预览态 |
| World map | 是 | 不适用 | 是 | 否 | 是 | 仅保留预览态 |

## 操作流清单

| 阶段 | 操作流 | 状态 |
| --- | --- | --- |
| Phase 01 | 登录 -> 站点列表 -> Add website -> 创建 `simpletrack-local` -> 进入设置页 | 已完成 |
| Phase 02 | 打开 Tracking Code -> 启动 demo -> 加载 tracker -> 记录 demo 状态 -> 回刷 Overview 数据 | 已完成 |
| Phase 03 | 触发 DOM event / `track()` / `identify()` / Node API -> 查看 Events -> 查看 Activity / Properties / Filter / 日期范围 -> 回刷有数据态 | 已完成 |
| Phase 04 | 进入 Dashboard -> Edit Dashboard -> 切换到 Boards -> Add board -> 打开 board type 下拉 | 已完成 |
| Phase 05 | 选择 Website board -> 选择 website -> 打开 Select component -> 浏览 7 个预览 -> 添加 Website chart -> 打开 Filter | 已完成 |
| Phase 06 | 展开日期下拉 -> 进入 Funnels / Journeys / Retention / Realtime -> 配置 Funnel -> 查看结果 | 已完成 |
| Phase 07 | 跑浏览器 persona -> 跑批量事件 -> 复验 Sessions / Realtime / Performance / Compare / BreakDown / Goals / Filter -> 应用 Segment | 已登录、已截图、已脱敏；Goals 已保存 checkout 目标并显示结果；Segment 应用态已补充 |
| Phase 08 | 复验 Funnels / Journeys / Retention / Replays / Segments / Cohorts / UTM / Revenue / Attribution -> 记录限制与截图 | 已登录、已截图、已脱敏；Funnels、Segments、Cohorts、Attribution 已补对象或口径，Replays 受套餐限制 |
