# 功能打通矩阵

| 功能 | 数据样本 | 数据来源 | 截图编号 | 当前状态 |
| --- | --- | --- | --- | --- |
| Session | 72 browser personas and 576 batch users, 3 sessions each | pageview, identify, tag, persona context | P07-S01 | 已截图；session count 进入 1.7k 量级，有会话列表 |
| RealTime | browser flows plus CTA events | live pageviews and track() calls | P07-S02 | 已截图；Views / Visitors / Events / Countries 有数据，活动流有命名事件 |
| Performance | data-performance enabled by URL flag | real browser page loads | P07-S03 | 已截图；LCP / FCP / TTFB 等指标有数据 |
| Compare | plan, campaign, cohort, source dimensions | batch event density | P07-S04 | 已截图；当前周期指标与路径表有数据 |
| BreakDown | source, medium, role, plan, cohort properties | batch event density | P07-S05 | 已截图；路径 breakdown 表有数据 |
| Goals | signup, first event, checkout endpoints | Cloud goal configuration | P07-S06 | 已截图；`Checkout Completed Goal` 显示 `49 / 1.73k`，转化率 `3%` |
| Filter | plan, campaign, cohort, role, workspace size | fields and named slices | P07-S07 | 已截图；Filter 弹窗可用，底层 Compare 有数据 |
| Funnels | pricing -> signup -> install -> first_event -> checkout | browser and batch event order | P08-S01 | 已截图；`Growth Baseline Checkout Funnel` 显示 `1.68k -> 45 -> 45 visitors` |
| Journeys | landing, compare, docs, dashboard, checkout paths | real multi-page navigation | P08-S02 | 已截图；路径流有真实非零数据 |
| Retention | 3 cohort labels over 42 logical days | synthetic_date and logical_day | P08-S03 | 已截图；留存矩阵已有 cohort 数据 |
| Replays | browser personas only | recorder availability and account capability | P08-S04 | 已截图；Business plan 限制 |
| Segments | named traffic slices | filter_applied and segment_opened | P08-S05, P08-S05A | 已截图；已保存 `Producthunt Launch Segment`，配置为 `UTM Campaign is producthunt_launch` |
| Cohorts | spring_launch / self_serve_wave / paid_pilot | identify and event properties | P08-S06, P08-S06A | 已截图；已保存 `Paid Checkout Cohort`，配置为 `Triggered event checkout_completed` / `Last 90 days` |
| UTM | producthunt_launch / google_brand / google_competitor / docs_seo / linkedin_founder / email_nurture | URL params and event properties | P08-S07 | 已截图；6 组 campaign 已出现非零 views |
| Revenue | 54 paid workspaces | checkout_completed revenue fields | P08-S08 | 已截图；当前累积站点结果为 `$11.86k / 355 orders` |
| Attribution | campaign -> signup -> checkout | UTM plus revenue fields | P08-S09 | 已截图；`checkout_completed` 归因显示 `49 visitors / 49 visits / 57 views`，并展示 referrer 与 UTM 分布 |

## 2026-04-25 上报执行记录

- 已执行真实浏览器流量：72 个 persona、216 个 browser session、1291 次页面内点击，无脚本错误。
- 已执行批量事件：run id 为 `growth-x3-full-20260425-1843`，17,280 条接受，0 失败。
- 合计预期业务样本：648 用户、1944 session、约 19,440 条业务事件、54 条收入转化。
- 仓库不记录真实 website id；本记录只保留执行结果和复验缺口。
- 2026-04-25 19:40 复核 Cloud UI：`default` Playwright 会话访问目标 analytics URL 后跳转到 `https://cloud.umami.is/login`；该问题已归档为历史阻塞截图 `P07-B00`、`P08-B00`。
- 本地页面 QA：`http://127.0.0.1:49173/site` 已完成 18 张桌面/移动页面截图检查，全部 200、无横向溢出、无脚本错误；2 个 persona smoke 跑通 6 个 session / 36 次点击。
- 2026-04-25 登录后复验：`P07-S01` 到 `P08-S09` 已全部采集并重新脱敏，另补 `P08-S05A / P08-S06A` 配置态截图。
- 根因修正：早期空态主要来自脚本使用自定义 User-Agent，Cloud 将这类请求按 bot / 非人类流量处理；切换为普通 Chrome UA 后，写入响应开始带 `sessionId / visitId`，Cloud 读接口也开始返回真实数据。
- 最新读侧证据：Overview stats 为 `pageviews=118 / visitors=2 / visits=2`，Sessions count 为 `1733`，Realtime 返回 112 条活动项，Events 按事件名聚合，Revenue sessions count 为 `100`；Goals 报告返回 `49 / 1731`，Attribution 在 `checkout_completed` 口径下显示 `49 visitors / 49 visits / 57 views`。
- Revenue 当前是多轮 smoke、旧 UA、修正 UA 和重跑后的累积站点结果；默认样本口径仍是单次 `growth-baseline-x3` 的 54 条收入转化。
- `configure-cloud-reports.mjs` 已把 Goal、Funnel、Segment、Cohort 的 Cloud UI 配置固化为可重复幂等步骤；`capture-cloud-snapshots.mjs` 会在配置态准备失败时停止，避免产出错误截图。

## 状态说明

- 本地仿真站、浏览器流量脚本和批量事件脚本已经提供数据来源。
- `send-event.mjs` 和 `bulk-send.mjs` 默认直发端点已改为 `https://api-gateway.umami.dev/api/send`，与真实浏览器脚本一致。
- 当前主要问题已从“未登录、未截图、读侧全空”切换为“Replays 受套餐限制，以及 Revenue 数值需注明多轮重跑累积口径”。
- `Goals / Funnels / Segments / Cohorts / Attribution` 已完成对象配置或转化口径校准；Segments/Cohorts 当前证明的是“可保存、可复用的人群对象”，列表页本身不直接展示人数。
- `Replays` 当前还存在套餐限制：页面提示需要 `Business plan`，且网站元数据 `replayEnabled=false`。
