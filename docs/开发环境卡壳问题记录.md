# 开发环境卡壳问题记录

> 用途：记录开发、验证、推送过程中遇到的卡壳问题、原因判断、解决方式和后续规则。不要把详细排障过程写进 README，README 只保留项目初始化和常用命令入口。

## 记录规则

- 遇到依赖安装、网络代理、SSH 权限、子仓库推送、构建验证、数据库连接等问题时，在本文件追加记录。
- 每条记录写清楚现象、原因、处理方式、验证结果和后续避免方式。
- 如果记录里标注任务进度或验收状态，必须写明对应仓库的 git commit id；未提交时标注 `pending commit` 或 `未提交`，提交后回填真实 commit id。
- 如果问题影响 SimpleTrack 阶段计划或实施状态，再同步更新 `simpletrack/docs/实施决策/README.md`。
- 不要记录 token、cookie、私钥、账号密码等敏感信息。

## 坑位速查索引

| 现象关键词 | 第一处理动作 | 详细记录 |
| --- | --- | --- |
| `index.lock: Permission denied` | 先查父仓或子仓 `.git` 目录 ACL，不要反复重跑 commit | [子仓 `.git` 目录 ACL 阻止当前沙箱提交](#2026-05-09子仓-git-目录-acl-阻止当前沙箱提交) |
| 子代理 timeout / `429 Too Many Requests` | 不得声明审查完成，缩小范围后重新跑真实审查 | [代码审查子代理超时或 429 不能算完成](#2026-05-09代码审查子代理超时或-429-不能算完成) |
| SSH 能手动认证但 Codex 推送失败 | 区分交互式用户和 Codex 沙箱身份，先查 `config_simpletrack` / 私钥 ACL | [临时可写 clone 已生成子仓提交，但当前沙箱无权推送](#2026-05-09临时可写-clone-已生成子仓提交但当前沙箱无权推送) |
| `(authenticated)` 变成命令名 | 不要把 Next.js route group 完整路径传给 Windows test runner，改用测试文件名过滤 | [Windows 下 Vitest 路径包含 `(authenticated)` 导致命令解析失败](#2026-05-07windows-下-vitest-路径包含-authenticated-导致命令解析失败) |
| GitHub 网络 reset / timeout | 先直连重试；仍失败时使用 `http://localhost:64320`，再试 `http://localhost:7897` | [临时可写 clone 已生成子仓提交，但当前沙箱无权推送](#2026-05-09临时可写-clone-已生成子仓提交但当前沙箱无权推送) |
| DeepSeek UI 评审暂不可用 | 记录为用户明确临时豁免，不把 DeepSeek 未运行写成已完成；保留 Codex 子代理和 Gemini 真实审查证据 | [DeepSeek UI 评审临时豁免](#2026-05-09deepseek-ui-评审临时豁免) |
| DeepSeek CLI 已恢复可用 | 先用 `--help`、`doctor` 和最小 `exec` 确认 CLI/API 真的通，再接回 UI 外部评审；`review` 子命令不要直接塞 git revision range | [DeepSeek CLI 恢复可用并重新接回审查流程](#2026-05-09deepseek-cli-恢复可用并重新接回审查流程) |

## 2026-05-09：DeepSeek UI 评审临时豁免

现象：

- Goals readback UI 状态拆分属于 UI/页面状态改动，按 `AGENTS.md` 原规则需要 Codex 子代理、`$ai-slop-cleaner`、Gemini 和 DeepSeek 外部 UI 评审。
- Codex 子代理已真实返回 `PASS`，Gemini 已真实返回 `PASS`。
- 用户明确说明：`DeepSeek 暂时不能用先不管了`。
- 当前版本标记：`simpletrack-saas` commit `e86ba88`；父仓 rebase 后同步记录 commit `b672e96`。

原因：

- 这是用户对当前环境限制给出的临时豁免，不是 DeepSeek 审查已经完成。
- 不能把未运行的 DeepSeek 写成通过，也不能让它阻塞已经具备其他真实审查和测试证据的提交。

处理方式：

- 本轮继续使用 Codex 子代理审查、Gemini 审查、目标 Vitest、type-check、lint 和 `git diff --check` 作为交付证据。
- 最终回复与修订记录中明确写明 DeepSeek 是按用户指令临时跳过。

验证结果：

- `src/simpletrack-saas` commit `e86ba88` 已推送到远端。
- 已有测试证据：`corepack pnpm --filter saas test -- readback-pages.test.tsx`、`corepack pnpm --filter saas run type-check`、`corepack pnpm lint`、`git diff --check`。

后续避免方式：

- DeepSeek 恢复可用后，涉及 UI 的后续提交继续按 `AGENTS.md` 双外部评审规则执行。
- 如果再次临时豁免，必须在卡壳记录和最终交付中写清楚，不得伪造 DeepSeek 结论。

## 2026-05-09：DeepSeek CLI 恢复可用并重新接回审查流程

现象：

- 用户提醒 “deepseek 现在应该是可以用了”，需要确认不是只有二进制文件存在，而是 CLI、认证和 API 调用都真正恢复。

定位：

- `deepseek --help` 正常返回命令列表，说明二进制可执行。
- `deepseek doctor` 显示 `API connection successful (model: deepseek-v4-pro)`，说明 provider key 和 DeepSeek API 连通性已恢复。
- `deepseek exec 'Reply with exactly: DEEPSEEK_OK'` 返回 `DEEPSEEK_OK`，说明非交互最小调用可用。
- `deepseek review HEAD~1..HEAD` 报 `unexpected argument 'HEAD~1..HEAD' found`，说明 `review` 子命令不是接收 git revision range 的通用入口。

结论：

- DeepSeek 现在可以重新接回 `AGENTS.md` 规定的 UI 外部评审流程。
- 后续要先确认 CLI/API 通，再决定用 `review` 还是改走 `exec` + 明确 prompt / 文件列表的方式做真实审查。

后续规则：

- 涉及 UI 的提交继续优先按 `AGENTS.md` 使用 DeepSeek 外部评审，不再默认按“临时豁免”处理。
- 如果 `review` 子命令仍不接受当前 diff 输入方式，就改用 `deepseek exec` 携带目标、文件列表、关键 diff 和已跑测试，不要再硬塞 git range 参数。

## 2026-05-09：子仓 `.git` 目录 ACL 阻止当前沙箱提交

现象：

- 在 `src/simpletrack-saas` 提交 Goal 并发唯一约束修复时失败：
  - `fatal: Unable to create 'C:/Users/admin/Documents/src/uba/src/simpletrack-saas/.git/index.lock': Permission denied`
- 随后检查到父仓 `.git`、`src/analytics-service/.git`、`src/analytics-core/.git` 也存在类似显式 `DENY` ACL。
- 当前进程身份为 `desktop-m23j16k\codexsandboxoffline`。
- `icacls src/simpletrack-saas/.git` 显示存在显式 `DENY` ACE，当前沙箱无法写 Git index。
- 2026-05-09 再次尝试只 stage `AGENTS.md` 时仍失败：
  - `fatal: Unable to create 'C:/Users/admin/Documents/src/uba/.git/index.lock': Permission denied`
- 当前版本标记：父仓 commit `674c1de` + 工作区未提交改动，`AGENTS.md` 进度 commit id 规则为 `pending commit`。

原因：

- 代码文件位于可写工作区内，但子仓 Git 元数据目录存在 Windows ACL 拒绝规则。
- 这会阻止父仓和子仓执行 `git add`、`git commit --amend`、`git commit` 等需要写 `.git/index.lock` 的操作。

处理方式：

- 本轮不能用管理员权限修 ACL，因为当前会话不允许提权。
- 继续完成代码修改、文档更新和验证；提交/推送需要在具备 `.git` 写权限的 PowerShell 或修复 ACL 后执行。
- 不要把这个问题误判成测试失败或代码生成失败。

验证结果：

- `go test`、`go vet`、`golint`、`go doc`、SaaS target tests、type-check 和 lint 已在当前工作区验证通过。
- 仅 Git 元数据写入被 ACL 阻塞。

后续规则：

- 如果再次出现 `index.lock: Permission denied`，先检查 `.git` 目录 ACL，而不是反复重跑提交命令。
- 修复方式应在管理员 PowerShell 中调整子仓 `.git` ACL，或用具备写权限的普通终端执行提交。

## 2026-05-09：代码审查子代理超时或 429 不能算完成

现象：

- P1 Goal 最小闭环进入代码审查时，架构审查子代理实际返回 `WATCH` 结论，但另一路代码质量审查子代理出现超时或 `429 Too Many Requests`。
- 当前版本标记：`analytics-core` commit `b820f7e`，`analytics-service` commit `f60ac59`，`simpletrack-saas` commit `e86ba88`，父仓文档 commit `b672e96`。

原因：

- 子代理启动成功不代表审查完成；只有拿到包含文件、行号、结论或明确无阻塞发现的返回，才算真实审查证据。
- 429 或 timeout 是工具执行失败，不是代码质量通过。

处理方式：

- 不能把“子代理超时、主代理本地复核”写成“子代理已审查”。
- 对超时或 429 的审查通道，必须重新发起更窄范围的审查，或明确记录“该通道未完成”。
- 已在 `AGENTS.md` 固化规则：代码审查必须真实完成才能声明完成。

验证结果：

- 架构审查通道已真实返回 `WATCH`，关注 Goal readback contract 复制、单页最多 25 个 Goal 的 count fan-out、readback policy 控制面闭环和内部读权限粒度。
- 代码质量复审后来已真实返回 `PASS`，覆盖 `analytics-core` Goal count read side、`analytics-service` `/v1/goals`、`simpletrack-saas` Goal readback 与 `P2002` duplicate-event 映射；仍保留的风险是 Goals 页面按 goal 数量线性 fan-out readback，尚未做 live browser / e2e 全链路验证。

后续避免方式：

- 提交说明、修订记录和最终回复只引用真实返回的审查结论。
- 如果审查代理不稳定，先拆小审查范围，必要时换用 verifier 子代理，但仍必须等待实际返回。

## 2026-05-09：临时可写 clone 已生成子仓提交，但当前沙箱无权推送

现象：

- 当前工作区 `.git` 目录 ACL 阻止 `git add` / `git commit` 后，在 `.tmp/commit-goal-20260509-092600/` 建立了可写临时 clone。
- 临时 clone 内已经生成两个子仓提交：
  - `analytics-service` commit `f60ac59`：内部 `/v1/goals`、OpenAPI、配置项和回归测试。
  - `simpletrack-saas` commit `461900a`：Goal 创建时把 Prisma `P2002` 唯一冲突映射为 `duplicate_event`。
- `analytics-core` 当前待推送提交仍是 commit `b820f7e`。
- 推送失败原因不是提交内容，而是当前沙箱没有可用 GitHub 凭据：
  - 读取 `C:/Users/admin/.ssh/config_simpletrack` 失败：`Permission denied`。
  - 直连 GitHub SSH 22 / 443 被当前环境拒绝。
  - 使用 `http://localhost:64320` 代理和临时 `ProxyCommand` 可连到 `ssh.github.com:443`，但读取 `C:/Users/admin/.ssh/id_ed25519_simpletrack` 失败：`Permission denied`。
  - HTTPS + `http.sslBackend=openssl` 可以 `ls-remote`，但推送没有 username/token，失败于 `could not read Username for 'https://github.com'`。
- 用户在交互式 PowerShell 中验证 `ssh -F "$env:USERPROFILE\.ssh\config_simpletrack" -T git@github-simpletrack` 已成功返回 `Hi RoggerZ!`，说明 GitHub key 和账号授权本身可用；剩余问题是 Codex 沙箱进程与用户交互式 PowerShell 的 Windows 身份不同，Codex 仍读不到 `config_simpletrack` / 私钥。
- 当前版本标记：父仓历史基线 commit `674c1de`，后续 rebase 收口 commit `b672e96`；`analytics-core` commit `b820f7e`；`analytics-service` commit `f60ac59`；`simpletrack-saas` commit `e86ba88`。

原因：

- 代码工作区可写，Git 元数据和 SSH 凭据文件不可写或不可读。
- 当前沙箱进程身份为 `desktop-m23j16k\codexsandboxoffline`，无法修改 `.git` ACL，也无法读取专用 SSH 私钥。
- `git config http.proxy` 只影响 HTTPS Git 请求，不会自动让 SSH remote 走代理；SSH 代理需要 `ProxyCommand` 或显式 `GIT_SSH_COMMAND`。

处理方式：

- 不继续重复 `git push`，先把本轮可复用提交和失败点记录下来。
- 后续可选做法：
  - 用有权限的 PowerShell 修复 `.git`、`config_simpletrack` 和 `id_ed25519_simpletrack` ACL 后，在当前仓库或临时 clone 中推送。
  - 或把 `id_ed25519_simpletrack` 加载到 ssh-agent，再使用 `ssh.github.com:443` + HTTP CONNECT 代理推送。
  - 或配置可用 GitHub HTTPS token / credential helper，再用 `http.proxy=http://localhost:64320` 和 `http.sslBackend=openssl` 推送。
- 已同步更新 `simpletrack/docs/Q&A/Windows-SSH仓库权限怎么配置.md`，补充 `icacls` 的 PowerShell 正确写法、Codex 沙箱身份差异和手动推送命令。

验证结果：

- 代码验证已经通过：
  - `analytics-core` commit `b820f7e`：`go test ./storage/...`。
  - `analytics-service` commit `f60ac59`：`go test ./internal/collectapi`、`go test ./...`、`go vet ./...`、`golint ./...`。
  - `simpletrack-saas` commit `461900a`：Goal/readback 目标 Vitest、`@repo/database` type-check、`saas` type-check、root lint；后续 Goals readback UI 状态拆分已推进到 commit `e86ba88`。

后续避免方式：

- 看到 `Permission denied` 时先区分三类：工作区文件权限、Git 元数据 ACL、SSH 凭据 ACL。
- 如果只能读代码但不能写 `.git`，可以先用 `.tmp/` 临时 clone 形成可复用提交；但推送仍需要凭据文件或 credential helper 可读。

## 2026-05-07：Windows 下 Vitest 路径包含 `(authenticated)` 导致命令解析失败

现象：

- 在 PowerShell 中执行带完整 Next.js route group 路径的测试命令时失败。
- 典型命令包含：
  - `app/(authenticated)/(main)/(organizations)/[organizationSlug]/websites-page.test.tsx`
- 典型报错：
  - `The term 'authenticated' is not recognized as a name of a cmdlet, function, script file, or executable program.`
  - 或 `/(main)/(organizations)/[organizationSlug]/websites-page.test.tsx was unexpected at this time.`

原因：

- 第一层问题是 PowerShell 会把未加引号的 `(authenticated)` 当成表达式或裸标识符解析。
- 第二层问题是 `corepack pnpm ... test -- <path>` 在 Windows 下可能再进入 `cmd.exe`，即使 PowerShell 使用单引号，`cmd.exe` 仍可能把括号当命令分组符号处理。
- 所以这不是测试文件、Next.js route group 或项目代码错误，而是 shell 参数解析错误。

处理方式：

- 不要把包含 route group 括号的完整路径直接传给 test runner。
- 优先使用 Vitest 文件名过滤：

```powershell
corepack pnpm --dir "C:\Users\admin\Documents\src\uba\src\simpletrack-saas\apps\saas" test -- websites-page.test.tsx website-management.test.ts
```

- 如果必须读取本地文件内容，用 `-LiteralPath` 和完整单引号路径：

```powershell
Get-Content -LiteralPath 'C:\Users\admin\Documents\src\uba\src\simpletrack-saas\apps\saas\app\(authenticated)\(main)\(organizations)\[organizationSlug]\websites\page.tsx'
```

验证结果：

- 使用文件名过滤后，`websites-page.test.tsx` 和 `website-management.test.ts` 相关 Vitest 目标测试通过。

后续避免方式：

- Windows 下运行 `pnpm` / `npm` / `corepack` 命令时，凡是路径中包含括号，优先不用完整路径作为参数。
- AGENTS.md 已补规则：测试命令使用文件名过滤或不含括号的相对路径；文件读取使用 `-LiteralPath`。

## 2026-05-07：Windows 下 `pnpm` / `corepack pnpm` 对 route group 路径的括号二次解析

现象：

- 执行 `corepack pnpm --dir apps/saas exec vitest run 'app/(authenticated)/(main)/...'` 时失败。
- 报错可能变成：
  - `The term 'authenticated' is not recognized as a name of a cmdlet...`
  - 或 `/(main)/(organizations)/[organizationSlug]/... was unexpected at this time.`

原因：

- PowerShell 的单引号并不能保证后续 `corepack` / `cmd.exe` 链路里括号路径还保持原样。
- `app/(authenticated)` 这种 Next.js route group 路径一旦作为命令行参数在 Windows 上经过多层 shell，就可能被当成分组表达式而不是普通文件路径。

处理方式：

- 不要把包含括号的完整 route group 路径直接传给 `pnpm` / `corepack pnpm` 的测试参数。
- 优先跑包级 Vitest 全量集合，或使用不带括号的文件名过滤。
- 如果必须读文件，继续使用 `Get-Content -LiteralPath`。

验证结果：

- `corepack pnpm --dir apps/saas exec vitest run` 通过，9 个测试文件、71 个测试全部通过。

后续避免方式：

- Windows 下不要把 `app/(authenticated)/...` 作为 shell 参数传给测试 runner。
- 需要定位单测时，优先按文件名过滤，不要按完整 route group 路径过滤。

## 2026-05-03：属性索引接入 ingestion 后的跨表幂等风险

## 2026-05-03：`simpletrack-saas` runtime-source 单测在干净环境下提前触发 Prisma client

现象：

- 执行 `corepack pnpm --filter @repo/api test` 时，`packages/api/modules/simpletrack/runtime-source.test.ts` 在模块加载阶段就失败。
- 失败信息是 `DATABASE_URL is not set`，并不是测试用例断言失败。
- 即使测试注入了 `lookupWebsiteByWriteKey` mock，顶层 `import { getWebsiteByWriteKey } from "@repo/database"` 仍会在导入时初始化 Prisma client。

原因：

- `runtime-source.ts` 把数据库查询函数放在了模块顶层 import。
- `@repo/database` 在初始化 Prisma client 时会检查 `DATABASE_URL` 是否存在，因此 clean test env 会在进入 handler 前直接报错。
- 这个问题会让本来只想测 handler 分支的单测，也被迫依赖数据库环境变量。

处理方式：

- 将数据库 lookup 改成 handler 边界上的懒加载，只有在没有注入 `lookupWebsiteByWriteKey` 时才动态 import `@repo/database`。
- 保留测试注入点，让单测继续用 mock lookup 跑完，不再依赖 `DATABASE_URL`。

验证结果：

- `corepack pnpm --filter @repo/api test` 在不设置 `DATABASE_URL` 的情况下通过。
- `corepack pnpm --filter @repo/api type-check` 通过。
- `corepack pnpm --filter @repo/database type-check` 通过。

后续避免方式：

- 只测 handler 分支时，优先让数据库依赖可注入，不要把 Prisma client 绑在模块顶层 import 上。
- 如果确实要在模块顶层引用数据库层，请在测试说明里明确写出最小环境变量要求，避免 clean env 下反复踩坑。

现象：

- 将 `PropertyBatchWriter` 组合进 ingestion 热路径时，主代理初版实现是：事件行写入成功后写 `_properties` 表，再提交 MySQL 属性状态。
- 代码复审指出两个隐藏风险：
  - 如果 ClickHouse 属性行已经写入成功，但 MySQL 属性状态 `Commit` 失败，队列会重试；若下一次自动 reclaim，就可能重复写同一批属性行。
  - 如果多个消费者同时重试同一个 failed 属性状态，普通 `SELECT` 后再 `UPDATE` 可能让两个消费者都认为自己抢到了写入权，导致并发重复写。

原因：

- ClickHouse 属性表和 MySQL 状态表不是同一个事务，不能假设“ClickHouse Send 成功”和“MySQL Commit 成功”同时发生。
- append-only ClickHouse `MergeTree` 属性表当前没有 ClickHouse 侧唯一约束或替换去重能力，不能依赖存储层自动消除重复属性行。
- `processing` 状态可能代表“ClickHouse 已写入但 MySQL commit 失败”的结果不明状态，不能把它当作 failed 自动重试。

处理方式：

- 新增 `storage.PropertyIndexingEventWriter`，让 ingestion 仍只依赖 `storage.EventWriter`，属性写入组合留在 storage 边界。
- 新增 MySQL `property_indexing_status` guard，属性索引使用独立 checkpoint：
  - `inserted`：重复消息直接跳过属性写入。
  - `failed`：允许重试，但必须用 `UPDATE ... WHERE status = 'failed'` 原子抢占，并检查 `RowsAffected == 1`。
  - `processing`：视为 ambiguous，不自动 reclaim，避免 ClickHouse 已写入后再次追加同一批属性行。
- `PropertyIndexingEventWriter` 在属性批量写入失败时 rollback 为 `failed`；在 ClickHouse 写入成功但 MySQL commit 失败时不 rollback，保留 `processing` 作为结果不明状态。

验证结果：

- 子代理按 `AGENTS.md` 结对审查规则复审，确认两个 duplicate-row 风险已关闭；非阻塞遗留是 ambiguous `processing` 需要后续运维恢复策略。
- 已通过：
  - `go test ./internal/storage/...`
  - `go test ./internal/e2e/...`
  - `go test ./...`
  - `go test -run Example ./...`
  - `go vet ./...`
  - `golint ./...`
  - `go doc -all` 全包检查
  - `ANALYTICS_CORE_E2E=1 go test -count=1 ./internal/e2e -v`

后续避免方式：

- 涉及 ClickHouse 写入 + MySQL 状态组合时，不要把 `processing` 当成可自动重试状态；必须先证明前一次 ClickHouse 写入没有成功，或引入 ClickHouse 侧去重/替换策略。
- 任何 failed 状态 reclaim 必须是原子状态迁移，并检查影响行数。
- 后续如果要自动恢复 ambiguous `processing`，应作为 P1.5/P2 运维任务处理，例如引入状态超时扫描、人工修复队列、ReplacingMergeTree/projection 去重，或属性表按 `(tenant_id, project_id, source_id, event_id, scope, name)` 做可验证去重。

## 2026-05-02：ClickHouse 属性过滤 correlated EXISTS 不支持外层 alias

现象：

- 为 `analytics-core` 的 Events 查询增加 property filter 后，dry-run query builder 单元测试通过，但真实 ClickHouse e2e 失败。
- 失败发生在 `TestPropertyBatchWriterToClickHouse` 通过 `EventReader.ListEvents` 查询属性过滤结果时。
- 典型错误：
  - `Resolved identifier 'e.event_id' in parent scope to expression 'e.event_id' with correlated column 'event_id'`
- 触发 SQL 形态类似：

```sql
SELECT ...
FROM `events_xxx` AS e
WHERE EXISTS (
  SELECT 1
  FROM `events_xxx_properties` AS p
  WHERE p.event_id = e.event_id
)
```

原因：

- GORM dry-run 只能证明 SQL 被构建出来，不能证明目标 ClickHouse 版本能执行该 SQL。
- 当前本地 ClickHouse 25.3 对这种 correlated `EXISTS` 子查询和外层表 alias 的解析不兼容，子查询里引用 `e.event_id` 会被拒绝。
- 这个问题不是 property writer 写入失败，也不是数据没写进去，而是读侧属性过滤 SQL 形态不适合当前 ClickHouse。

处理方式：

- 将属性过滤从 correlated `EXISTS` 改为 ClickHouse 可执行的 tuple `IN (SELECT ...)` 半连接形态。
- 新 SQL 思路：

```sql
WHERE (tenant_id, project_id, source_id, event_id) IN (
  SELECT tenant_id, project_id, source_id, event_id
  FROM `events_xxx_properties`
  WHERE tenant_id = ?
    AND project_id = ?
    AND source_id = ?
    AND property_scope = ?
    AND property_name = ?
    AND property_type = ?
    AND string_value = ?
)
```

- 属性名、scope、value type 和属性值仍然走 allowlist + 绑定参数，只有物理属性表名由 ClickHouse `TableRouter` 派生。

验证结果：

- `go test ./internal/storage/clickhouse ./internal/e2e` 通过。
- 冷启动后执行 `ANALYTICS_CORE_E2E=1 go test -count=1 ./internal/e2e -v` 通过。
- 验证结束后已执行 `docker compose down` 停止 Redis/MySQL/ClickHouse。

后续规则：

- ClickHouse 查询 builder 的 dry-run 测试只能作为 SQL 结构检查；新增非平凡 SQL 形态时必须补真实 ClickHouse e2e。
- 避免在 ClickHouse 查询中依赖 correlated subquery 访问外层 alias；优先使用 tuple `IN`、显式 JOIN 或预聚合表方案。
- 属性过滤、漏斗、留存、分群等后续查询只要引入子查询，都要在本地 ClickHouse 版本上复验执行，而不能只看 GORM dry-run 输出。

## 2026-05-02：analytics-core e2e 冷启动 ClickHouse native handshake EOF

现象：

- 在 `src/analytics-core` 执行依赖冷启动后立即运行 `go test ./internal/e2e -v`，测试可能失败在 ClickHouse native 连接阶段。
- 典型错误类似：`ping native clickhouse failed: EOF` 或 native handshake EOF。
- 同一轮输出里还可能看到 MySQL 驱动打印的 `[mysql] ... unexpected EOF`。这类日志出现在容器刚启动但 MySQL 尚未完成握手时，不等同于最终测试失败，最终结果以 `go test` 的退出码和 `PASS/FAIL` 为准。

原因：

- `docker compose up -d` 只表示容器已启动，不代表 Redis、MySQL、ClickHouse 已经完成协议握手、认证和数据库初始化。
- 旧版 e2e 测试在 compose 启动后马上打开 Redis/MySQL/ClickHouse，并且 ClickHouse native 只做一次 `Ping`；ClickHouse TCP 端口可能已经开放，但 native server 还没有准备好完成握手，因此出现 EOF。
- 原测试上下文只有 20 秒，覆盖不了 Windows + Docker Desktop 冷启动时偶发的慢启动窗口。

处理方式：

- 在 `src/analytics-core/internal/e2e/pipeline_test.go` 为 Redis、MySQL、ClickHouse native、ClickHouse GORM 连接增加 readiness retry loop。
- 统一使用：
  - `e2eDependencyTimeout = 90 * time.Second`
  - `e2eDependencyPollInterval = 500 * time.Millisecond`
- ClickHouse native 连接失败或 `Ping` 失败时关闭当前连接并重试，避免把启动期 EOF 当成真实写入链路失败。
- MySQL 连接先用 `database/sql` 做 readiness 探活，探活成功后再创建生产路径使用的 GORM 连接。
- 顺手补了 `TestPropertyBatchWriterToClickHouse`，用真实 ClickHouse 表验证 `PropertyBatchWriter` 能把 `event_properties` / `user_properties` 展开记录写入并查询回来。

验证方式：

```powershell
cd C:\Users\1\Documents\git\uba\src\analytics-core
docker compose down
docker compose up -d
$env:ANALYTICS_CORE_E2E = '1'
go test -count=1 ./internal/e2e -v
docker compose down
```

本次验证结果：

- 冷启动后 `TestCollectToRealtimeAndEventsPipeline` 通过。
- 冷启动后 `TestPropertyBatchWriterToClickHouse` 通过。
- 验证结束后已执行 `docker compose down` 停止 Redis/MySQL/ClickHouse。

后续规则：

- 以后排查 e2e 数据库连接失败时，先确认是否是冷启动 readiness 问题，不要把第一次 EOF 直接当成 ClickHouse 写入代码问题。
- 涉及 Docker Compose 依赖的 e2e 必须用显式 readiness retry，并用 `go test -count=1` 做冷启动复验，避免缓存掩盖问题。
- 只要启动/复验过 compose，结束时必须执行 `docker compose down`，不要让本地依赖容器长期挂着。

## 2026-05-01：analytics-core Docker Compose Redis 端口冲突

现象：

- 在 `src/analytics-core` 执行 `docker compose up -d` 时，MySQL 和 ClickHouse 镜像拉取并启动成功，但 Redis 容器启动失败。
- Docker 报错：`Bind for 0.0.0.0:6379 failed: port is already allocated`。

原因：

- 本机已有 Redis 或其他服务占用了默认 `6379` 端口。
- analytics-core 的 compose 初版直接映射 `6379:6379`，不适合在已有 Redis 的开发机上作为默认配置。

处理方式：

- 已执行 `docker compose down` 清理本次部分启动的容器。
- 已将 `src/analytics-core/docker-compose.yml` 默认端口改为更高位端口，并支持环境变量覆盖：
  - Redis：`26379`
  - RedisInsight：`28001`
  - MySQL：`23306`
  - ClickHouse HTTP：`28123`
  - ClickHouse native TCP：`29000`
- 已同步更新 `src/analytics-core/README.md` 的本地运行说明。
- 第二次启动时发现 `13306` 也被占用，因此默认端口继续上移到当前端口组。
- Redis Stream 集成测试使用 `localhost:26379` 时出现过一次 `i/o timeout`，改用 `127.0.0.1:26379` 后稳定通过。

验证结果：

- `docker compose config` 通过。
- `go test ./...`、`go vet ./...`、`git diff --check` 通过。

后续规则：

- analytics-core 本地运行依赖默认使用高位端口，避免覆盖开发机已有数据库。
- 如需使用标准端口，通过 `ANALYTICS_CORE_REDIS_PORT` 等环境变量显式覆盖。
- Windows + Docker Desktop 下运行 Redis 集成测试优先使用 `127.0.0.1:26379`，避免 `localhost` 解析带来的偶发连接问题。

## 2026-04-30：Supastarter 依赖安装与验证

现象：

- `src/simpletrack-saas` 使用旧 Node 版本安装依赖时，Prisma preinstall 会失败。
- npm / pnpm 网络不稳定时，依赖下载会卡住或落到不稳定镜像源。
- `saas` type-check 在 Prisma generated client 未生成时会报 `packages/database/prisma/generated/client` 缺失。

原因：

- Supastarter 当前依赖的 Prisma 要求 Node 20.19+、22.12+ 或 24.0+，Node 22.10.0 不满足要求。
- 本地网络访问 npm registry 可能不稳定，需要走本机代理。
- `saas` app 依赖数据库包生成的 Prisma client。

处理方式：

```powershell
$env:HTTP_PROXY = "http://localhost:7897"
$env:HTTPS_PROXY = "http://localhost:7897"
$env:npm_config_proxy = "http://localhost:7897"
$env:npm_config_https_proxy = "http://localhost:7897"
$env:npm_config_registry = "https://registry.npmjs.org/"
```

```powershell
& "$env:USERPROFILE\AppData\Local\nvm\v24.1.0\npm.cmd" exec --yes pnpm@10.9.0 -- --config.manage-package-manager-versions=false --config.package-manager-strict=false --registry=https://registry.npmjs.org/ install --frozen-lockfile
```

```powershell
$env:DATABASE_URL = "postgresql://postgres:postgres@127.0.0.1:5432/simpletrack"
& "$env:USERPROFILE\AppData\Local\nvm\v24.1.0\npm.cmd" exec --yes pnpm@10.9.0 -- --config.manage-package-manager-versions=false --config.package-manager-strict=false --filter @repo/database run generate
```

验证结果：

- `pnpm install --frozen-lockfile` 已通过。
- `pnpm --filter @repo/database run generate` 已通过。
- `pnpm --filter saas --filter marketing --filter docs run type-check` 已通过。

后续规则：

- Windows 本地优先使用 Node 24.1.0 验证 `src/simpletrack-saas`。
- 网络失败先使用 `http://localhost:7897` 代理和官方 npm registry。
- 跑 `saas` type-check 前先确保 Prisma client 已生成。

## 2026-04-30：Windows 后台启动 Next dev 时环境变量引号被吃掉

现象：

- 使用 `Start-Process powershell -ArgumentList "-Command", "...$env:NEXT_PUBLIC_DOCS_URL=\"http://localhost:3002\"..."` 启动 dev server 时，日志出现：
  - `http://localhost:3002 : 无法将“http://localhost:3002”项识别为 cmdlet...`
  - `postgresql://... : 无法将“postgresql://...”项识别为 cmdlet...`

原因：

- Windows PowerShell 的多层 `-ArgumentList` / `-Command` 字符串转义容易丢失引号，导致环境变量赋值右侧被当成命令执行。

处理方式：

- 后台启动复杂 PowerShell 脚本时，优先使用 `-EncodedCommand`。
- 先构造完整脚本，再用 UTF-16LE 转 Base64：

```powershell
$script = @"
`$env:NEXT_PUBLIC_DOCS_URL = 'http://localhost:3002'
`$env:NEXT_PUBLIC_SAAS_URL = 'http://localhost:3000'
`$env:DATABASE_URL = 'postgresql://postgres:postgres@127.0.0.1:5432/simpletrack'
& '$npm' exec --yes pnpm@10.9.0 -- --filter marketing run dev
"@
$encoded = [Convert]::ToBase64String([Text.Encoding]::Unicode.GetBytes($script))
Start-Process powershell -ArgumentList @("-NoProfile", "-ExecutionPolicy", "Bypass", "-EncodedCommand", $encoded)
```

验证结果：

- marketing、docs、mail-preview 均成功监听对应端口。

后续规则：

- Windows 后台启动包含 URL、数据库连接串、引号的命令时，优先使用 `-EncodedCommand`，不要拼多层裸字符串。

## 2026-04-30：Playwright CLI 缺少浏览器二进制

现象：

- 使用 `pnpm exec playwright screenshot` 截图时报错：
  - `Executable doesn't exist at ... chromium_headless_shell...`
  - 提示需要执行 `playwright install`。

原因：

- Playwright 包已安装，但本机缺少对应 Chromium 浏览器二进制。

处理方式：

- 本轮没有直接执行 `playwright install`，因为这会下载并安装新的浏览器二进制。
- 改用 Codex in-app browser / Playwright MCP 完成本地页面截图验证。

验证结果：

- 已通过浏览器截图验证 marketing、docs quickstart、mail-preview。

后续规则：

- 如果后续需要稳定使用 shell Playwright 截图或 E2E，再安装 Playwright 浏览器二进制。
- 在安装前先确认这是当前任务必需项；非必需时优先使用已可用的 in-app browser / Playwright MCP。
